# 版管理（バージョン履歴）— 目標B

すべての修正・検証に識別番号をつけて記録する。

\---

## 目標B：初語（音に意味がつく）

|版|変更内容|結果|状態|
|-|-|-|-|
|**B-1**|身体シミュレーション＋脳の拡張。胃（消化→空腹）、肺（肺活量→発声モーラ数）、内受容感覚（つらさ1次元）、島皮質（体→脳への入力線）、恒常性の本能（つらさ低下→報酬）、泣き（段階的・確率的）、イベント駆動時間、親シミュレータ、手動CLI、スケジュール設定yaml|基本ループは動作。1日シミュレーション：泣き70,297回（多すぎ）、睡眠62回（頻繁すぎ）。食事が一瞬で入る、泣きが毎秒判定、寝入りが即時という問題あり|✅ → B-2で修正|
|**B-2**|行動を「一瞬」から「期間がある」に修正。授乳＝30分間、泣き＝数分間継続、寝入り＝うとうと5-10分→睡眠2-4時間。胃の消化速度を新生児に合わせて調整|1日シミュレーション：泣き43回、食事32回、睡眠6回。人間の新生児に近づいた。オウム返しも出る（転移テスト0.90-0.99）|✅ → B-3で修正|
|**B-3**|血管（血中栄養素）を追加し「胃空=空腹MAX」を解消。定期授乳を廃止し「泣いて空腹のときだけ授乳」に変更。自発的な喃語（脳の現在の分布からサンプリング）を実装|1日シミュレーション：泣き35回、食事13回、喃語240回、睡眠6回。全指標が人間の新生児の範囲に|✅|
|**B-4**|アデノシン状態変数を追加し眠さをメカニズムから算出（`adenosine.py`）。数式決め打ちを廃止|1日：泣き50回、食事13回、喃語381回、睡眠6回。全指標が人間範囲内|✅|
|**B-5**|海馬バッファ実装（`hippocampus.py`）。喃語経験を覚醒中に蓄積し睡眠移行時に大脳皮質へ定着（McClelland 1995 CLS理論）。逆伝播を330回/日→6回/日に削減|9ヶ月57分。まんま自発産出0件（「ん」が語彙にない）。r\_imit が6ヶ月でジャンプ（0.41→0.66）|✅|
|**B-6**|「ん」を両唇鼻音(1,1,1,5)として定義しstage1(6ヶ月)から解禁。肺活量バグ修正（`stamina=0`が偽値判定→50字喃語）。最大モーラ数15→8・成長速度調整で喃語長を人間に合わせた|9ヶ月19分（57分→大幅短縮）。まんまオウム返し97件。まんま自発産出0件|✅|
|**B-7**|停止ヘッド（`head\_stop`）を実装し、発話の長さを学習で制御しようとした|空喃語69%発生。原因：知覚時と生成時で隠れ状態が異なるため停止ヘッドが誤発火。人間模倣の根拠も弱いと判断し削除|❌ 削除|
|**B-8**|親の発話→授乳の順序を逆に（「まんま」を言ってから授乳）。空腹のまま「まんま」を聞けるよう修正|9ヶ月19.6分。まんまオウム返し73件。まんま自発産出0→**213件**に増加。hunger相関はまだなし（差-0.009）|✅|
|**B-9**|`generate()` に `body_state` を渡すよう修正。発声の各ステップでhunger等がGRUに届くようにした（B-8まではゼロが入力されていた）|9ヶ月21.0分。まんまオウム返し75件。まんま自発産出127件（B-8の213件より減少）。hunger相関 r=-0.0029（変化なし）|✅|
|**B-10**|(1) 汎用イベントログ（`events.jsonl`）を追加し、泣き・授乳・あやし・睡眠・喃語反応を記録。(2) `respond_to_babble()`を新設し、自発喃語にも40%の確率で親が随伴的に反応する仕組みを追加（Goldstein & Schwade 2008に基づく）。(3) シミュレーション期間を9ヶ月→12ヶ月に変更（初語の平均出現月齢はMacArthur-Bates CDIで生後12ヶ月、9ヶ月は根拠不足だった）。(4) `device: auto`→`cpu`に固定（実測でCPUがGPUより1.6倍速い。バッチ1・極小モデルのためGPUのカーネル起動オーバーヘッドが勝る）|12ヶ月42.8分。まんまオウム返し81件。まんま自発産出**42件**（B-9よりさらに減少）。hunger相関r=0.0022（変化なし）。target=まんまのbabble_response 10,970件中、類似度0.5以上は0件（平均0.276）|✅|
|**B-11**|複数エージェント議論（学術・技術・方法論・設計哲学の4視点）で判明した5点を修正：(1) `stomach.grow()`/`lungs.grow()`が`tick_body()`のforループ外にあり睡眠中の身体成長がほぼ止まっていたバグを修正。(2) `respond_to_babble()`を「内的状態から正解を決め打ち」から「太郎が実際に発した音を全候補語と比較し、最も近い語との類似度が閾値(0.4)を超えたら反応する」設計に修正。(3) デッドコード削除（`brain/brocas_area.py`重複、`cortex.py`の`should_cry()`と未使用の`cry_head`、`core_b.py`の未使用`prev_arousal`）。(4) `Habituation`（馴化）を`self_babble()`にも適用。(5) 状態依存クリティック（`Critic`クラス、body_state→V(s)を予測するMLP）を新設し、Dopamineのスカラー移動平均baselineを置き換え（Actor-Critic化）|185日統合テスト：泣き8,847回・食事2,342回・喃語49,636回・睡眠977回、エラーなく完走、指標もB-10相当の範囲。12ヶ月フル検証は次回|✅ 現行版|

### 複数エージェント議論による発見（2026-07-01）

学術・技術・方法論・設計哲学の4視点で議論した結果、以下が判明（B-11で対応済み）：

- **`respond_to_babble`のtarget_word選択が概念的に誤っていた**（2つの独立した技術レビューが同じ結論）。「太郎が実際に発した音」ではなく「内的状態から先に正解ラベルを決め打ち」して比較しており、Goldstein & Schwade (2008)が示した「言葉らしい発声にのみ随伴的に反応する」設計とズレていた
- **`stomach.grow()`/`lungs.grow()`が`tick_body()`のforループの外にあり、睡眠中の身体成長がほぼ止まっていた**（1日16-17時間の睡眠中、成長は1回分しか適用されない）。B-1〜B-10の全シミュレーションに影響
- デッドコード：`brain/brocas_area.py`（重複）、`cortex.py`の`should_cry()`・`cry_head`、`core_b.py`の未使用`prev_arousal`。なお`body/stamina.py`＋config.yamlの`stamina:`セクションは検証の結果、Model A系の`environment/core.py`が現在も参照しているため削除しなかった（エージェント報告の訂正）
- `Habituation`（馴化）が`step()`にしか適用されておらず`self_babble()`には適用されていない
- 単一試行問題：乱数シード1回の実行で213→127→42という変化を「効果」と解釈すること自体、統計的根拠が薄い（方法論エージェント指摘、未対応・要検討）
- 「本能のみ・決め打ち禁止」という設計哲学は継続すべきと判断（学術・設計哲学の両エージェントが一致）

### 残課題

* 単一試行問題：バージョン比較に複数シードでのアブレーションが必要（方法論エージェント指摘、未対応）

\---

## 目標B2：構造改革（根本前提の検証を受けて）

B-11完了後、94体規模の多角エージェント議論（技術・研究・学問・人間模倣・AI開発・検証の6観点）で
「太郎を人間と同じように育てれば人間のように発達するか」というモデルBの根本前提そのものを検証した。
結論は**条件付きNO**：実装の質とは無関係に、シミュレーションが表現する「育てられ方」が人間の乳児の
育てられ方の本質的要素（特に「発話が実際に欲求充足に影響する」という道具的随伴性）を欠いている、
という指摘だった。これはB-12という連番修正ではなく、構造そのものの見直しが必要と判断し、
**B2-N**という新しい命名系列を開始する。

|版|変更内容|結果|状態|
|-|-|-|-|
|**B2-1**|太郎の発話内容を初めて実際の授乳判断に結びつけた。`core_b.py`に`sounds_like(tokens, word)`を追加（太郎の発声が指定した語にどれだけ似ているかを判定）。`parent_sim_b.py`で「空腹(hunger>0.5)かつ、自発喃語が『まんま』に類似度0.4以上で似ている」場合、泣いた時と同じ経路（`_schedule_response`、親の在/不在・反応確率）で親に気づかせ、実際に授乳させる仕組みを追加。Skinner (1957)の言語行動理論における「mand（要求語）」——欠乏状態でのみ要求語がその結果と結びつく——を根拠とする。空腹条件を外さなかったのは、(1)mand理論の定義そのもの、(2)空腹に関係なく授乳されると学習すべき関連自体が壊れる、(3)測定不能になる、の3点による|185日統合テストで新経路が62回発火（全授乳2,410回中）を確認。12ヶ月フル検証：まんま自発産出213→127→42→12→**131件**（B-11から急増、方向転換）。hunger相関r=0.0090（過去最大の正値）。ただしword_requestは全365日中最初の10日にしか発火せず、6ヶ月（声道stage1解禁）以降も類似度平均0.31まで上がるのに閾値0.4を一度も超えなかった（追加調査で判明）|✅|
|**B2-2**|B2-1の追加調査で判明した「音素は使えるようになっても正しい並びを学習できていない」問題（REINFORCEのクレジット割り当て問題）に対応。`imitation.py`に`compute_alignment_credit()`を新設：模倣報酬で既に使っている重み付き編集距離のDPテーブルをバックトレースし、生成した文字ごとに目標語のどの位置とどれだけ一致していたかを求める。**⚠️逸脱**：文字ごとの比較自体は人間模倣ではない（音素意識は発達的に最も遅く獲得されるスキルで4〜6歳の読み書き学習と結びつく。生後1年の太郎が実際にやっていることではない）。ただし「発話全体を待たず継続的に修正信号を受け取る」という原則自体はDIVAモデル（Guenther）の運動学習理論に沿う。`basal_ganglia.py`の`learn_action()`がこの文字ごとの一致度でδを重み付けするよう変更（`credits`引数）。`step()`と`respond_to_babble()`の両方に組み込んだ。新しい決め打ちパラメータは追加せず、既存の音声距離計算を再利用しただけ|単体テストで動作確認（「まんみ」vs「まんま」→credit=[1.0,1.0,0.75]、文字ごとの一致度が正しく反映）。185日統合テストで完走。12ヶ月フル検証：まんま自発産出131→**71件**、hunger相関0.0090→0.0046、いずれも悪化。原因：週次分析でword_request/respond_to_babbleとも閾値0.4を6ヶ月以降も一度も超えていないと判明——学習方法を改善しても、報酬が発生する機会自体がほぼゼロで機能しなかった（鶏と卵の状態）|✅|
|**B2-3**|B2-2の分析結果を受け、ユーザー方針「閾値はできるだけ使いたくない」のもと、`respond_to_babble()`の類似度足切り（閾値0.4未満は無反応）を撤廃。`sounds_like()`（真偽値）を`word_similarity()`（0〜1の連続値）に変更し、`parent_sim_b.py`の`word_request`トリガーも「類似度が閾値を超えるか」ではなく「類似度そのものを確率として使う」方式に変更（近いほど気づかれやすいが、段階のないオールオアナッシングではない）。決め打ちの閾値パラメータを完全に削除した|単体テストで動作確認（word_similarity=0.300のような連続値、respond_to_babbleは閾値なしで必ず反応）。185日統合テストで`request`発火数が17件→**2,210件**に急増（機会がほぼゼロだった問題が解消）。12ヶ月フル検証：まんまを含む喃語0件だが、これは喃語長が肺活量成長で早期（生後4ヶ月頃）に8文字固定になり3文字の部分文字列が出にくくなったため。連続類似度で見てもhunger>0.5時0.1745 vs hunger<0.3時0.1697とほぼ差なし——機会は確保できたが喃語の中身自体はまだhungerに反応していない|✅|
|**B2-4**|2点対応。(1) ユーザー指摘「喃語が8文字固定は人間じゃありえない」を受け、B-7で学習可能な停止head撤去以来なくなっていた発話停止機構を、息切れという身体的制約による確率的停止として`cortex.py`の`generate()`に追加（喃語期のみ。残り呼気が少ないほど続ける確率が下がる。決め打ちの閾値ではなく物理的制約として実装）。(2) ユーザー指摘「実行時間短縮は急務」を受け、`logger.py`のログ書き込みを毎回open/closeする方式から、ファイルハンドルを開いたままにする方式に変更（1年で数十万回のログ書き込みが発生するため）|単体テストで喃語長のばらつきを確認（変更前は8文字固定、変更後は1〜5文字等に変化）。185日統合テストが8.5分で完走（B2-3の同条件推定20分程度から半減以下に短縮）。12ヶ月フル検証は次回|✅|
|**B2-5**|ユーザーから「息切れは本当に人間の停止理由か」との質問を受け文献調査。喃語の停止は呼吸容量（絶対的な上限）だけでなく、新奇性追求（まだ習得していない・面白い音を出し続けたい動機、Oudeyer & Kaplan系の内発的動機付け）と養育者の反応が主な決定要因と判明。B2-4の「息切れのみ」の停止確率を修正し、GRU出力分布のエントロピー（まだどの音を選ぶか定まっていない度合い＝興味度）で息切れの影響を弱める形に変更：`停止確率 = 息切れ圧力 × (1 - 興味度)`。新しい決め打ちパラメータは追加せず、既存のサンプリング分布を再利用しただけ|単体テスト・185日統合テストで動作確認、エラーなし。12ヶ月フル検証：喃語長平均3.12文字（自然な分布）、実行時間15.1分。診断実験（hunger強制固定・1000サンプル比較）でhunger=0.0時0.2830 vs hunger=1.0時0.2950、t≈2.03と、弱いが統計的に検出可能な正の効果を確認——「学習されていないのではなく、信号が弱すぎて自然変動のノイズに埋もれていた」と判明|✅|
|**B2-6**|診断実験の結果を受け、信号を弱めている技術的原因を調査。`cortex.py`の`generate()`で、方策勾配の学習対象である`log_prob`が**NEノイズ適用前**の意図した値から計算されていたのに対し、実際に発声され報酬・信用割り当ての対象になるのは**NEノイズ適用後**の値だったという不整合を発見（コードを直接確認して検証済み）。太郎が「ま」を選んでもノイズで「み」に変わった場合、その結果への評価が誤って「ま」のlog_probに結びついていた。`log_prob`をノイズ適用後の実際の値から`_log_prob_of()`で計算し直すよう修正。既存の関数を再利用するだけで新しい決め打ちパラメータは追加していない|単体テスト・185日統合テストで動作確認、エラーなし。12ヶ月フル検証：まんまを含む喃語633件（0.617%）、hunger相関r=+0.0338。診断実験（hunger強制固定）は差+0.1044・t≈19.1に拡大（B2-5比で約8.7倍）。**複数試行検証**（`run_12months.py`をtrial ID対応に改修し2試行を並列実行）：trial1はまんま含む喃語1045件(1.013%)・r=+0.0601、trial2は608件(0.593%)・r=+0.0726。3試行とも相関の符号が一貫して正で、94エージェント議論が指摘した「単一試行問題」への最低限の反証となった|✅|
|**B2-7**|【打ち手1＝反復バイアス】喃語が「ま」1文字ずつ独立に選ばれ、「まんま」型の反復が偶然しか出ない問題に対応。`cortex.py`の`generate()`（喃語期のみ）で、直前に発声した口・のどの形（調音位置・様式・声帯・母音の各パラメータ）へ戻りやすくするバイアスを各logitに加算。Frame/Content理論（MacNeilage & Davis 1998）：喃語は顎の開閉振動が生む音節で、舌・唇は最初ほとんど動かないため同じ音節が反復される。声道の物理定数（調音の慣性）の一種で行動の閾値ではない。log_probは生logitから計算するためB2-6の不変条件を維持（バイアスは選択のみに作用）。**結果：目標には非寄与（むしろ微減）**。反復スコアは0-4ヶ月・6-8ヶ月で+0.04と実装は正常だが、まんま含む喃語527/357件（B2-6の633/1045/608件を下回る）、hunger相関0.062/0.039（改善なし）。月齢別診断で判明：生後6-8ヶ月で喃語の66%が既に「ま」始まり——「ま」の産出はそもそも壁ではなく、壁は「空腹という文脈に結びつける（無条件→条件つき）」ことだった。人間の初語も規準喃語を大量に発した後に文脈依存で立ち上がる点と一致。反復自体は人間模倣として正しく害もないため残す|✅（目標には非寄与と記録）|
|**B2-8**|【打ち手2＝探索の結晶化。着想は人間模倣・実装は⚠️逸脱】B2-7の診断が示した本当の壁「無条件に喃語を出す段階から、条件つきで出す段階へ移れない」に対応。既に太郎に載っている経験ベースのNE調整（青斑核：報酬あり→探索を減らす。Aston-Jones & Cohen適応ゲイン理論）に加え、**成熟ベースの探索上限低下**を追加。`locus_coeruleus.py`に`mature(progress)`と成熟天井`mature_max_ne`を新設し、発達の進行（月齢）に応じてNE上限を1.0→0.3へ低下させる。`core_b.py`の`tick_body()`で声道成熟と同じ月齢基準（stage3_time=12ヶ月）で駆動。人間の根拠：探索→活用の発達的移行（Gopnik）、シナプス刈り込み・臨界期の閉じによる変動減少、鳥のLMANの運動系への影響低下（結晶化）。ただし複数の成熟機構を太郎の1つのゆらぎ調整本能に集約した近似で、NE単独が人間の結晶化を駆動すると主張するものではない。単体テストで月齢進行とともに天井が1.0→0.65→0.30へ低下を確認（報酬ゼロでも頭打ち）。**12ヶ月フル検証（2試行）：まんま含む喃語4877件(4.76%)/1882件(1.81%)とB2-6の約5〜10倍に激増。頻出喃語もま・まん・まんま・まあが上位を占め、まんま一族へ収束（平均長3.20・種類数15369で崩壊なし）＝「音形安定」を達成。要求語も8300→9200-9700に増加。ただしhunger相関は0.039/0.082でB2-6と同水準——無条件に結晶化しており「文脈一貫（空腹時のみ）」は未達。事前予告どおり、結晶化は既にある弱い手がかりを固めるだけで、空腹→まんまの随伴性自体が弱いため無条件に固まった。次は随伴性の強化へ**|✅|
|**B2-9**|【随伴性の強化＝mand配線。着想は人間模倣・実装は⚠️逸脱】B2-8で判明した「無条件に結晶化する」問題に対応。原因：喃語への報酬（`respond_to_babble`のr_imit）が**空腹と無関係**にまんま様発声を報いていたため、太郎は空腹でも満腹でも同じくらいまんまと言うよう学んでいた。一方、空腹→食事→ホッとする報酬（r_home）は**まんまの発声に信用が割り当てられていなかった**。この2つのすれ違いを配線。`respond_to_babble`に経路2（要求語mand, Skinner 1957）を追加：空腹時にまんま様発声で食事が得られたら、その解消（ホッとする）を報酬`r_mand = w_home × hunger`として加算し、アライメント信用でまんまの文字へ割り当てる。hungerに連続比例させることで満腹時は自然にゼロ＝閾値なしで「空腹時のみ報われる」ギャップを作る（満腹でまんまと言っても食べ物は解消をもたらさない＝mandの本質）。二重backward回避のため社会的反応とmandの報酬を合算し学習更新は1回に統一（`parent_sim_b.py`のword_request経路を再構成）。単体テスト：mand=Falseでr_mand=0、hunger=0.6→0.180、0.9→0.270と連続比例、二重backwardエラーなし。空腹時のまんま発声の報酬が満腹時の約2倍。**12ヶ月フル検証（2試行）：hunger相関0.104/0.073と過去最高（B2-6平均0.056・B2-8平均0.061を上回る）。空腹>0.5時と<0.3時の類似度差(gap)も+0.049/+0.037とB2-6(+0.023)の約2倍に拡大。まんま産出量(3.35%/4.64%)も高水準を維持＝音形安定を失わずに文脈一貫が改善。mand配線は方向的に成功。ただしgapは絶対値では+0.04程度で満腹時もそれなりにまんまを言っており、「空腹時のみ」の完全な条件づけには未到達（人間の初語も条件の絞り込みは数ヶ月〜数年かかる点と整合）。初語4条件は自発的✅・音形安定✅・伝達的✅・文脈一貫🔶（過去最高だがまだ弱い）。2年シミュレーション検証（2試行）：報酬設計を変えず期間だけ延ばすと文脈一貫が12〜24ヶ月でhunger相関r=0.5〜0.75・gap+0.28〜0.39まで自然に成熟（満腹時類似が0.37→0.18〜0.26へ低下＝満腹時に言わなくなった）。人間の「一貫性は2年目以降に育つ」順序と一致し、行動レベルの4条件は出そろった。ただし産出側のみの結果で理解（聞いて分かるか）は未検証＝到達点は「強いconditioned mandの成熟」に限定**|✅|
|**B2-10**|【理解の配線＝満腹予期ヘッド。着想は人間模倣・実装は⚠️逸脱】理解テストA案で「聞いた語→体の未来（ごはん）の予期を表す場所が無い」＝conditioned mand止まりと判明したのを受け、その欠けた向きを配線。`cortex.py`にGRU隠れ状態（聞いた音＋体の状態の統合表現）から「もうすぐ満腹になるか」を予測する`satiety_head`を新設。人間のOFCの結果同定（正体の予期）＋島皮質の内受容予測、Schultzの手がかり→報酬予測に相当。criticの「価値(良さ)」とは別に「特定の結果(ごはん)」を予期する。学習は`step()`で「発話の後に授乳が来たか(feed=1/comfort=0)」を教師にBCE、`parent_sim_b`がまんま=授乳時・よしよし=慰め時に聞かせるので「まんま→ごはん」を学ぶ。⚠️逸脱：教師が結果随伴でなく環境が与える2値ラベル。**理解テスト結果：12ヶ月は満腹時まんま予期0.00（あうあと同=理解なし）だが、24ヶ月は満腹時まんま=0.38≫あうあ0.00・ままん0.08＝「まんまを聞くとごはんが来る」を特異的に予期＝理解が発達的に出現（産出側の文脈一貫と同じく2年目に成熟）。空腹時は3語とも~1.0（空腹なら語に関係なく授乳が来るため語の寄与は満腹時に現れる）。A案の理解ゼロから、産出（mand）＋理解（satiety予期）の双方向リンクへ前進。ただし部分的(0.38)・遅い(24ヶ月)・満腹時のみ・1シードで要追試**|✅|
|**B2-11**|【理解の学習頻度を底上げ＝睡眠リプレイに親との会話を追加】理解が産出よりずっと遅い原因を特定：自発喃語は年10万回練習できるが親との会話（理解の学習機会）は年1万回程度で、頻度差が理解の遅れの主因（人間は逆で、聞くだけの経験は運動発達を待たず無制限に積めるため理解が先に育つ）。既存の睡眠リプレイ機構（海馬→皮質、McClelland 1995）が親との会話を一度も記録していなかった配線漏れを修正。`hippocampus.record_episode`にsatiety_targetを追加、`step()`で親との会話も記録、`consolidate()`で満腹予期も反芻・学習。新報酬・閾値なし、海馬容量がそのまま反芻回数を決める。この漏れの再発防止にB_プラン§0へ「接続漏れチェックリスト」を追記。**理解テスト結果（明暗）：12ヶ月は満腹時まんま−あうあ=+0.60（B2-10は+0.00）＝理解が早く強く出現＝反芻仮説を支持。しかし24ヶ月は満腹時3語とも0＝hungerだけで決めて語を無視＝崩壊。原因はまんま=空腹時・よしよし=満腹時にしか聞かない＝語とhungerの完全相関で、満腹予期をhungerだけで当てられる近道があるため。リプレイで学習量が増え近道に収束した。n=1で12/24は別シードのためブレの可能性大。次は交絡を崩す＋複数シード追試**|✅|
|**B2-12**|【理解の指標を訂正＋交絡崩し（空腹較正＋時間割授乳）】ユーザーの指摘で理解テストの指標が的外れと判明：人間は空腹を体内センサー（内受容感覚）で直接知るので「まんまを聞いて今の満腹度を当てる」は順序が逆＝hungerで解ける近道。裏取り（参考文献§9, Bergelson&Swingley 2012他）で正しい指標は「語→対象（食べ物）の先取り」＝hunger一定で語だけ変えた差（語の寄与）と確認し、probe/コメントを訂正（satiety_headの中身は不変、意味づけのみ）。さらにユーザー指摘「hunger頼りは人間的でズルでない／hunger=1は餓死でない／喃語返事はオウム返しでなく知覚入力も無い」で軌道修正。**交絡崩し**：太郎は"耳で聞く"語がまんま(空腹)・よしよし(満腹)の2つで完全相関していた。①空腹速度を人間較正（実測27回/日→calibrate_hunger.pyでconsumption_rate 0.0001→0.00003＝約7.5回/日≈3.2時間おき。実効値はconfig.yamlにあり修正）。②時間割授乳を再導入（config/schedule.yamlにmeals=7,10,12,15,18,21時、parent_sim_bで食事時刻に空腹に関わらずまんま＋授乳satiety_target=1.0、オンデマンドは残す。step()経由で知覚入力として聞く）。**動作確認（verify_meals.py,10日）：総食事7.2回/日（人間域）、時間割授乳の82%が満腹寄り(hunger<0.5)＝まんまを空腹以外でも聞くようになり混線を崩した。**次は本番シミュ＋再定義指標で語の寄与が正・安定になるか複数シード検証。残る欠け＝親の返事を"聞く"経路|✅ 現行版|

\---

## 命名規則

```
B-2
│ └─ 修正番号（1から連番）
└──── 目標（A=オウム返し, B=初語, ...）
```

```
B2-1
│  └─ 構造改革の連番（1から）
└──── 目標Bの構造改革フェーズ（根本前提の見直し以降）
```

* 各修正は**gitコミット**と**この表への追記**で記録する
* A（オウム返し）の版管理は `A/docs/版管理.md` を参照

\---

*最終更新：2026-07-02（B2-12 理解の指標訂正＋空腹較正・時間割授乳で混線を崩す）*

