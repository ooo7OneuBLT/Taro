"""太郎の中身を見るための常駐デスクトップアプリ群（PySide6）。

【なぜ要るか、2026-08-15】ユーザーの要望：「ブラウザは絶対に嫌だ、ちゃんと
アプリケーションにして」。既存の `run/tools/wiring.py`（静的HTML生成）は削除せず、
その中身の定義（`run/wiring_map.py`）を土台にして、常駐アプリ側から読む。

このパッケージの下に、目的ごとのアプリを1つずつ増やしていく想定
（まず `wiring_viewer`＝配線・設定の一覧「TaroMap」）。
"""
