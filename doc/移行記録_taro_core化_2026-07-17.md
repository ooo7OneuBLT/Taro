# 移行記録：太郎の脳・paths・2体環境を `taro_core/` へ切り出し（2026-07-17）

**目的**：太郎の脳（`C/src`）・場所案内板（`C/paths.py`）・2体環境（`D/scripts/d_env.py`等）が
目標フォルダ `C/`・`D/` の中に埋まっていて、D が隣の C に `sys.path` で手を伸ばして借りている
歪んだ依存を解消する。太郎の脳は「目標Cのもの」でなく「太郎そのもの」なので、中立な
`taro_core/` に置き、C も D も対等に import する形にする。

**背景の判断**：
- 脳を外に出す＝根本を直す（ユーザー承認）。
- 2体環境も外に出す＝最終ゴールが介護AI（相手がいる世界）で、C以降はほぼ全部他者を扱う
  ＝2体環境は共有インフラだから（ユーザー承認）。
- MIMo（体）は既に `Taro/MIMo` に独立済みで、今回は触らない。
- A/B は独自の脳コピーを持ち C 非依存なので影響なし（今回は触らない）。

**安全策**：作業前の作業ツリーはクリーン（全コミット済）。各ステップを「動く状態」で
こまめにコミットするので、`git log` とこの記録を突き合わせれば巻き戻し先が分かる。
壊れたら `git reset --hard <その手前のコミット>` で戻せる。

---

## 依存構造の事実（移行が安全な根拠）

- `C/src` は**パッケージでなくフラット構成**（`__init__.py` 無し）。モジュールは**素の名前**で
  import し合う（例：`from hippocampus import ...`）。"C" という文字は import に埋め込まれていない。
  → ディレクトリごと動かせば 22 モジュール本体は**無修正**で動く。
- パスを通しているのは2箇所だけ：
  1. `paths.setup_brain_path()` が `src/{wrapper,senses,brain}` と `tests` を sys.path に載せる。
  2. `src/wrapper/hybrid_env.py` が自分の隣の `../body` を `__file__` 基準で載せる（相対なので移動に強い）。
- 外部（D等）が使う paths の窓口は **`SCENE` / `MIMO_DIR` / `setup_brain_path()` の3つだけ**
  （`paths.SRC/C_ROOT/LOGS/TESTS` を外部から使う箇所は grep でゼロ）。
- テスト（`C/tests/*.py`）は `__file__` 基準で自分の `../src/wrapper` を載せる自己完結型
  → コピー先でも無修正で動く。

---

## 手順と進捗

| # | 手順 | 状態 | コミット |
|---|---|---|---|
| 0 | ベースライン確認：`C/tests/test_hybrid_env.py` = 全PASS | ✅ | (作業前) |
| 1 | `taro_core/` 新設、`C/src`＋`paths.py`＋`tests` を**コピー**、`paths.py`アンカー付け替え | ✅ | (step1) |
| 2 | D側37本の bootstrap を `"C"`→`"taro_core"` に置換 | ✅ | (step2) |
| 3 | C側スクリプト/tests を taro_core 参照へ | ✅ | (step3) |
| 4 | 旧 `C/src`・`paths.py`・`tests` を削除（重複解消） | ✅ | (step4) |
| 5 | ~~現行2体envを `taro_core/env/` へ~~ → **方針変更：本番env設計として `world/`(兄弟)へ** | ⏸ 保留 | - |
| 6 | docs（README構成図・`d1_touch`コメント・移行記録）＋メモリ更新 | ✅ | (step6) |

## 変更したファイルの記録（随時追記）

**ステップ1（コピー・非破壊）**
- `C/src/**` → `taro_core/src/**`（body/brain/senses/wrapper 全22モジュール、無修正コピー）
- `C/tests/*.py` → `taro_core/tests/*.py`（自己完結型なので無修正）
- `C/paths.py` → `taro_core/paths.py`：アンカーを `C_ROOT`→`CORE_ROOT` に改名、コメント更新。
  `MIMO_DIR`/`SCENE` の解決ロジックは不変（taro_core も Taro 直下のため os.pardir で同じ位置に届く）。
  `LOGS` は後方互換で残置（未使用）。
- 検証：taro_core から脳 import 成功＋`test_hybrid_env.py` 全PASS。**C側は無変更で従来どおり**。
- この時点は「新旧両方が動く」状態。旧 C/src はまだ生きている（削除はstep4）。

**ステップ2（D側の付替え）**
- D/scripts/*.py 37本の **paths用挿入** `os.pardir, "C"))` → `os.pardir, "taro_core"))`（各1件、計37）。
  → D は paths/脳を **taro_core** から取るようになった（Cの src/paths には触れない）。
- **据置き**：8本にある `os.pardir, "C", "scripts"))` は変更せず残した。これは
  `C/scripts/run_c_metrics_ac_lr.py` の **MinimalFusion**（Cの感覚融合）を借りるため。
  C/scripts は今回移動しないので動き続ける。
  - ⚠️ **残課題**：MinimalFusion がまだ C/scripts の実験スクリプトに埋まっている。本番で
    感覚融合を使う段で `taro_core/` へ抽出するのが望ましい（今回はYAGNIで見送り）。
  - 該当8本：d_double_touch, d_speed_profile, d_supine_split, d_supine_touch_truth,
    d_touch_analog, d_touch_classify, d_touch_predictability, d_transfer_check（主に旧・触覚系）。
- 検証：`d_env.py` 自己実証がtaro_coreで全項目正常（アルファ621次元＝C同一形式・ベータ別駆動）。
  MinimalFusion の混在依存（taro_core paths＋C/scripts fusion）も import スモークで解決確認。

**ステップ3（C側スクリプトの付替え）**
- `C/scripts/` の脳/paths参照を taro_core へ（C/src・C/paths を step4 で消しても動くように）：
  - `run_c_metrics_ac_lr.py`：src/tests参照を `_CORE`(=taro_core)へ。**`_BRIDGE`(=C)は
    logs出力用に復活**（`C/logs/...`はC側に残す）。※最初 `_BRIDGE` を消してしまい
    line50のLOG_DIRで NameError → スモークテストが検出 → 復活させて解決（依存の見落とし例）。
  - `fair_rollout.py`：src/tests参照を `_CORE`(=taro_core)へ（`_BRIDGE`は他用途なしで削除）。
  - `verify_c2_brain.py`：`import paths` の挿入先を `Taro/C`→`Taro/taro_core` へ。
  - `mem_profile.py`：同上（`_HERE/..`→`_HERE/../../taro_core`）。
- 検証：4本 syntax OK。`run_c_metrics`(MinimalFusion)＋`fair_rollout` import 成功・LOG_DIR設定OK。
  `verify_c2_brain`/`mem_profile` の paths ブートストラップが taro_core を指すことを複製テストで確定。
  C側テスト原本も緑（回帰なし）。
  - ※`verify_c2_brain.py` のフル実行（C2脳の指標計算）は元々重く150sで未完走。ブートストラップ
    の解決は確認済みだが、フル完走の確認は今回はしていない（移行の目的外）。

**ステップ4（旧C/srcの削除＝破壊的）**
- `git rm -r C/src C/tests C/paths.py`（gitが安全網。全コミット済なので `git reset --hard 866a944`
  で直前に戻せる）。C直下に残るのは docs/logs/models/scripts/requirements のみ。
- 削除前の最終スイープ：旧C/src・C/testsを指す残存参照は「削除対象のC/tests原本」と
  `d1_touch.py`のコメント文中prose（実コードはstep2でtaro_core化済）のみ。A/BはC非依存を確認。
- 検証（C/src無しの状態で）：taro_coreのCテスト緑／`d_env.py`自己実証OK／MinimalFusion経路も
  taro_core経由で解決。＝**脳・paths・testsの移設は完了**。

**ステップ5（2体envの移設）＝方針変更で保留**
- 当初 `taro_core/env/` へ移す予定だったが、調査で2点判明：
  1. envは「1つの綺麗な土台」ではなく**並行する4〜5系統の試作**（TwoAgentMIMo/TwoMimoEnv/D1Env/
     SupineMimoEnv(LeanMimoEnv基底)/CarerEnv→CarerVisionEnv）。同じ「2体attach」が各所で重複実装。
     D/README通り「探索中（確定コードなし）」。丸ごと移すと試作の混乱を共有中核に持ち込む。
  2. **環境は太郎の一部ではない**（太郎=脳+体+感覚のエージェント／環境=住む世界）。よって
     `taro_core`の下でなく、**兄弟の `world/`** に置くのが正しい（体=MIMo・脳=taro_core・世界=world
     の3分割。プロジェクト哲学「学習の工夫は太郎の中でなく環境側」とも整合）。
- **結論**：env移設は「本番環境の設計」の一部として行う。①バラバラの2体実装を単一の基底env
  （例：`world/two_body_env.py`）に一本化 → ②D側はそれを継承する実験版（supine/carer/vision/ego）
  として残す（＝「環境自体はworld・編集はDで」がユーザーの意図通り成立）→ ③旧5系統を置換・削除。
  これは設計メモ→承認→実装の順で、脳移設とは別タスクとして進める。

**ステップ6（docs/メモリ）**
- `README.md` 構成図に MIMo/taro_core/C/D/（予定）world を追記＋「体・脳・世界の3分割」注記。
- `D/scripts/d1_touch.py` の古いコメント（C/src・C/paths表記）を taro_core へ修正。
- 本移行記録を仕上げ。メモリ [[project-taro]] に taro_core 化を記録。
- `コード構成.md` は目標B専用の説明で移動パスを参照しないため更新不要（B中心のまま整合）。

## 現状サマリ（この移行の到達点）
- ✅ **完了**：太郎の脳・感覚・内臓・paths・tests を `C/` から中立な `taro_core/` へ切り出し。
  C・D とも taro_core を対等に import。A/B は元から独立で無影響。全ステップ working state でコミット。
- ⏸ **保留（本番設計へ）**：2体環境を単一基底に一本化して兄弟 `world/` へ。D は継承した実験版を持つ。
- ✅ **解消済み(2026-07-17)**：MinimalFusion を `taro_core/src/senses/fusion.py` へ抽出完了
  （本番視覚ONの実装ステップ0として実施。視覚オプション付き。run_c_metricsは再エクスポートで後方互換）。
</content>
</invoke>
