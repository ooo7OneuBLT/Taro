# 太郎の構造

**自動生成です。表を手で書き足さないでください**（`python run/tools/index_core.py` で作り直す）。
手で書いてよいのは下の印で囲まれた節だけです。

**97 ファイル・21110 行・公開関数 338 個**（`taro_core/src` のみ。`run/` は未収録）

- 呼称の決まり … `doc/脳の地図.md` §0（**これ以外の呼び方をしない**）
- 入力と出力 … `doc/部位の入出力一覧.md`（Grepで必要な節だけ引く）
- 「**なぜ**そうしたか」は**ここには書きません**。コードのコメントにあります（例：`taro_core/src/body/infant_body.py` の 640〜653行目に、`NECK_TONE_STIFFNESS = 0.40` の文献と Tier が書いてあります）
- 測定器・ビューアなどの道具は `doc/道具索引.md`

<!-- 手書きここから（自動生成はこの間を書き換えません） -->
## 実際に通る筋道

**手書き（2026-09-15）。コードを読んで確かめた事実だけ。ここは腐るので日付を見ること。**

```
実験ファイル（F/experiments/*.json）
   ↓
run/main.py   load_spec → build_plugins → _write_run_meta（走行の記録を必ず残す）
   ↓  run.type で4つに分かれる
   ├ train    run/config.py Config.from_spec → run/trainer.py Trainer
   ├ measure  run/plugins/common/scene.py build() で環境だけ作り、
   │          **脳を通さず**ゼロ入力で進める（道具＝プラグインが測るだけ）
   ├ view     run/viewer.py view()（等倍速の目視）
   └ edit     run/viewer_tools/e_viewer.py を子プロセスで開く（環境変数で渡す）
```

学習（train）の1歩：

```
run/trainer.py Trainer
   ├ self.taro          run/taro_setup.py Taro（脳・学習器・神経調節・小脳を組む）
   ├ self.taro.tick     taro_core/src/brain/brain_tick.py（脳の1歩ぶん）
   └ 1回の判断で K 物理ステップ進める（同じ命令を K tick 保持する）
      各 tick で伸張反射と共通駆動を計算し直し、親の発話を聞く
```

語を言うまで（`produce.word_choice: "gru_hippo"`。呼称は `doc/脳の地図.md` §0）：

```
本体GRU：切れ目を見つける ┐
                           ├ 塊GRU vs 言語海馬 → 自信の高い方（0.5未満は黙る）
                           ┘                     → 本体GRU：口を動かす
```

**注意：`run/loop.py` はもう呼ばれていない。**自分の説明文に「役目は終わった（2026-07-30）」
と書いてある。新しい経路が古い経路（`E/scripts/e_growth_train.py`）と同じ結果を出すかを
比べるためだけに残してある。新しい実験では使わない。
<!-- 手書きここまで -->

---

## 部位ごとの索引

### 感覚（目・耳・触覚） （13本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `fusion.py` | 感覚融合（fusion）— 各感覚エンコーダの出力を1本のベクトルに統合する、太郎の"本物の融合層"。 | 107 |
| `hearing.py` | 耳（Hearing） — 親の発話文字列を、脳が扱えるトークン列に変える薄い入口。 | 133 |
| `insula.py` | 島皮質（Insula） — 体の感覚を大脳皮質に伝える経路 | 44 |
| `object_detector.py` | 物の切り出し ── 「ここに何かがある」だけを返す。種類は出さない。 | 565 |
| `otolith_organs.py` | 耳石器（前庭迷路の一部）— 卵形嚢・球形嚢。直線加速度と、重力に対する頭の傾きを感知する。 | 145 |
| `proprioceptive_map.py` | 固有感覚（腕の関節角度）の中から、狙った関節だけを取り出す索引表を作る。 | 133 |
| `retina.py` | 網膜 — 「自分が動いたせいで流れる背景」と「対象そのものの動き」を分ける。 | 92 |
| `semicircular_canals.py` | 三半規管（前庭迷路の一部）— 頭の回転（角速度）を感知する。 | 125 |
| `sensory_encoders.py` | 感覚の通訳層（Phase 5・接続層） — MIMoの各感覚を、太郎の脳が読める形に変換する。 | 71 |
| `somatosensory_cortex.py` | 体性感覚系(触覚)の脳内経路：視床VPL核（部位別集約） → 一次体性感覚野S1（部位間統合）。 | 470 |
| `touch_adaptation.py` | 触覚の順応（同じ場所を押され続けると感じ方が弱まる、人間の受容器が持つ性質）。 | 183 |
| `vision_backends.py` | 視覚バックエンドの差し替え機構（F1-3b）— 語彙学習(Lexicon)が使う「見ていたもの」の | 304 |
| `vision_encoder.py` | 視覚の通訳層（Phase 5・接続層） — MIMoの画像を、太郎の脳が読める形に変換する。 | 70 |

### 大脳皮質 （7本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `chunk_vocab.py` | 塊の名簿（ChunkVocab） — 塊（モーラID列）に番号を振る器。 | 91 |
| `message_layer.py` | 言いたいことの層（MessageLayer） — 物の塊と述語の塊を別々に選んで並べる。 | 315 |
| `recurrent_core.py` | 大脳皮質（Cortex） — 予測する単一の再帰ネットワーク | 603 |
| `recurrent_core_motor.py` | 本物のTaroBrain（taro_brain.py）に、運動性喃語（身体の感覚運動予測ループ）専用の | 465 |
| `sensorimotor_brain.py` | 感覚運動の脳（試作） — Phase 6・運動出力＋感覚運動予測ループ | 86 |
| `visual_projection.py` | 視覚投射（VisualProjection） — 視覚野から連合野への「軸索の束」。 | 31 |
| `world_predictor.py` | 連合野の世界予測器（M7a・測るだけ／M7b-1・物ごとに動かす）。 | 1448 |

### 大脳皮質 / 頭頂葉 （4本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `intraparietal_sulcus.py` | 物体ファイル ── 「これ」を物として保つ最小の仕組み。 | 714 |
| `priority_map.py` | 段6 注意の優先度地図 ── 「下からの目立ち」と「上からの目的」を1枚の地図で競わせる。 | 189 |
| `spatial_priority_map.py` | 場所の優先度地図 ── 「次にどこへ注意を向けるか」を、視野の升目ごとの値で持つ。 | 176 |
| `visual_attention.py` | 視覚と注意の手順そのもの（脳の部品）。 | 990 |

### 大脳皮質 / 側頭葉 （1本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `segmentation.py` | 分節 — 聞いた音の列を「語の塊」に切り分ける（側頭葉） | 226 |

### 大脳皮質 / 前頭葉 （2本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `corticospinal.py` | 皮質脊髄路（corticospinal tract）相当の薄い配線層。 | 19 |
| `motor_cortex.py` | 運動野（motor cortex）相当のモジュール。 | 28 |

### 左前頭葉（ブローカ野） （1本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `brocas_area.py` | ブローカ野（Broca's Area） — 左前頭葉にある発話計画の中枢 | 105 |

### 言語海馬 （1本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `language_hippocampus.py` | 言語海馬（LanguageHippocampus） — 聞いた発話の鎖状連想メモリ＋睡眠リプレイ。 | 199 |

### 中脳 （6本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `efference_copy.py` | 段0 座標の安定化（遠心性コピー）── 目を動かす命令の写しを、感覚側へ先回りで送る。 | 121 |
| `orienting.py` | 視線誘導反射（新版）＝設計図（`E/docs/視線誘導反射_設計図.md`）に基づく実装。 | 1870 |
| `orienting_v1.py` | 視線誘導反射（Orienting Reflex）：周辺視野の動きへ、首・目を反射的に向ける。 | 149 |
| `preattentive_map.py` | 段1 前注意の地図 ── 目を向ける前に「どこで何が起きているか」を並列に作る。 | 159 |
| `salience_map.py` | 目立ちの地図 ── 視野ぜんたいの各場所に「どれくらい注意を向けるべきか」の値を1つ持つ。 | 242 |
| `superior_colliculus.py` | 上丘（superior colliculus）— 視野の地図と、そこからサッケードを決める仕組み。 | 358 |

### 脳幹 （4本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `atnr.py` | 非対称性緊張性頸反射（ATNR, asymmetric tonic neck reflex）＝脳幹反射弓。 | 88 |
| `locus_coeruleus.py` | 青斑核（Locus Coeruleus） — 脳全体の探索レベルを調整する中枢 | 195 |
| `vergence.py` | 輻輳反射：左右の周辺カメラ画像のズレ（両眼視差）から、目をどれだけ寄せるかを決める。 | 422 |
| `vor.py` | VOR（前庭動眼反射）：眼球を方策から切り離し、頭の動きを打ち消して視線を安定させる。 | 289 |

### 小脳 （2本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `motor.py` | 運動小脳（連続版, MotorCerebellum） — 運動の自動化（結晶化） | 61 |
| `speech.py` | 発話小脳（Cerebellum） — 口の動きと音の対応を体験から学ぶ帳面（順モデル＋逆モデル） | 121 |

### 脊髄 （6本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `common_drive.py` | 脊髄・脳幹相当の上位層：周期・振幅が揺らぐ振動子による共通駆動。 | 152 |
| `cpg.py` | 脊髄・中枢パターン発生器（CPG）相当のモジュール。 | 373 |
| `grasp_reflex.py` | 把握反射（palmar grasp reflex）＝脊髄反射弓。 | 79 |
| `postural_gate.py` | 姿勢制御反射（層1）＝座位で体幹が前後に傾いたとき、傾きと逆側の筋だけを通す | 345 |
| `righting_damper.py` | 立ち直り反射・層2＝角速度ダンパー（VCR風の連続的バイアス）。 | 215 |
| `stretch_reflex.py` | 伸張反射（脊髄反射弓）。新しい駆動モジュール（伸張反射＋揺らぐ振動子の共通駆動）の下位層。 | 114 |

### 皮質下核 （2本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `basal_ganglia.py` | 大脳基底核（Basal Ganglia） — 行動選択と強化学習 | 137 |
| `speech_gate.py` | 発話の門（SpeechGate） — 「言うかどうか」を反応の随伴で学ぶ大脳基底核の門。 | 121 |

### 大脳辺縁系 （1本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `hippocampus.py` | 海馬（Hippocampus） — 運動性自己モデル用の短期経験バッファ＆睡眠リプレイ | 58 |

### 欲求 （3本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `double_touch.py` | ダブルタッチ（頭への自己接触）検出器 ── 報酬に繋ぐための本能版。 | 338 |
| `imitation_reward.py` | 模倣衝動 — 親の発話に似るほど心地よい | 136 |
| `learning_progress.py` | 内発的動機 — 太郎が「何を嬉しいと感じるか」。 | 280 |

### 神経修飾 （1本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `dopamine.py` | ドーパミン（報酬予測誤差） — 学習を駆動するごほうび信号 | 28 |

### 目標喃語 （2本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `reach_goal_head.py` | 目標専用の順モデル（ReachGoalHead）— 手先位置の目標表現（案C）用。 | 47 |
| `trajectory.py` | 区分線形の軌道（GoalTrajectory）— Goal Babbling 段階1。 | 63 |

### 成熟 （2本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `developmental_clock.py` | 発達の時計（DevelopmentalClock） — ③発達年齢を「累積学習回数」で刻む | 35 |
| `developmental_schedule.py` | writhing→fidgety移行のスケジュール（2026-07-23設計）。 | 48 |

### 原則 （3本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `homeostatic_scaling.py` | 恒常的シナプススケーリング（Homeostatic Synaptic Scaling） — | 57 |
| `precision_perception.py` | 精度重み付き知覚（Precision-Weighted Perception） — 感覚と期待を精度で混ぜる | 52 |
| `predictive_coding_latent.py` | 確率的な潜在変数＋推論時のその場調整（PV-RNNに着想） | 129 |

### 脳の土台（1歩を回す） （23本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `basal_ganglia.py` | 【転送のみ】このファイルの中身は basal_ganglia/basal_ganglia.py へ移した（2026-08-30の整理）。 | 12 |
| `brain_tick.py` | 太郎の脳の「1周期」──毎tickで脳が行う処理そのもの（2026-09-13・段B-1）。 | 2017 |
| `cerebellum.py` | 【転送のみ】このファイルの中身は cerebellum/speech.py へ移した（2026-08-30の整理）。 | 12 |
| `cerebellum_motor.py` | 【転送のみ】このファイルの中身は cerebellum/motor.py へ移した（2026-08-30の整理）。 | 12 |
| `corticospinal.py` | 【転送のみ】このファイルの中身は cerebral_cortex/frontal_lobe/corticospinal.py へ移した（2026-08-30の整理）。 | 12 |
| `developmental_clock.py` | 【転送のみ】このファイルの中身は development/developmental_clock.py へ移した（2026-08-30の整理）。 | 12 |
| `developmental_schedule.py` | 【転送のみ】このファイルの中身は development/developmental_schedule.py へ移した（2026-08-30の整理）。 | 12 |
| `dopamine.py` | 【転送のみ】このファイルの中身は neuromodulator/dopamine.py へ移した（2026-08-30の整理）。 | 12 |
| `double_touch.py` | 【転送のみ】このファイルの中身は drives/double_touch.py へ移した（2026-08-30の整理）。 | 12 |
| `hippocampus.py` | 【転送のみ】このファイルの中身は limbic/hippocampus.py へ移した（2026-08-30の整理）。 | 12 |
| `homeostatic_scaling.py` | 【転送のみ】このファイルの中身は principles/homeostatic_scaling.py へ移した（2026-08-30の整理）。 | 12 |
| `imitation_reward.py` | 【転送のみ】このファイルの中身は drives/imitation_reward.py へ移した（2026-08-30の整理）。 | 12 |
| `learning_progress.py` | 【転送のみ】このファイルの中身は drives/learning_progress.py へ移した（2026-08-30の整理）。 | 12 |
| `locus_coeruleus.py` | 【転送のみ】このファイルの中身は brainstem/locus_coeruleus.py へ移した（2026-08-30の整理）。 | 12 |
| `motor_cortex.py` | 【転送のみ】このファイルの中身は cerebral_cortex/frontal_lobe/motor_cortex.py へ移した（2026-08-30の整理）。 | 12 |
| `object_files.py` | 【転送のみ】このファイルの中身は cerebral_cortex/parietal_lobe/intraparietal_sulcus.py へ移した（2026-09-04）。 | 14 |
| `precision_perception.py` | 【転送のみ】このファイルの中身は principles/precision_perception.py へ移した（2026-08-30の整理）。 | 12 |
| `predictive_coding_latent.py` | 【転送のみ】このファイルの中身は principles/predictive_coding_latent.py へ移した（2026-08-30の整理）。 | 12 |
| `sensorimotor_brain.py` | 【転送のみ】このファイルの中身は cerebral_cortex/sensorimotor_brain.py へ移した（2026-08-30の整理）。 | 12 |
| `superior_colliculus.py` | 【転送のみ】このファイルの中身は midbrain/superior_colliculus.py へ移した（2026-08-30の整理）。 | 12 |
| `taro_brain.py` | 【転送のみ】このファイルの中身は cerebral_cortex/recurrent_core.py へ移した（2026-08-30の整理）。 | 12 |
| `taro_brain_motor.py` | 【転送のみ】このファイルの中身は cerebral_cortex/recurrent_core_motor.py へ移した（2026-08-30の整理）。 | 12 |
| `vocal_tract.py` | 【転送のみ】中身は body/vocal_tract.py へ移した（2026-08-30の整理）。 | 17 |

### からだ （12本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `adenosine.py` | アデノシン — 眠さの原因物質 | 44 |
| `blood_vessel.py` | 血管（血中栄養素） — 胃と空腹感の間にあるバッファ | 57 |
| `development.py` | 月齢(age_months) → 発達パラメータ の対応表を一本化する（2026-08-23新設）。 | 148 |
| `homeostasis.py` | 恒常性（Homeostasis） — 体の状態を快適値に保とうとする本能 | 74 |
| `infant_body.py` | 新生児の体型（プロポーション）— 太郎の身体そのものの定義。 | 1184 |
| `infant_limbs.py` | 四肢の「発達の向きの逆転」を解消する補正。注意恣意性は小さいが逸脱リスト対象。 | 704 |
| `infant_neck.py` | 新生児の「首がすわっていない」を再現する補正。注意**恣意的な逸脱**（下記ラベリング参照）。 | 266 |
| `internal_state.py` | 内受容感覚 — 身体の信号を統合して脳に渡す | 197 |
| `joint_compliance.py` | 関節可動域の限界へ近づく抵抗を滑らかにする — MuJoCoのjnt_solimpを実行時に書き換える。 | 461 |
| `smooth_actuation.py` | 筋活性化ダイナミクス（一次遅れ）を既存のトルクモータに足す実行層（taro-C5 実験①）。 | 82 |
| `stomach.py` | 胃 — 食べ物を受け取り、消化で空になる容器 | 107 |
| `vocal_tract.py` | 声道シミュレータ — 太郎の「口」 | 305 |

### 外枠 （1本）

| ファイル | 何をするか | 行 |
|---|---|---|
| `hybrid_env.py` | HybridEnv — MIMo（外側の身体）と太郎の内臓（内側の身体）を束ねるラッパー層。 | 184 |

