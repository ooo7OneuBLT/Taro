# 部位の入力と出力

**自動生成です。表を手で書き足さないでください**（`python run/tools/index_core.py` で作り直す）。
手で書いてよいのは下の印で囲まれた節だけです。

公開している関数が、**何を受け取り・何を返し・何をするか**。コードの引数と返り値から機械的に書き起こしたもの。
**「なぜ」は載せません**（コードのコメントにあります）。
全体像は `doc/太郎の構造.md`、呼称は `doc/脳の地図.md` §0。

## 目次

| 部位 | ファイル | 公開関数 |
|---|---|---|
| 走らせる仕組み（入口・設定・学習ループ） | 9 | 57 |
| 世界（環境・親・おもちゃ） | 9 | 57 |
| 感覚（目・耳・触覚） | 13 | 53 |
| 大脳皮質 | 7 | 66 |
| 大脳皮質 / 頭頂葉 | 4 | 11 |
| 大脳皮質 / 側頭葉 | 1 | 8 |
| 大脳皮質 / 前頭葉 | 2 | 1 |
| 左前頭葉（ブローカ野） | 1 | 5 |
| 言語海馬 | 1 | 7 |
| 中脳 | 6 | 22 |
| 脳幹 | 4 | 13 |
| 小脳 | 2 | 11 |
| 脊髄 | 6 | 22 |
| 皮質下核 | 2 | 9 |
| 大脳辺縁系 | 1 | 3 |
| 欲求 | 3 | 12 |
| 神経修飾 | 1 | 2 |
| 目標喃語 | 2 | 7 |
| 成熟 | 2 | 4 |
| 原則 | 3 | 5 |
| 脳の土台（1歩を回す） | 23 | 2 |
| からだ | 12 | 72 |
| 外枠 | 1 | 3 |
| 測る道具（実験ファイルで付け外しする） | 1 | 8 |
| 測る道具 / 共通 | 27 | 109 |

<!-- 手書きここから（自動生成はこの間を書き換えません） -->
（手書きの節はここには置きません。「実測でわかっている接続の穴」は
`doc/太郎の構造.md` へ移しました＝**毎回読む文書に手書きを集める**ため。
ここは完全に自動生成です。2026-09-15）
<!-- 手書きここまで -->

---

## 走らせる仕組み（入口・設定・学習ループ）

### `run/config.py`

実験の設定を1つの入れ物にまとめる。

**class `Config`** 実験の設定。属性で読む（`cfg.lr` `cfg.reward`）。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `from_spec` | cls, spec | あり | 実験ファイル（辞書）から作る。これが正式な経路。 |
| `from_env` | cls | あり | 環境変数から作る。注意古い方式と同じ数値が出るか確かめるためだけに使う。 |
| `is_muscle` | — | あり | **（説明文なし）** |
| `target_kind` | — | あり | 予測対象の種類。'0' / 'vision' / 'all'。 |
| `target_has_vision` | — | あり | **（説明文なし）** |
| `target_has_all` | — | あり | **（説明文なし）** |
| `grows` | — | あり | 体を育てる実験か。 |
| `age_at` | step | あり | 学習が step 回進んだ時点の月齢。age_to が無ければ常に同じ。 |
| `summary` | — | あり | 既定から変えた項目だけを並べる（ログの1行目に出す）。 |
| `as_dict` | — | あり | モデルの保存に入れる用（あとから条件を確認できるように）。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `touch_setting_of` | model_path, verbose | あり | 保存されたモデルを覗いて、触覚の設定（touch / somatosensory）を言い当てる。 |

### `run/context.py`

実行中の「いま」をまとめた入れ物。プラグインはこれだけを受け取る。

**class `Ctx`** 実行中の状態。プラグインへ渡す唯一の入れ物。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `log` | row | なし | **（説明文なし）** |
| `sim_sec` | — | あり | シミュレーション上の経過秒数。 |

### `run/log_setup.py`

ログの置き場所と等級をここ1か所で決める（2026-09-12）。

**class `_JaFormatter`** 等級を日本語にして、行を短く保つ。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `format` | record | あり | **（説明文なし）** |

**class `_ShortFormatter`** エラー.log 用。1件が長すぎると一覧性が死ぬので切る（2026-09-12 の実測で必要に）。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `format` | record | あり | **（説明文なし）** |

**class `_Counter`** 警告・エラーが何件出たかを数えるだけ（走行の最後に出すため）。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `emit` | record | なし | **（説明文なし）** |

**class `_MikanFilter`** `taro.未実装` だけを通す／落とす。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `filter` | record | あり | **（説明文なし）** |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `get_logger` | name | あり | モジュールごとのログ口。`__name__` を渡す。 |
| `mikan_logger` | name | あり | 「ここは後で作る」用のログ口。未実装.log にだけ残り、エラーとは混ざらない。 |
| `setup_logging` | out_dir | あり | 走行の入口で1回だけ呼ぶ。2回目以降は何もせず同じパスを返す（冪等）。 |
| `log_crash` | exc | あり | 落ちた例外を エラー.log に記録する。同じ例外は二度書かない。 |
| `swallowed` | log, 場面, level | なし | `except Exception: pass` の置き換え。黙って消さず、traceback を残して続行する。 |
| `warn_if` | log, 条件, fmt, *args | あり | 条件が真なら警告を残す。**2026-09-12 の教訓からいえば、ここが一番効く。** |
| `counts` | — | あり | {"警告": n, "エラー": n}。まだ setup していなければ 0。 |
| `paths` | — | あり | **（説明文なし）** |
| `log_tail` | p | あり | 走行の**最後**に呼ぶ。エラー.logの中身を画面の一番下に出す。 |

### `run/loop.py`

古い経路（`E/scripts/e_growth_train.py`）を呼ぶ橋。**比較のためだけに残す**。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `run_train` | spec | あり | 学習（または目視）を回す。注意既存の e_growth_train を子プロセスで呼ぶ。 |

### `run/main.py`

シミュレーションシステム ── 実験・学習・目視の唯一の入口。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `load_spec` | path | あり | 実験ファイルを読む。書き間違いをここで止める。 |
| `build_plugins` | spec | あり | 実験ファイルの plugins 欄から、使う道具を組み立てる。 |
| `run` | spec | あり | **（説明文なし）** |
| `main` | — | あり | **（説明文なし）** |

### `run/taro_setup.py`

太郎一式（脳・学習器・神経調節・小脳）を組み立てる。

**class `_DoubleTouchBonusContributor`** taro.reward_contributors の1要素（2026-08-05・全身一般化の設計1-4節）。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `compute` | ctx | あり | **（説明文なし）** |

**class `_MouthTouchBonusContributor`** taro.reward_contributors の1要素（2026-08-12・口元自己接触報酬の実装仕様3節）。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `reset` | — | なし | エピソード境界で呼ぶ。前回tickの状態を引き継がない。 |
| `compute` | ctx | あり | **（説明文なし）** |

**class `Taro`** 太郎そのもの。脳・学習器・神経調節・小脳・海馬を持つ。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `build_visual_attention` | — | あり | 太郎の視覚と注意（見る→注意を決める→眼球へ命令）を組み立てる。 |
| `on_body_change` | env | あり | 体を作り直したら、触覚の地図を差し替える。**学習した重みは保つ。** |
| `apply_touch_adaptation` | obs, is_reset | あり | 触覚の順応を1回だけ進め、obs["touch_percept"]を追加した新しいdictを返す。 |
| `encode_target` | obs | あり | 予測する対象を作る（元 `ln_prop`）。既定は固有感覚のみ。 |
| `encode_reach_goal` | obs | あり | 目標ベクトル g（22次元）＝腕の固有感覚(7)＋自己接触(15)。座標は作らない。 |
| `dummy_reach_goal` | like | あり | 陰性対照：実在の目標と似た値域だが、意味的な相関の無いランダム目標を作る。 |
| `block_pe` | pred, target, blocks | あり | 予測誤差＝**ブロックごとに平均してから足す**（次元数の影響を除く）。 |
| `infer_latent` | sv, prev_a, cf, h | あり | [感覚, 前回の行動] → 内部表現 z（元 `zc`）。太郎の infer_latent を呼ぶだけ。 |
| `act_mean` | z | あり | 決定的な行動平均（評価・agency用）。太郎の act_deterministic を呼ぶだけ。 |
| `motor_drive` | z | あり | 行動の平均と揺らぎ。運動野の精密制御＋小脳の自動化ブレンド。 |
| `infer_goal_action` | z, clp, init_mean, g, n_steps, lr_inf | あり | 目標指向：凍結した順モデルを反転し、望む感覚 g に届く行動を推論する。 |
| `infer_reach_goal_action` | z, gclp, init_mean, g, n_steps, lr_inf | あり | 目標指向（手先位置の目標表現・案C版）：凍結した reach_head を反転し、 |
| `init_state` | obs | あり | 学習ループが持つ「いまの状態」の初期値。 |
| `save` | path | なし | 確立した自己モデルを保存する。条件も一緒に入れる。 |

### `run/trainer.py`

学習ループ本体。太郎を動かし、学習させ、プラグインに測らせる。

**class `Trainer`** 太郎に「生きて学ぶ」をさせる。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `build` | — | あり | 環境と太郎を作る。乱数の順序を元の実装に合わせる。 |
| `step_k` | a | あり | 1回の判断で K 物理ステップ進める（＝同じ命令を K tick 保持する）。 |
| `reset_state` | — | なし | **（説明文なし）** |
| `run` | — | あり | 学習ループ。e_growth_train.py 1175〜1304行の写し。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `close_env` | env | なし | 環境を閉じる。閉じる処理そのものの失敗で本来の例外を隠さない。 |
| `train` | cfg | あり | 設定から学習を1本回す。これが新しい経路の本体。 |

### `run/viewer.py`

目視（等倍速で太郎を見る）。`run.type = "view"`。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `view` | cfg | あり | 学習済みの太郎を等倍速で動かして見る。学習しない。 |

### `run/wiring_map.py`

太郎の中身の地図 ── 何が実装されていて、どの実験で何が効いているか。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `node_index` | — | あり | **（説明文なし）** |
| `is_on` | node, cfg | あり | この実験でその機構が効いているか。cfg は run/config.py の Config。 |

## 世界（環境・親・おもちゃ）

### `run/world/body_config.py`

体型補正の「実験設定」— 環境変数から係数を読み、太郎の身体定義（core）へ渡す。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `shape_enabled` | — | あり | E_SHAPE=0 で体型補正を完全に切る（＝素のmimoGrowth体型＝アブレーション）。 |
| `newborn_scales_from_env` | — | あり | 環境変数から体型の係数を読む。補正OFFなら全部1.0（＝触らない）。 |
| `head_elongation_from_env` | — | あり | 頭の楕円化率。補正OFFなら1.0（＝球のまま）。 |
| `body_scale_custom_from_env` | age, verbose | あり | 環境変数の設定で custom_measurements を作る。補正が全て1.0なら None を返す。 |
| `body_kwargs_from_env` | age, verbose | あり | 環境を作るときに渡す身体の設定を**まとめて**返す唯一の入口。 |
| `limb_scale_from_env` | — | あり | 四肢の筋力補正にさらに掛ける係数（感度分析用）。既定1.0＝補正そのまま。 |
| `limb_fix_enabled` | — | あり | E_LIMB_FIX=0 で四肢の筋力補正を完全に切る（＝素のmimoGrowth＝新生児が3.5倍強い）。 |
| `distal_mass_from_env` | — | あり | 手足（末端）の質量の倍率（感度分析用）。既定1.0＝何もしない。 |

### `run/world/hand_in_view.py`

【E1の指標】太郎の手が「両目の視野内」にある割合を測る＝hand regard の測定尺度。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `hand_in_view` | model, data, half_fov, mode | あり | E1の主指標の判定：手が視野に入っているか（左右どちらかの眼でよい）。 |
| `eye_angles` | model, data, target_pos | あり | 各眼から見て、target_pos が視線から何度ずれているか（度）。 |
| `main` | — | なし | **（説明文なし）** |

### `run/world/head_hold.py`

実験の測定条件 — 実験者が乳児の頭を抑える（視線誘導反射を測るため）。

**class `CaregiverHands`** 親が手で頭を支える。関節のバネとして表現する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `hold` | target, stiffness, verbose | あり | 頭を支え始める。 |
| `release` | verbose | なし | 支えるのをやめて、元のバネ（首の筋緊張）に戻す。 |
| `holding` | — | あり | **（説明文なし）** |
| `head_angles` | — | あり | 今の頭の角度 [度] を辞書で返す（支えられているかの確認用）。 |
| `offsets` | — | あり | 目標角からのずれ [度]。支えが効いているかはこれで判定する。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `joint_group` | short_name | あり | 関節名（"robot:" と左右の接頭辞を外した名前）から部位を判定する。 |
| `joints_to_support` | model, free | あり | 「自由にする部位」以外の、動かせる関節の名前を返す。 |

### `run/world/parent.py`

親のfollow-in labeling（F1-3） — 太郎の注視に随伴させて、物の名前を言う。

**class `ParentLabeling`** 親の状態機械。ToySupineEnv.step() から毎物理stepぶん update(env) を呼ばれる。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `reset` | — | なし | エピソード境界で呼ぶ（前エピソードの状態を持ち越さない。e_orienting.reset()と同じ位置づけ）。 |
| `update` | env | あり | 1物理stepぶん進める。発話が起きたstepだけ {"text","target"} を返す（他はNone）。 |

**class `WordSchedule`** 語の再生装置（F1-4h） — 親の状態機械を経由せず、指定した時刻に指定した語を

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `reset` | — | なし | エピソード境界で呼ぶ（ParentLabeling.resetと同じ位置づけ）。 |
| `update` | env | あり | 1物理stepぶん進める。 |

### `run/world/probes.py`

測定器（probe類）— 太郎を外から観察するための計測装置。学習ループ本体からは独立。

**class `ProbeContext`** 太郎モデル・環境・モデル操作ヘルパー・設定を束ねて probe へ渡す入れ物。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `advance_state` | hn, next_a | なし | 毎tick共通の状態更新：GRU隠れ状態を進め、prev_a（遠心性コピー）を更新する。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `evaluate` | ctx | あり | **（説明文なし）** |
| `agency_probe` | ctx, n | あり | **（説明文なし）** |
| `inverse_probe` | ctx, n, n_steps, n_restarts, lr_inf | あり | 逆モデルStage1診断：凍結した順モデル(nat_head)を"反転"して、望む次感覚に当てる |
| `inverse_exec_probe` | ctx, n, n_steps, n_restarts, lr_inf | あり | 逆モデルStage1.5＝実行テスト：推論a*を"実際にMIMoで実行"し、現実の次感覚が |
| `closed_loop_probe` | ctx, goal_buf, n, max_reach | あり | C4診断：閉ループ制御 vs 開ループ で、目標姿勢へどれだけ届くか。 |
| `record_video` | ctx, mp4_path, make_render_env | なし | 学習後の太郎を等速で録画する。 |

### `run/world/scene.py`

環境を作る。環境を組み立てるのはここだけ。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `resolve` | scene_name, taro, world | あり | シーンを読んで、実験ファイルの `taro` 欄による上書きまで済ませた辞書を返す。 |
| `build` | scene_name | あり | シーンの名前から環境を作る。 |

### `run/world/toy_env.py`

E1環境：仰向けの太郎の「手の届く所」に、押すと動く**おもちゃ**を置く。

**class `ToySupineEnv`** 仰向け＋手の届く所におもちゃ（押すと動く対象）。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `place_toy_now` | — | なし | 登場演出を飛ばして、いますぐ視線の正面へ置く。 |
| `toy_contacts` | — | あり | 今おもちゃに触れている体の部位名のリスト。 |
| `toy_distance` | — | あり | 肩からおもちゃまでの距離[m]（届く範囲=REACH_MAX と比べるため）。 |
| `hand_toy_distance` | — | あり | 手からおもちゃまでの距離[m]（リーチの成否を測る素材）。 |
| `reset_model` | — | あり | **（説明文なし）** |
| `step` | action | あり | **（説明文なし）** |
| `posture_height_reward` | — | あり | 頭の高さ報酬（reward="posture_height"）。座位開始時（reset_model直後、 |
| `pin_root` | on, qpos | あり | 体全体（自由関節）を今の位置・向きに留める／解除する。 |
| `pin_joints` | names, on | あり | 指定した関節を「今の角度」に完全固定する（毎ステップ書き戻す）。 |
| `get_vision_obs` | — | あり | MIMoの壊れたgym描画を迂回し、眼球カメラを生APIで直接描画する（D側と同じ）。 |
| `anchor_distance` | — | あり | おもちゃが吊り下げ基準点からどれだけずれているか[m]（押された量の目安）。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `required_px` | age_months, fovy_deg | あり | 視力フィルタが効く上限まで画素を用意するのに必要な解像度[px]（F1-7新設）。 |
| `infant_vision_params` | size, fovy, acuity_age, fovea_camera, develop_from_age, fovea_fovy, acuity_filter | あり | 新生児の視覚パラメータ。acuityに月齢を渡す（＝解像度を恣意的に決めない）。 |

### `run/world/toy_touch.py`

おもちゃに手が触れたか・手とおもちゃの距離を測る。判定の実体はここだけ。

**class `ToyTouchProbe`** おもちゃへの接触と最接近距離を溜める。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `rebind` | model | なし | 体を作り直したあとに id を引き直す。溜めた回数・最接近は**消さない**。 |
| `update` | model, data | なし | **（説明文なし）** |
| `summary` | dt_per_step | あり | dt_per_step: 1ステップの秒数（学習ループなら K*DT=0.1秒） |
| `line` | dt_per_step | あり | **（説明文なし）** |

### `run/world/visibility.py`

「太郎に見えているか」を判定する共通の道具。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `gaze_angle` | model, data, toy_bid, camera | あり | 視線と対象のなす角[度]。注意遮蔽を見ないので単独で使わないこと。 |
| `visible_by_ray` | model, data, toy_bid, camera, exclude_head | あり | 目から対象へ光線を飛ばし、遮られていないかを見る。 |
| `red_mask` | image | あり | 画像から「赤が飛び抜けて強い画素」を拾う。おもちゃ（赤）の検出用。 |
| `visible_in_image` | image, min_pixels | あり | 太郎の目に実際に映っているか。赤い画素の数と、その重心を返す。 |
| `segment_mask` | model, data, body_id, camera, size | あり | MuJoCo に描き分けさせて、対象の画素を正確に取り出す。 |
| `visible_by_segment` | model, data, body_id, camera, size, min_pixels | あり | 描き分けで「対象が視野に何画素映っているか」と重心を返す。 |
| `close_renderers` | — | なし | 作った Renderer を片付ける（環境を作り直す前に呼ぶ）。 |
| `report` | model, data, toy_bid, image, camera | あり | 3つの判定をまとめて返す。食い違いがあれば、それ自体が情報になる。 |
| `describe` | r | あり | report() の結果を1行の日本語にする。 |

## 感覚（目・耳・触覚）

### `taro_core/src/senses/fusion.py`

感覚融合（fusion）— 各感覚エンコーダの出力を1本のベクトルに統合する、太郎の"本物の融合層"。

**class `MinimalFusion`** 内受容＋固有感覚＋前庭覚を基本に、触覚・視覚をフラグで足せる融合層。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `parameters` | — | あり | insula・proprio・vestibular・touch（設定されていれば）・vision（設定されていれば）の各パラメータを連結したイテレータを返す。引数は無い。 |
| `encode` | obs | あり | 観測辞書 obs を受け取り、内受容・固有感覚・前庭覚（と有効なら触覚・視覚）を各エンコーダに通して連結し、layer_norm をかけた融合ベクトルを返す。 |
| `freeze` | — | あり | 正解ターゲット用に凍結（RND式＝予測側と正解側の馴れ合い崩壊を防ぐ）。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `to_tensor` | x | あり | x を numpy 配列に変換してから、torch.float32 のテンソルに変換して返す。 |

### `taro_core/src/senses/hearing.py`

耳（Hearing） — 親の発話文字列を、脳が扱えるトークン列に変える薄い入口。

**class `Vocabulary`** 見た文字から動的に語彙を構築する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `encode` | text | あり | 文字列 text を1文字ずつ走査し、未登録の文字があれば新しいIDを語彙に追加しながら、各文字をIDに変換したリストを返す。 |
| `add_special` | token | あり | 複数文字の特殊トークン（"<PARENT>" 等）を1つのIDとして登録する |
| `decode` | indices | あり | 整数のリスト indices を受け取り、各要素を対応する文字に戻して連結した文字列を返す。<PAD>/<BOS>/<EOS>は出力に含めず、未登録のIDは"?"にする。 |

**class `Hearing`** 耳。環境から親の発話文字列を受け、トークン列にして脳へ渡す入口。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `hear` | text | あり | 発話文字列text を受け取り、トークンID列を返す。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `expand_long_vowel` | text | あり | 「ー」を直前の母音に展開する（"ぶーぶー" → "ぶうぶう"）。 |
| `to_hiragana` | text | あり | カタカナ（ァ U+30A1 〜 ヶ U+30F6）をひらがなに写す。「ー」はそのまま残す（次段で母音に展開）。 |
| `normalize_kana` | text | あり | 耳に届く文字列の正規化＝カタカナ→ひらがな→長音展開。 |

### `taro_core/src/senses/insula.py`

島皮質（Insula） — 体の感覚を大脳皮質に伝える経路

**class `Insula`** 島皮質。体の感覚を脳が処理できるベクトルに変換する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `forward` | state_vector | あり | 内部状態ベクトル → 脳が処理できるベクトル。 |

### `taro_core/src/senses/object_detector.py`

物の切り出し ── 「ここに何かがある」だけを返す。種類は出さない。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `patch_features` | model, img224, device | あり | DINOv2 のマスごとの特徴（パッチトークン）を取り出す。 |
| `figure_mask` | patch_feats, n, thresh | あり | 視野の中心のマスを種にして、図（0=地, 1=図）のマスクを返す。thresh は0〜1、 |
| `detect` | patch_feats, n, img_size, thresh, min_cells, img, mask_generator, cohesion_gap_px, point_predictor, blobs | あり | 検出の一覧を返す。各検出＝ (中心xy[画素・img_size基準], 面積比[0-1], 見た目ベクトル[384])。 |
| `load_mobilesam` | weights_path, device, points_per_batch | あり | MobileSAM（訓練済み・軽量な物体切り出しモデル）を読み込む。1回だけ呼んで |
| `make_point_predictor` | mask_generator | あり | 点プロンプトによる問い合わせ（`segment_at_points`）用の `SamPredictor` を返す。 |
| `segment_at_points` | predictor, img224, points, patch_feats, n, img_size, min_area_frac, max_area_frac, blobs | あり | 段2 分節：点プロンプトで指定した場所だけを切り出す（`predictor.set_image` |

### `taro_core/src/senses/otolith_organs.py`

耳石器（前庭迷路の一部）— 卵形嚢・球形嚢。直線加速度と、重力に対する頭の傾きを感知する。

**class `OtolithOrgans`** 重力方向を推定し、傾きに対する眼球反応（ocular counter-roll）を出す耳石器。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `reset` | — | なし | 推定中の重力方向 gravity_est をゼロベクトルに戻し、_warm を0に戻す。upright は変更しない。引数・返り値ともに無い。 |
| `update` | accel, dt | あり | 比力 accel[m/s^2] を1ステップ入れて、推定した重力方向を返す。 |
| `roll_angle` | — | あり | 推定した左右の傾き[rad]。基準方向からのずれのうち、ロール成分を返す。 |
| `counter_roll` | — | あり | 眼球の目標回旋角[rad]。傾きと逆向きに、利得 0.15 だけ回す。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `read` | vestibular_obs | あり | 前庭ベクトル(6次元)から、耳石器に相当する直線加速度3軸を取り出す（生の値）。 |

### `taro_core/src/senses/proprioceptive_map.py`

固有感覚（腕の関節角度）の中から、狙った関節だけを取り出す索引表を作る。

**class `ArmProprioMap`** 片腕7関節の、obs["observation"]内での位置と可動域。

公開しているメソッドはありません。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `build_arm_proprio_map_from_env` | env, side | あり | env から、片腕7関節の obs["observation"]内indexと可動域を作る。 |

### `taro_core/src/senses/retina.py`

網膜 — 「自分が動いたせいで流れる背景」と「対象そのものの動き」を分ける。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `object_motion` | motion, fovy_deg, surround_deg, uniform_gain | あり | 動きマップから「まわりも一緒に動いている成分」を割り引く。 |

### `taro_core/src/senses/semicircular_canals.py`

三半規管（前庭迷路の一部）— 頭の回転（角速度）を感知する。

**class `SemicircularCanals`** 高域通過フィルタつきの三半規管。角速度を入れると「感じている回転」を返す。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `reset` | — | なし | baseline と sensed を両方ゼロベクトルに戻す。引数・返り値ともに無い。 |
| `update` | omega, dt | あり | 角速度 omega[rad/s] を1ステップ入れて、感じている回転を返す。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `time_constant` | age_months | あり | 月齢に応じた実効時定数[秒]。新生児10秒 → 成人20秒へ線形に延びる。 |
| `read` | vestibular_obs | あり | 前庭ベクトル(6次元)から、三半規管に相当する角速度3軸を取り出す（生の値）。 |

### `taro_core/src/senses/sensory_encoders.py`

感覚の通訳層（Phase 5・接続層） — MIMoの各感覚を、太郎の脳が読める形に変換する。

**class `ProprioceptionEncoder`** 固有感覚の通訳。MIMoの `observation`（関節角度・速度・トルク・可動域、621次元）を

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `forward` | observation | あり | observation（固有感覚の生の数値ベクトル）を1層のLinearに通し、embedding_dim次元のベクトルを返す。 |

**class `VestibularEncoder`** 前庭覚の通訳。MIMoの `vestibular`（加速度3＋角速度3、6次元）を

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `forward` | vestibular | あり | vestibular（前庭覚の生の数値ベクトル）を1層のLinearに通し、embedding_dim次元のベクトルを返す。 |

**class `TouchEncoder`** 触覚の通訳。MIMoの `touch`（全身4,274点×力ベクトル3、12,822次元）を

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `forward` | touch | あり | touch（触覚の生の数値ベクトル）を Linear→ReLU→Linear の2層に通し、embedding_dim次元のベクトルを返す。 |

### `taro_core/src/senses/somatosensory_cortex.py`

体性感覚系(触覚)の脳内経路：視床VPL核（部位別集約） → 一次体性感覚野S1（部位間統合）。

**class `TouchMap`** 触覚flat配列を「点→部位」に対応づける地図。体を作り直すたびに作り変える。

公開しているメソッドはありません。

**class `SomatosensoryCortex`** 視床VPL核（部位別集約） + 一次体性感覚野S1（部位間統合）。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `rebuild` | touch_map | あり | 体を作り直したときに地図だけ差し替える。**学習した重みは保つ。** |
| `part_features` | touch_flat | あり | 部位ごとの要約を返す。(..., 部位数, 5) ＝ 有無・強さ・重心x・重心y・重心z。 |
| `forward` | touch_flat | あり | touch_flat: (..., n_points*3) の触覚flat配列。戻り値: (..., embedding_dim)。 |
| `summary` | — | あり | デバッグ用サマリー(初回起動時にprintすると設計が一目で確認できる)。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `body_names_of_group` | group_name | あり | 触覚グループ名（例："right_palm"）から、実際のMuJoCoのbody名のリストを引く。 |
| `build_touch_map` | model, data, touch | あり | MIMoの環境から TouchMap を作る。 |
| `build_touch_map_from_env` | env | あり | gym の環境から TouchMap を作る。呼び出し側の定型処理をまとめただけ。 |
| `build_sensor_layout` | model, touch | なし | 呼び出されると常に NotImplementedError を送出する。model と touch を引数に取るが内部では使わない。返り値は無い。 |

### `taro_core/src/senses/touch_adaptation.py`

触覚の順応（同じ場所を押され続けると感じ方が弱まる、人間の受容器が持つ性質）。

**class `TouchAdaptation`** 触覚の順応（速順応＋遅順応・末梢＋脳）の状態を持つ。1点ずつ独立に進む。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `total_dim` | — | あり | n_points に3を掛けた値（触覚flat配列の長さ）を返すプロパティ。引数は無い。 |
| `rebuild` | n_points | なし | 体を作り直した（点の数・並びが変わった）ときに呼ぶ。全点リセット。 |
| `reset_episode` | — | なし | エピソード境界(env.reset())用の軽量リセット。点数は変えない。 |
| `advance` | touch_flat | なし | 新しい物理観測1個ぶん、状態を進める。戻り値は無い（adapted()で取る）。 |
| `adapted` | — | あり | 順応済みの値（touch_flatと同じ形）を返す。状態は変えない。 |

### `taro_core/src/senses/vision_backends.py`

視覚バックエンドの差し替え機構（F1-3b）— 語の学習が使う「見ていたもの」の

**class `CustomVisionBackend`** 後方互換の既定。taro.fusion.vision（未訓練・ランダム初期化のCNN、64次元）を

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `dim` | — | あり | self._enc（VisionEncoderインスタンス）が持つ fuse 層の出力次元数を整数で返すプロパティ。 |
| `encode` | img_left, img_right | あり | img_left, img_right を self._enc に渡してベクトルを得て、detach・CPU転送したうえでnumpy配列に変換して返す。 |

**class `DINOv2VisionBackend`** torch.hub経由のDINOv2 ViT-S/14。両目それぞれフォビア切り出し→エンコード

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `dim` | — | あり | self._dim（384）を返すプロパティ。 |
| `encode` | img_left, img_right | あり | self.eye の設定（both/left/right）に応じて片目または両目の画像を_encode_one_eyeでエンコードし、bothの場合は左右の平均を取る。得られたベクトルをL2正規化してnumpy配列で返す。 |

**class `UntrainedViTVisionBackend`** 訓練前の ViT-S/14（重みランダム）。dinov2_vits14 と器の形は同一。

公開しているメソッドはありません。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `fovea_crop` | img, fovea_px | あり | 画像の中央 fovea_px × fovea_px を切り出す。 |
| `register` | name | あり | 登録名 name を受け取り、クラスを引数に取って BACKENDS に name→クラスとして登録してからそのクラスをそのまま返す関数(deco)を返す。 |
| `get_backend` | spec | あり | taro.lexicon_vision の値からバックエンドを組み立てる。 |

### `taro_core/src/senses/vision_encoder.py`

視覚の通訳層（Phase 5・接続層） — MIMoの画像を、太郎の脳が読める形に変換する。

**class `VisionEncoder`** 視覚の通訳。MIMoのステレオ視覚（eye_left, eye_right、各256×256×3）を

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `forward` | eye_left, eye_right | あり | eye_left, eye_right をそれぞれ_encode_one_eyeでエンコードして連結し、fuse層に通してembedding_dim次元のベクトルを返す。 |

## 大脳皮質

### `taro_core/src/brain/cerebral_cortex/chunk_vocab.py`

塊の名簿（ChunkVocab） — 塊（モーラID列）に番号を振る器。

**class `ChunkVocab`** 塊(tuple[int]) <-> id の対応を動的に構築する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `size` | — | あり | 現在登録されている塊・特殊トークンの総数（次に割り当てるid）を返す。引数は無い（self のみ）。 |
| `add_special` | name | あり | 複数文字の特殊トークン（"<PARENT>" 等）を1つのidとして登録する。 |
| `encode_chunk` | chunk | あり | 塊(tuple[int])のidを返す。無ければ新規に割り当てる。 |
| `encode_chunks` | chunks | あり | 塊のリストをidのリストにする（encode_chunkを順に呼ぶだけ）。 |
| `chunk_string` | idx, hearing_vocab | あり | idを文字列にする（特殊トークンは""、塊はhearing_vocab.decodeで文字列に）。 |
| `state_dict` | — | あり | 保存用（torch.save可能な素のdict/listのみ。tupleはlist化）。 |
| `load_state_dict` | d | なし | state_dict形式の辞書 d を受け取り、chunk2idx・idx2chunk・specials・_next_id を復元する。返り値は無く、自身の状態を書き換える。 |

### `taro_core/src/brain/cerebral_cortex/message_layer.py`

言いたいことの層（MessageLayer） — 物の塊と述語の塊を別々に選んで並べる。

**class `MessageLayer`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `observe` | chunk_ids, state, key_vis | なし | 親の発話1文（塊id列、特殊トークン除く）と、そのときの状態・見た目を数える。 |
| `role` | chunk_id | あり | "noun" | "pred" | None(未知＝観測min_count未満、または群分けできない)。 |
| `compose` | state, noun_candidates | あり | 名詞の塊＋述語の塊を役割の順に並べる。 |
| `state_dict` | — | あり | 学習済みの内部状態（state_count・global_mean・vis_mean・vis_cons・pred_count・role_bigram・base・role_dev_thresh・min_count）をtorch.save可能な素のdictにして返す。引数は無い（self のみ）。 |
| `load_state_dict` | d | なし | state_dict形式の辞書 d を受け取り、内部状態（state_count・global_mean・vis_mean・vis_cons・pred_count・role_bigram・base・role_dev_thresh・min_count）を復元する。返り値は無い。 |

### `taro_core/src/brain/cerebral_cortex/recurrent_core.py`

大脳皮質（Cortex） — 予測する単一の再帰ネットワーク

**class `Vocabulary`** 見た文字から動的に語彙を構築する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `encode` | text | あり | 文字列 text を1文字ずつidに変換したリストを返す。未知の文字には新しいidを割り当てて語彙に追加する（語彙は動的に増える）。 |
| `decode` | indices | あり | idのリスト indices を文字列に戻して返す。<PAD>/<BOS>/<EOS>は結果から除かれ、未知のidは"?"として扱われる。 |

**class `TaroBrain`** 太郎の脳。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `predict_satiety` | hidden_last | あり | 【人間模倣】聞いた音＋体の状態（GRU隠れ状態）から「食べ物（授乳）が |
| `forward_hidden` | x, hidden, body_state, prefix_vec, state_id | あり | 入力トークンを処理して隠れ状態を更新する。 |
| `forward_perception` | x, hidden, body_state | あり | トークン列 x と隠れ状態 hidden（任意で body_state）を forward_hidden に通し、その出力に perception_head を適用して語彙サイズ分の予測logitsを作る。戻り値は (logits, 更新後のhidden)。 |
| `sequence_prob` | token_ids, hidden | あり | トークン列の「文脈から見た言いやすさ」を返す（2026-08-30・文脈設計の決定4）。 |
| `token_probs` | token_ids, hidden | あり | 列の各トークンを「直前までからどれだけ予測できたか」を返す（2026-08-31・二語文）。 |
| `end_probs` | token_ids, hidden, start | あり | 列の各位置で「ここで発話が終わる確率」を返す（分節第2案・2026-09-03）。 |
| `forward_articulation` | gru_output | あり | GRUの出力 gru_output を、口の部位(place)・調音法(manner)・声帯振動(voicing)・母音(vowel)それぞれのヘッドに通す。戻り値は4つのlogitsのタプル (place, manner, voicing, vowel)。 |
| `generate` | hidden, max_length, eos_idx, stamina, vocal_tract, ne_level, cerebellum, speech_plan, body_state, jaw_cycles | あり | 太郎の番に文字を産出する。 |
| `set_vocab_mapping` | char2idx | なし | 文字から語彙idへの対応表 char2idx を self._vocab_char2idx として保持する。引数は char2idx の辞書1つ、返り値は無い。 |
| `receive_ne` | ne_level | なし | アドレナリン受容体 — NEレベルを記録する。 |
| `resize_embedding` | new_vocab_size | なし | embeddingとperception_headの語彙サイズを new_vocab_size に拡張する。既存の重みは先頭部分へそのままコピーし、増えた行は新規初期化する。new_vocab_size が現在の語彙数以下なら重みは変更せず vocab_size の値だけ更新する。返り値は無い。 |

### `taro_core/src/brain/cerebral_cortex/recurrent_core_motor.py`

本物のTaroBrain（taro_brain.py）に、運動性喃語（身体の感覚運動予測ループ）専用の

**class `TaroBrainWithMotor`** TaroBrainのサブクラス。運動性喃語用の`step_motor()`だけを追加する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `predict_vision` | z, action, current_vision | あり | 残差予測：現在の視覚embedding + Δ(z, 行動) = 次の視覚embeddingの予測。 |
| `motor_gru` | — | あり | self.motor_cortex.motor_gru（運動用GRU本体）をそのまま返す転送用プロパティ。引数は無い。 |
| `pc_latent` | — | あり | self.motor_cortex.pc_latent（潜在変数の推論モジュール）をそのまま返す転送用プロパティ。引数は無い。 |
| `motor_head` | — | あり | self.motor_cortex.motor_head（潜在変数から関節指令を計算する層）をそのまま返す転送用プロパティ。引数は無い。 |
| `init_motor_hidden` | — | あり | 運動用GRUの初期隠れ状態を、形状 (num_layers, 1, hidden_dim) のゼロテンソルとして作って返す。引数は無い（self のみ）。 |
| `step_motor` | sensory_vec, hidden, current_sensory_target, ne_level, min_std, max_std | あり | 運動性喃語の1ステップ。 |
| `prediction_error` | predicted_next_sensory, actual_next_sensory | あり | 感覚運動予測誤差。 |
| `sensorimotor_reward` | prediction_error_value | あり | 感覚運動予測誤差 → 報酬[0, 1]への変換。 |
| `infer_latent` | sensory_vec, prev_action, current_sensory_target, hidden | あり | [感覚, 前回行動] → GRU → 潜在z（遠心性コピーを再帰の中に入れた改良版）。 |
| `predict_proprio` | z, action, current_proprio | あり | 残差予測：現在の固有感覚 + Δ(z, 行動) = 次の固有感覚の予測。 |
| `motor_drive` | z, ne_level, cerebellum, min_std, max_std | あり | 運動野の精密制御（motor_head）＋小脳の自動化ブレンドで、行動の中心(mean)と |
| `act_deterministic` | z, cerebellum | あり | 決定的な行動平均（評価・agency用、ノイズなし）。小脳ONなら自動化ブレンドを適用。 |
| `enable_spinal_babble` | n_act, leg_r, leg_l, arm_r, arm_l, beta, synergy, syn_w, seed, antagonist, co_activation, pair_offset | なし | 脊髄CPG（運動性喃語の探索ノイズ源）を太郎の中で有効化する。以後 explore() は |
| `explore` | mean, std | あり | 探索：mean を中心に std のゆらぎでサンプルし、(行動, log_prob) を返す。 |
| `to_env_action` | policy_action | あり | 脳が出した「関節レベルの指令」を、環境が受け取る形（筋レベル等）に変換する。 |
| `enable_reflex_common` | n_joint, moment_1, moment_2, groups | なし | 伸張反射＋揺らぐ振動子の共通駆動を太郎の中で有効化する。 |
| `set_reflex_common_baseline` | muscle_lengths | なし | 反射の基準長L0(0)を、env.reset()直後の実際のmuscle_lengthsで初期化する |
| `step_reflex_common` | dt, muscle_lengths, muscle_velocities | あり | 共通駆動を1tick進め、moment_1/moment_2経由で基準長オフセットに変換して |
| `consolidate` | learner, n_batches, batch_size | なし | 睡眠リプレイ：海馬に貯めた経験を再生し、順モデル（自己内部モデル）を定着させる。 |

### `taro_core/src/brain/cerebral_cortex/sensorimotor_brain.py`

感覚運動の脳（試作） — Phase 6・運動出力＋感覚運動予測ループ

**class `SensorimotorBrain`** 5感覚を統合し、次の感覚を予測しながら、運動を出力する試作の脳。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `init_hidden` | — | あり | 隠れ状態の初期値として、形状 (hidden_dim,) のゼロテンソルを返す。引数は無い（self のみ）。 |
| `step` | sensory_vec, hidden | あり | 1ステップ分の処理。 |
| `prediction_error` | predicted_next_sensory, actual_next_sensory | あり | 感覚運動予測誤差。予測と実際の感覚のズレ（MSE）。 |

### `taro_core/src/brain/cerebral_cortex/visual_projection.py`

視覚投射（VisualProjection） — 視覚野から連合野への「軸索の束」。

**class `VisualProjection`** 384次元の視覚ベクトル → embedding次元の「視覚トークン」1個。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `forward` | vec | あり | vec: tensor (in_dim,) → (out_dim,) |

### `taro_core/src/brain/cerebral_cortex/world_predictor.py`

連合野の世界予測器（M7a・測るだけ／M7b-1・物ごとに動かす）。

**class `_ObjState`** 物1つぶんの「記憶」（学習される重みではない）。

公開しているメソッドはありません。

**class `WorldPredictor`** 連合野の世界予測器（MTRNN型2段）。速い層=頭頂連合野、遅い層=前頭前野に相当。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `drop` | file_id | なし | その物の記憶を捨てる（重みには触らない）。 |
| `active_ids` | — | あり | 今記憶を持っている物のidの一覧（file_id=Noneの単一状態を含む）。 |
| `reset_hidden` | — | なし | 隠れ状態と直前の予測を全物ぶんリセットする（エピソード境界などで呼ぶ）。 |
| `observe_for` | file_id, target_now | あり | 多物モードphase1：state[file_id]に前tickの予測(state.pred)が |
| `flush_pending` | — | なし | 多物モードphase1と2の間：このtickぶんobserve_forへ積んだ誤差 |
| `predict_for` | file_id, input_now | あり | 多物モードphase2：flush_pendingの"後"に呼ぶ（更新済みの重みで |
| `step_for` | file_id, input_now, target_now | あり | 1tickぶん進める（file_idの物の記憶を使う）。 |
| `step` | input_now, target_now | あり | 従来（M7a・単一状態）のstep()。`step_for(None, input_now, target_now)`の別名 |
| `state_dict` | *args, **kwargs | あり | モジュールの重みだけを保存する（隠れ状態・baselineは含まない）。 |
| `load_state_dict` | state_dict, *args, **kwargs | あり | 塊名簿の大きさが保存時と違っても読めるようにする（2026-09-08、F2-89b の失敗を受けて）。 |

**class `_PortSpec`** ポートの規格（手で決める側。中身の学習はPortWorldPredictor側に任せる）。

公開しているメソッドはありません。

**class `_PortMem`** 1ポートぶんの「記憶」（学習される重みではない）。

公開しているメソッドはありません。

**class `PortWorldPredictor`** 速い層＝ポート（視覚・聴覚・体）、遅い層＝1つ共有、のMTRNN型世界予測器。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `drop` | file_id | なし | その物の視覚ポートの記憶を捨てる（重みには触らない）。 |
| `active_ids` | — | あり | 今、視覚ポートの記憶を持っている物のidの一覧。 |
| `reset_hidden` | — | なし | 全ポート・遅い層の隠れ状態と直前の予測をリセットする（エピソード境界などで呼ぶ）。 |
| `grad_report` | — | あり | 構築直後の重み（_init_params）との差で「動いた重み」を機械判定する。 |
| `observe_all` | vision_targets, hearing_target, body_target | あり | 1tickの①：全ポートのobserve（前tickの予測と今の観測を比べる）＋ |
| `predict_all` | vision_inputs, hearing_input, body_input | あり | 1tickの②：全ポートのpredict（今の入力で状態を更新し次tickの予測を作る）。 |
| `state_dict` | *args, **kwargs | あり | モジュールの重みだけを保存する（隠れ状態・baselineは含まない）。 |
| `load_state_dict` | state_dict, *args, **kwargs | あり | 塊名簿の大きさが保存時と違っても読めるようにする（既存WorldPredictorと同じ流儀）。 |

## 大脳皮質 / 頭頂葉

### `taro_core/src/brain/cerebral_cortex/parietal_lobe/intraparietal_sulcus.py`

物体ファイル ── 「これ」を物として保つ最小の仕組み。

**class `ObjectFile`** 1つの物体ファイル。位置・速度（カルマン状態）と見た目を持つ。名前は持たない。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `pos` | — | あり | 現在位置を (x, y) のタプルで返す。カルマン状態ベクトル self.x の先頭2要素をfloatにしたもの。引数は無い（self以外）。 |
| `predict` | t, coast_max_s, coast_min_speed_px_s, frame_dt_s, shift | なし | shift（【2026-09-09・仕様_見る側の段構成_実装「後半」段0・段3】）： |
| `compute_explained` | img_size | あり | 見失った瞬間に1度だけ呼ぶ。「消えたことがどれだけ説明できるか」を |
| `innovation_cov` | — | あり | 観測前の予測から決まるイノベーション共分散 S = H P H^T + R。 |
| `mahalanobis` | pos | あり | 予測位置と候補位置の距離を、予測の不確実性(P)で正規化した値。 |
| `update` | pos, appearance, area, appearance_lr, t, emb | あり | 新しい観測値でカルマンフィルタの状態を更新する。pos（観測位置）・appearance（観測した見た目、既存の見た目とappearance_lrの重みでEMA更新）・area（観測面積、そのまま置換）・t（省略可、last_seen_tを更新）・emb（省略可、そのまま置換）を受け取る。hitsを+1し、misses・since_seenを0に戻し、area_histにareaを追加し、budgetを1.0・explainedを0.0に戻す。戻り値は更新前の予測位置とposとの間のマハラノビス距離（float）。 |

**class `ObjectFileSystem`** 物体ファイルの集合を1コマぶん進める。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `predict_only` | t, shift | なし | 【2026-09-09・仕様_予測して確かめる検出】確認（confirm）専用：対応づけは |
| `step` | detections, t, protect_id, skip_predict, shift | あり | detections = [{"pos": (x,y), "area": float, "appearance": vec}, ...]（1コマぶん）。 |

### `taro_core/src/brain/cerebral_cortex/parietal_lobe/priority_map.py`

段6 注意の優先度地図 ── 「下からの目立ち」と「上からの目的」を1枚の地図で競わせる。

**class `PriorityMap`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `update` | files, t, motion_by_id, static_sal, surprise_trace, z_vec_by_id, goal_bonus, moving, dt | あり | 見えている（misses==0の）物体ファイルごとに、大きさ・中心近さ・動き・驚き・新奇性・目標ボーナス・IOR・慣れの重み付き和で優先度を計算し、ヒステリシスと切り替え遅延を経て次に注意する物（attended_id）を決める。探索地点（_compute_explore）とIOR値も計算する。files/t/motion_by_id/static_sal/surprise_trace/z_vec_by_id/goal_bonus/movingを使う（dtは受け取るが使わない）。戻り値はattended_id・switch_signal・switch_decided・priority・explore_point・iorを持つ辞書。 |

### `taro_core/src/brain/cerebral_cortex/parietal_lobe/spatial_priority_map.py`

場所の優先度地図 ── 「次にどこへ注意を向けるか」を、視野の升目ごとの値で持つ。

**class `SpatialPriorityMap`** 升目ごとの優先度を持ち、いちばん高い升を返す。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `update` | salience, shift_px, dt, goal | あり | 1コマぶん。 |

### `taro_core/src/brain/cerebral_cortex/parietal_lobe/visual_attention.py`

視覚と注意の手順そのもの（脳の部品）。

**class `VisualAttention`** 1コマぶんの「見る」を最初から最後まで回す。ctxを受け取らない。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `step` | — | あり | 呼び出し側（プラグイン）が毎tick呼ぶ。ctxは受け取らない。 |

## 大脳皮質 / 側頭葉

### `taro_core/src/brain/cerebral_cortex/temporal_lobe/segmentation.py`

分節 — 聞いた音の列を「語の塊」に切り分ける（側頭葉）

**class `Segmenter`** 聞いた発話を語の塊に切り分け、塊ごとの出会った回数を数える。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `segment` | tokens, confidences | あり | 発話を分節し、切り出した1単位（token列）を返す（無ければNone）。 |
| `segment_all` | tokens, confidences | あり | 発話を分節し、min_len以上の**全単位**をリストで返す（2026-08-31・二語文）。 |
| `segment_end_prob` | tokens, confidences, end_probs | あり | 発話を分節し、min_len以上の全単位をリストで返す（分節第2案・2026-09-03）。 |
| `observe` | tokens, confidences, end_probs, mode | あり | 発話を分節し、切り出した塊を数えて、いちばん長い塊を返す。 |
| `state_dict` | — | あり | 保存用（torch.save 可能な素の dict/list のみ）。 |
| `load_state_dict` | d | なし | 保存から復元する。古い `lexicon` 形式の blob もそのまま読める |
| `top` | n | あり | 出会った回数の多い塊を n 個返す。 |
| `known` | chunk | あり | その塊を何回聞いたか。 |

## 大脳皮質 / 前頭葉

### `taro_core/src/brain/cerebral_cortex/frontal_lobe/corticospinal.py`

皮質脊髄路（corticospinal tract）相当の薄い配線層。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `project` | mean, gen_out, w_mean | あり | 運動野の出力(mean)と脊髄CPGの出力(gen_out)を、w_meanで混合する。 |

### `taro_core/src/brain/cerebral_cortex/frontal_lobe/motor_cortex.py`

運動野（motor cortex）相当のモジュール。

**class `MotorCortex`** 

公開しているメソッドはありません。

## 左前頭葉（ブローカ野）

### `taro_core/src/brain/left_frontal_lobe/brocas_area.py`

ブローカ野（Broca's Area） — 左前頭葉にある発話計画の中枢

**class `BrocasArea`** ブローカ野。音韻バッファと構造バッファを持つ。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `plan` | parent_text, cerebellum, vocal_tract, use_hear_fallback | なし | 親の発話から発話計画を立てる。 |
| `has_next` | — | あり | 音韻バッファに未取り出しの項目が残っているかを返す。current_indexがstructure（計画の長さ）未満ならTrue。引数は無い。 |
| `next_motor` | — | あり | 音韻バッファの現在位置の項目（motor・known・targetを持つ辞書）を返し、current_indexを1つ進める。取り出す項目が無い（has_next()がFalse）場合はNoneを返す。引数は無い。 |
| `get_plan_length` | — | あり | 現在の発話計画に入っている項目数（structure）を返す。引数は無い。 |
| `reset` | — | なし | 音韻バッファ・structure・current_indexをすべて空/0に戻す。引数・戻り値は無い。 |

## 言語海馬

### `taro_core/src/brain/language_hippocampus/language_hippocampus.py`

言語海馬（LanguageHippocampus） — 聞いた発話の鎖状連想メモリ＋睡眠リプレイ。

**class `LanguageHippocampus`** 聞いた発話（視覚＋話者＋音列）をエピソード単位で焼き付け、睡眠でリプレイする。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `write` | key_vis, speaker, tokens, step, strength | なし | 1エピソードを焼き付ける（1回で覚える＝海馬の性質）。 |
| `decay` | — | なし | 睡眠1回ごとに全エピソードのstrengthを減衰させ、薄れきったものを消す。 |
| `sample` | rng, n | あり | strengthを重みに、直近偏重でn個のエピソードを抽出する（重複あり）。 |
| `recall` | key_vis | あり | key_visに最も近い（コサイン類似度）エピソードのtokensを返す（測定専用）。 |
| `recall_with_conf` | key_vis | あり | 【段階2・即答】key_visに最も近いエピソードの (tokens, 似ている度, 記憶の強さ) を返す。 |
| `state_dict` | — | あり | 保存用（torch.save可能な素のdict/listのみ。ndarrayはリスト化）。 |
| `load_state_dict` | d | なし | state_dict() が返した形の辞書 d を受け取り、capacity・decay・replay_passes・recent_ratio・sleep_lr・unit と episodes（key_visはndarrayに戻す）を自分の属性へ書き戻す。d にキーが無い項目は今の値のまま変えない。返り値は無い。 |

## 中脳

### `taro_core/src/brain/midbrain/efference_copy.py`

段0 座標の安定化（遠心性コピー）── 目を動かす命令の写しを、感覚側へ先回りで送る。

**class `EfferenceCopy`** 毎tick呼ぶ（検出コマでなくても）。orienting が None なら全部 0/False。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `consume_shift` | — | あり | 前に呼ばれてから今までに溜まった「画像の中身のずれ」を返し、0 に戻す。 |
| `update` | orienting, t | あり | 毎tick呼ばれ、orienting（目の反射オブジェクト、または None）と時刻tを受け取る（tは本体では使わない）。orienting が None のときは内部状態を全てリセットし、shift_pred・shift_actual・shift_accum・moving・eye_h・eye_v・d_eye_h・d_eye_v を全て0/Falseにした辞書を返す。それ以外のときは今回と前回の目の水平・垂直角度から実際の画素ずれ(shift_actual)を計算して内部の累積(_accum)に足し込み、目標角度との差から予告ずれ(shift_pred)も計算し、サッケード中かどうかから moving フラグ（終了後も数tickだけ真を保つ）を決める。返り値はこれらをまとめた辞書。 |

### `taro_core/src/brain/midbrain/orienting.py`

視線誘導反射（新版）＝設計図（`E/docs/視線誘導反射_設計図.md`）に基づく実装。

**class `OrientingReflexV2`** 視線誘導反射・新版。段階的に組み立てる。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `reset` | — | なし | 引数・返り値は無い。内部状態を初期化する。フレームバッファ、動き・バイアス・競合・IOR・疲労などの各種の地図、方向・強さ(h_dir/v_dir/strength)、内部時計(_t)とサッケードの残り時間・連射・保持の積み上げ項、認識信号(_recognition)、固視の場所の追跡などをすべて初期値に戻す。学習された適応倍率(_adapt_gain)だけは変えずに保持する。 |
| `set_map_target` | h_dir, v_dir, t, latency_s | なし | 【2026-09-10・見る側4段目】外（頭頂の優先度地図）から「ここを見ろ」を渡す。 |
| `set_recognition` | sim | なし | F1-4b：語から読み出した「思い浮かべているものとの一致度」を渡す。 |
| `update` | eye_image | なし | 【2026-09-11・退避】自前の動き検出は taro_core から外した。何もしない。 |
| `is_saccading` | — | あり | いまサッケードの最中か。VOR を抑制するかの判定に外から使う（2026-09-11）。 |
| `apply` | action, dt | あり | 関節指令の配列action と、経過時間dt（Noneならself.dtを使う）を受け取り、階段状に発射するサッケード（目標角度への位置フィードバック）を足し込んだ配列を返す。まず連射の続き・外部から指定された地図の目標(set_map_targetで渡されたもの)・自前の方向信号(h_dir/v_dir/strength)のいずれかで今回発射するかを判定し、発射しないときは保持(hold)条件を満たす場合に限り_hold_command() の結果をそのまま返す。発射する場合は新しい目標角度(self._tgt)を定め、以後は目標との角度差に応じた指令を目に（NECK_SHAREが0でなければ首にも）additiveに書き込み続け、目標に十分近づいて一定時間経つとサッケードを終え、サッケード適応倍率・連射の続行判定・IOR記録（それぞれ有効時のみ）を更新する。 |

### `taro_core/src/brain/midbrain/orienting_v1.py`

視線誘導反射（Orienting Reflex）：周辺視野の動きへ、首・目を反射的に向ける。

**class `OrientingReflex`** 視野の残差（自分の回転による均等なズレを引いた残り）から、動きの方向へ首・目を向ける。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `reset` | — | なし | 引数・返り値は無い。内部状態を初期化する。前回の目の画像キャッシュ(prev_eye)をNoneに戻し、水平・垂直の反射方向(h_dir, v_dir)を0.0にする。 |
| `update` | eye_image | なし | 新しい画像が描画されたときに呼ぶ（毎物理stepではない、視覚のキャッシュ間隔なり）。 |
| `apply` | action | あり | update()でキャッシュした方向を、actionに足して返す（首は加算、目はVOR適用後に加算）。 |
| `override` | action, eye_image | あり | update()+apply()をまとめて行う便利関数（単体テスト・簡易利用向け）。 |

### `taro_core/src/brain/midbrain/preattentive_map.py`

段1 前注意の地図 ── 目を向ける前に「どこで何が起きているか」を並列に作る。

**class `PreattentiveMap`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `update` | img224_gray_f32, shift_actual, moving | あり | 毎検出コマ呼ぶ。`moving`のコマは塊の一覧を空にする（前コマの保持だけ更新）。 |

### `taro_core/src/brain/midbrain/salience_map.py`

目立ちの地図 ── 視野ぜんたいの各場所に「どれくらい注意を向けるべきか」の値を1つ持つ。

**class `SalienceMap`** 下からの目立ちを1枚にまとめる。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `update` | img_rgb, shift_actual, moving | あり | 1コマぶん。 |

### `taro_core/src/brain/midbrain/superior_colliculus.py`

上丘（superior colliculus）— 視野の地図と、そこからサッケードを決める仕組み。

**class `CollicularMap`** 画像の各画素と上丘の座標を対応づける地図。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `to_grid` | image | あり | 画像 → 上丘の格子 [2, nv, nu]（0:右視野 / 1:左視野）。 |
| `grid_centroid` | grid, thresh_frac | あり | 格子の活動 → 視野の座標[度]（population vector）。 |
| `grid_direction` | grid, thresh_frac | あり | grid_centroid を -1〜1 に正規化して返す（右が正、上が正）。 |
| `population_vector` | activity, thresh_frac | あり | 上丘の座標で重心を取り、視野の座標[度]に戻す。 |
| `direction` | activity, thresh_frac | あり | population_vector を -1〜1 に正規化して返す（右が正、上が正）。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `visual_to_collicular` | ecc_deg, azim_rad, a, bu, bv | あり | 視野の位置 → 上丘の位置。 |
| `collicular_to_visual` | u, v, a, bu, bv | あり | 上丘の位置 → 視野の位置（逆変換）。 |
| `magnification` | ecc_deg, a, bu | あり | マグニフィケーションファクター M(R) = Bu/(R+A) [mm/度]。 |

## 脳幹

### `taro_core/src/brain/brainstem/atnr.py`

非対称性緊張性頸反射（ATNR, asymmetric tonic neck reflex）＝脳幹反射弓。

**class `ATNR`** model から首(head_swivel)のqpos位置・肘/肩外転アクチュエータidxを名前ベースで検索し、

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `apply` | action, qpos, reflex_strength | あり | action(numpy, shape=(n_act,))に反射を重ねて返す。reflex_strength<=0または首が |

### `taro_core/src/brain/brainstem/locus_coeruleus.py`

青斑核（Locus Coeruleus） — 脳全体の探索レベルを調整する中枢

**class `LocusCoeruleus`** 青斑核。最近の報酬を監視してノルエピネフリン（NE）量を決定する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `mature` | progress | なし | 発達の進行度（0〜1）を受け取り、探索の天井を下げる。 |
| `observe_reward` | reward | なし | 毎ターンの報酬を観測する。 |
| `release_ne` | — | あり | ノルエピネフリンを放出する。 |
| `get_ne_level` | — | あり | 現在のNEレベル(self.ne_level)をそのまま返す。引数は無く、内部状態を変更しない単純な取得用メソッドである。 |
| `observe_surprise` | z, rate, thresh | なし | 驚き（世界の予測器の物ごとの誤差のz値の最大）をNEへ足す（M7b-1、2026-09-09）。 |

### `taro_core/src/brain/brainstem/vergence.py`

輻輳反射：左右の周辺カメラ画像のズレ（両眼視差）から、目をどれだけ寄せるかを決める。

**class `VergenceReflex`** 左右周辺カメラの視差から目標輻輳角を求め、左右の眼球アクチュエータへ

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `reset` | — | なし | 前エピソードの状態を持ち越さない（e_orienting_v2.reset()と同じ流儀）。 |
| `measure_disparity_deg` | img_left, img_right | あり | 左右画像から視差角[度]を測るだけ（積分はしない）。単体テスト用に公開。 |
| `update` | img_left, img_right | なし | 左右の周辺カメラ画像からずれを測り、目標輻輳角(vergence_deg)を更新する。 |
| `apply` | action | あり | actionへ左右差を加算して返す（上書きではなく加算）。 |

### `taro_core/src/brain/brainstem/vor.py`

VOR（前庭動眼反射）：眼球を方策から切り離し、頭の動きを打ち消して視線を安定させる。

**class `VOR`** 頭部角速度を打ち消す向きに眼球を動かし、方策の眼球出力を無効化する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `reset` | — | なし | 半規管モジュール(self.canals)と耳石器モジュール(self.otolith)が設定されていれば、それぞれのreset()を呼んで内部状態を初期化する。引数・返り値は無い。 |
| `override` | action, model, data, dt, suppress | あり | 方策の行動から眼球ぶんを取り除き、VORの指令に差し替えて返す。 |
| `gaze_axis` | model, data, camera | あり | 今の視線方向（カメラのz軸）。揺れの測定用。 |

## 小脳

### `taro_core/src/brain/cerebellum_lobe/motor.py`

運動小脳（連続版, MotorCerebellum） — 運動の自動化（結晶化）

**class `MotorCerebellum`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `predict` | z | あり | 小脳のフィードフォワード行動（[-1,1]）。 |
| `gate` | z, policy_mean | あり | 馴染み度 → 自動化重み w（状態特異）。小脳が方針をよく再現できる状態ほど w 大。 |
| `observe` | e | なし | 馴染み度の基準（誤差の走行平均）を更新。覚醒中の学習1回につき1度だけ呼ぶ。 |
| `imitation_loss` | z, action | あり | 実際に行った運動を状態から真似る（反復で滑らかな平均＝結晶化パターンを獲得）。 |

### `taro_core/src/brain/cerebellum_lobe/speech.py`

発話小脳（Cerebellum） — 口の動きと音の対応を体験から学ぶ帳面（順モデル＋逆モデル）

**class `Cerebellum`** 発話小脳。口の動きと音の対応を体験から学ぶ。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `automatization` | place, manner, voicing, vowel | あり | この口の動きの自動化度（0=不慣れ 〜 1=ほぼ完全自動）。練習回数で飽和する。 |
| `learn_from_experience` | place, manner, voicing, vowel, produced_char | なし | 体験から学ぶ。「この口の動きでこの音が出た」を記録する。 |
| `predict_sound` | place, manner, voicing, vowel | あり | 順モデル：この口の動きをしたら何の音が出るか予測する。 |
| `lookup_motor` | target_char | あり | 逆モデル：この音を出すにはどう口を動かせばいいか検索する。 |
| `lookup_motor_sequence` | target_text | あり | 逆モデル：文字列全体に対して口の動きの列を検索する。 |
| `get_known_sounds` | — | あり | 今まで出したことのある音の一覧を返す。 |
| `get_experience_summary` | — | あり | 体験の統計を返す。 |

## 脊髄

### `taro_core/src/brain/spinal_cord/common_drive.py`

脊髄・脳幹相当の上位層：周期・振幅が揺らぐ振動子による共通駆動。

**class `WanderingOscillator`** 位相φ・周波数f・振幅Aを持つ、1本の揺らぐ振動子。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `step` | dt | あり | 周波数・振幅を、平均に戻ろうとするランダムウォーク（オルンシュタイン= |

**class `RhythmicCommonDriveGroup`** 関節グループごとに、独立成分と共通成分の2種類のWanderingOscillatorを持ち、

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `step` | dt | あり | {関節名: c_j(t)}（基準長へ加える前の、無次元の信号）を返す。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `joint_signal_to_L0_offset` | c_j, moment_1_j, moment_2_j, amp | あり | 関節jの無次元共通駆動信号c_j(t)を、拮抗筋2本(neg/pos)の基準長オフセットへ |

### `taro_core/src/brain/spinal_cord/cpg.py`

脊髄・中枢パターン発生器（CPG）相当のモジュール。

**class `ColoredNoiseGenerator`** 1/f^β ノイズを次元ごとに独立生成する。sample(beta) を毎tick呼ぶと (n_dim,) を返す。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `sample` | beta | あり | 1/f^βノイズを1tickぶん取り出して返す。引数betaを受け取り、内部バッファが未生成か使い切っていれば、その時点のbetaで新しいブロックを作り直してからshape=(n_dim,)の1点を返す。 |

**class `CPG`** 複数関節をまとめて駆動するCPG本体。独立ノイズ＋粗い相関（脚・腕）を合成して

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `sample` | beta, synergy, syn_w | あり | 引数betaで色付きノイズをshape=(n_act,)ぶん生成して返す。synergy=Trueの場合は、脚(左右逆位相)・右腕・左腕それぞれの共通シナジー信号を重みsyn_wで各関節の値へ混ぜてから返す。synergy=Falseなら混ぜずにそのまま返す。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `antagonist_map` | motor_cmd, co_activation | あり | 関節あたりの運動指令 motor_cmd(n_joint次元, [-1, 1]) を、拮抗筋2本ペアの |
| `is_antagonist_action` | action, n_actuator | あり | 行動配列が拮抗筋2本ペア形式か（＝長さが 関節数×2 か）を判定する。 |
| `write_joint_command` | action, joint_index, cmd, n_actuator, co_activation, additive | あり | 1つの関節への指令 cmd([-1, 1]) を、行動配列に**駆動方式に合った形で**書き込む。 |
| `read_joint_command` | action, joint_index, n_actuator, co_activation | あり | 行動配列から、1つの関節への指令（[-1, 1]・符号が向き）を取り出す。 |

### `taro_core/src/brain/spinal_cord/grasp_reflex.py`

把握反射（palmar grasp reflex）＝脊髄反射弓。

**class `GraspReflex`** model から手のひらのbody id・4指(親指除く)のアクチュエータidxを名前ベースで検索し、

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `apply` | action, sensor_outputs, reflex_strength | あり | action(numpy, shape=(n_act,))に反射を重ねて返す。reflex_strength<=0なら無変更 |

### `taro_core/src/brain/spinal_cord/postural_gate.py`

姿勢制御反射（層1）＝座位で体幹が前後に傾いたとき、傾きと逆側の筋だけを通す

**class `PosturalGate`** 座位での体幹前後の傾きに応じて、傾きと同じ側の体幹筋の指令を0にクリップする

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `proprioceptive_tilt_deg` | qpos | あり | hip_bend系qposの平均を度で返す。正=前屈（前傾）、負=後屈（後傾） |
| `touch_tilt_signal` | touch | あり | 骨盤(hip・lower_body)の触覚から、前後差(diff)と力の合計(mag)を返す。 |
| `tilt_direction` | qpos, touch | あり | 傾きの向きを +1（前傾）・-1（後傾）・0（不感帯内、判定しない）で返す。 |
| `compute` | qpos, touch | あり | 傾きの向き(+1/-1/0)を計算する（行動配列にはまだ適用しない）。 |
| `apply` | action, qpos, touch | あり | 傾きと同じ側の体幹筋の指令を0にクリップして返す（ゲート、加算ではない）。 |

### `taro_core/src/brain/spinal_cord/righting_damper.py`

立ち直り反射・層2＝角速度ダンパー（VCR風の連続的バイアス）。

**class `RightingDamper`** VCR（前庭頸反射）の周波数特性に近似した、角速度比例の立ち直りダンパー。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `compute` | head_angular_velocity, support_fraction | あり | bias（スカラー、正負の向きを持つ）を返す。副作用なし。 |
| `apply` | action, neg_indices, pos_indices, head_angular_velocity, support_fraction | あり | 行動配列(1次元、[0,1]の要素を持つ)に bias を加算し、クリップして返す。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `support_fraction_from_pinned_groups` | pinned_groups, all_groups | あり | constraint_summary(scene)["pinned_groups"] から support_fraction[0,1] を作る。 |

### `taro_core/src/brain/spinal_cord/stretch_reflex.py`

伸張反射（脊髄反射弓）。新しい駆動モジュール（伸張反射＋揺らぐ振動子の共通駆動）の下位層。

**class `StretchReflex`** 拮抗筋2本（neg=[:n]・pos=[n:]）それぞれについて、伸びている量・速さに応じた

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `init_reference` | muscle_lengths | なし | env.reset()直後のmuscle_lengthsを、そのままL0として採用する（案①4-2節・既定）。 |
| `set_reference` | new_L0 | なし | 基準長L0を書き換える（上位層＝共通駆動が毎tick呼ぶことでL0を動かす）。 |
| `compute` | muscle_lengths, muscle_velocities | あり | 反射の活性化 r（2n次元）を計算する（行動配列にはまだ加算しない）。 |
| `apply` | action, muscle_lengths, muscle_velocities | あり | 反射出力を行動配列(2n次元、[0,1])に加算し、クリップして返す（案①4-3節）。 |

## 皮質下核

### `taro_core/src/brain/subcortical_nuclei/basal_ganglia.py`

大脳基底核（Basal Ganglia） — 行動選択と強化学習

**class `TaroLearner`** 太郎の学習を管理する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `learn_perception` | input_tokens, body_state | あり | 知覚の学習：入力トークン列の次トークン予測誤差を最小化する。 |
| `compute_value_loss` | value_pred, reward | あり | クリティック（状態価値関数）の学習：TD誤差の二乗を最小化する。 |
| `learn_action` | log_probs, delta, credits | あり | 行動の学習：方策勾配（REINFORCE）。 |
| `update` | perception_loss, policy_loss, value_loss, satiety_loss | あり | 知覚・行動・価値・満腹予期の損失を合算し、重みを1回更新する。 |

### `taro_core/src/brain/subcortical_nuclei/speech_gate.py`

発話の門（SpeechGate） — 「言うかどうか」を反応の随伴で学ぶ大脳基底核の門。

**class `SpeechGate`** 言う価値 q_go と黙る価値 q_nogo を別々に持ち、その差で言う確率を決める門。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `prob` | — | あり | q_goとq_nogoの差をtemperatureで割ってシグモイド関数に通し、「言う」を選ぶ確率を計算して返す。引数は無い。 |
| `decide` | rng | あり | 言うかどうかを引く。rng は環境の np_random（再現性のため）。 |
| `resolve` | action, reward, sim_sec | なし | 結果（窓内に反応が来たか）で1回ぶん学習する。 |
| `state` | — | あり | q_go・q_nogo・temperature・logit（(q_go-q_nogo)/temperature）・baseline（dopamineがあればその値、無ければ0.0）・n_decisions・n_speak・n_rewarded・historyのコピーを持つ辞書を返す。引数は無い。 |
| `load_state` | st | なし | 辞書stからq_go・q_nogoを復元する（stに'q_go'があればそのまま使い、無く'logit'があれば logit*temperature をq_go・q_nogo=0として復元する）。dopamineがあれば'baseline'も復元し、n_decisions・n_speak・n_rewardedも復元する。historyは復元しない。戻り値は無い。 |

## 大脳辺縁系

### `taro_core/src/brain/limbic/hippocampus.py`

海馬（Hippocampus） — 運動性自己モデル用の短期経験バッファ＆睡眠リプレイ

**class `MotorHippocampus`** 運動性自己モデル用の海馬。覚醒中の感覚運動経験を蓄積し、睡眠移行時にリプレイする。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `record` | sv, prev_a, action, cf, clp, nlp, hidden | なし | 覚醒中の1ステップを記録する。全テンソルは detach 済みで渡すこと。 |
| `replay` | — | あり | 蓄積した全経験をリストで返す。 |
| `clear` | — | なし | 睡眠後にクリア（皮質への転送完了）。 |

## 欲求

### `taro_core/src/brain/drives/double_touch.py`

ダブルタッチ（頭への自己接触）検出器 ── 報酬に繋ぐための本能版。

**class `DoubleTouchDetector`** toucher（reach_arm_sideの手のひら）と touched_names（既定 頭のみ）の presence が

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `rebuild` | touch_map, model, touch | なし | 体を作り直したときに地図を差し替える（成長対応、2026-08-05追加）。 |
| `mouth_presence` | touch_flat, gain | あり | 口元の presence（0〜1）を返す。仕様3節。 |
| `detect` | touch_flat, toucher_name, touched_names | あり | toucher_name: 触れている側の部位名（例 "right_palm"） |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `mouth_point_mask` | touch_map, model, touch, x_frac, z_frac, head_group | あり | touch_map の "head" グループの点のうち、頭のローカル座標で |

### `taro_core/src/brain/drives/imitation_reward.py`

模倣衝動 — 親の発話に似るほど心地よい

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `compute_alignment_credit` | target_tokens, generated_tokens, vocab, vocal_tract | あり | 生成した各文字が目標語のどの位置にどれだけ一致していたかを求める。 |
| `compute_imitation_reward` | parent_tokens, taro_tokens, vocab, vocal_tract | あり | 親の発話と太郎の出力の類似度 → 内的報酬 [0, 1] |

### `taro_core/src/brain/drives/learning_progress.py`

内発的動機 — 太郎が「何を嬉しいと感じるか」。

**class `LearningProgress`** 学習進度＝「予測誤差が減っていること」を報酬にする（Oudeyerの好奇心）。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `update` | prediction_error | あり | 予測誤差を1つ受け取り、移動平均を更新して学習進度を返す。 |
| `relative_surprise` | — | あり | 「いつもより驚いたか」を [0,1] で返す（0.5＝平常の驚き）。 |
| `state` | — | あり | ログ用（pe_fast, pe_slow）。 |
| `resync` | — | なし | 速い/遅い移動平均を今の値どうしで揃え、progressを0からスタートさせる。 |

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `smoothness_cost` | action, prev_action | あり | 行動の**急変**にかかるコスト（CAPS）＝「さっきと違うことを急にするのは損」。 |
| `effort_cost` | action, muscle_weights | あり | 努力コスト（代謝コスト）＝「大きな力を出すのは損」。 |

## 神経修飾

### `taro_core/src/brain/neuromodulator/dopamine.py`

ドーパミン（報酬予測誤差） — 学習を駆動するごほうび信号

**class `Dopamine`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `compute_rpe` | reward | あり | 報酬予測誤差δを計算し、baselineを更新する。 |
| `get_baseline` | — | あり | 引数は無い。今のbaseline（予測される報酬の推定値、float）をそのまま返す。 |

## 目標喃語

### `taro_core/src/brain/goal_babbling/reach_goal_head.py`

目標専用の順モデル（ReachGoalHead）— 手先位置の目標表現（案C）用。

**class `ReachGoalHead`** [z（内部表現）, 行動] → 目標ベクトルgの変化量Δg、を予測する順モデル。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `forward` | z, action | あり | Δg（残差）を返す。呼び出し側が `current_g + Δg` にして使う。 |
| `predict` | z, action, current_g | あり | 残差予測：現在の目標g + Δ(z, 行動) = 次のgの予測。predict_proprioと同じ形。 |

### `taro_core/src/brain/goal_babbling/trajectory.py`

区分線形の軌道（GoalTrajectory）— Goal Babbling 段階1。

**class `GoalTrajectory`** 現在地から目標まで、区分線形（等分割）で近づく中間目標を作る。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `active` | — | あり | いま軌道の途中か（新しい目標を選ぶ必要が無いか）。 |
| `begin` | target | なし | 新しい目標を設定し、L ステップかけて近づく軌道を開始する。 |
| `waypoint` | current | あり | いま向かうべき中間目標を1つ返す（呼ぶたびに残りステップ数を1つ消費する）。 |
| `done` | — | あり | 軌道を使い切ったか（次に active() を呼ぶと新しい目標が要る）。 |
| `reset` | — | なし | 軌道を打ち切る（探索へ切り替わったときに呼ぶ）。 |

## 成熟

### `taro_core/src/brain/maturation/developmental_clock.py`

発達の時計（DevelopmentalClock） — ③発達年齢を「累積学習回数」で刻む

**class `DevelopmentalClock`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `tick` | n | なし | 覚醒中の学習を1回（n回）観測した。発達年齢を進める。 |
| `progress` | maturity | あり | 成熟までに要する学習回数 maturity に対する発達の進行度 [0, 1]。 |

### `taro_core/src/brain/maturation/developmental_schedule.py`

writhing→fidgety移行のスケジュール（2026-07-23設計）。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `schedule_w_mean` | age_months | あり | 皮質脊髄路の成熟スケジュール。0(writhing)→W_MEAN_CEILING(fidgety途上)。 |
| `schedule_syn_w` | age_months, baseline | あり | CPGの多筋協調スケジュール。baseline(今の既定)→SYN_W_CEILING。 |

## 原則

### `taro_core/src/brain/principles/homeostatic_scaling.py`

恒常的シナプススケーリング（Homeostatic Synaptic Scaling） —

**class `HomeostaticScaling`** 融合ベクトルの「時間をまたいだ活動量」を移動平均で追跡し、それが

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `homeostatic_loss` | fused_vec | あり | 今回の融合ベクトルが、最近の平均からどれだけ離れているか(二乗)を見る。 |
| `observe` | fused_vec | なし | 今回の値を観測し、"最近の平均"を更新する（次回の評価用）。 |

### `taro_core/src/brain/principles/precision_perception.py`

精度重み付き知覚（Precision-Weighted Perception） — 感覚と期待を精度で混ぜる

**class `PrecisionWeightedPerception`** 最近の実際の感覚の移動平均を「期待(belief)」として保持し、

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `perceive` | actual, precision | あり | actual: 実際の感覚（今回の融合ベクトルなど） |
| `observe` | actual | なし | 今回の実際の感覚で「期待」を更新する（次回の知覚評価用）。 |

### `taro_core/src/brain/principles/predictive_coding_latent.py`

確率的な潜在変数＋推論時のその場調整（PV-RNNに着想）

**class `PredictiveCodingLatent`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `infer` | hidden_before, hidden_after, current_sensory | あり | hidden_before: GRUに今回の感覚を通す"前"の状態 (prior用) |

## 脳の土台（1歩を回す）

### `taro_core/src/brain/basal_ganglia.py`

【転送のみ】このファイルの中身は subcortical_nuclei/basal_ganglia.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/brain_tick.py`

太郎の脳の「1周期」──毎tickで脳が行う処理そのもの（2026-09-13・段B-1）。

**class `BrainClock`** 太郎の時計（段B-2c・2026-09-13）。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `set` | — | なし | 太郎の時計を進める。キーワード引数 step は現在のステップ番号、dt は1ステップあたりの秒数。self.step を step に、self.sim_sec を step×dt に置き換える。返り値は無い。 |

**class `BrainTickState`** 太郎が1周期の中で自分に渡す値の置き場＝**脳の作業台**（段B-2a・2026-09-13）。

公開しているメソッドはありません。

**class `TaroBrainTick`** 毎tickで太郎の脳が行う処理。`run/trainer.py` の Trainer が継承する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `consolidate` | n_batches, bs | なし | 睡眠中の記憶定着：貯めた経験をバッチで再生し、予測経路を復習で固める。 |

### `taro_core/src/brain/cerebellum.py`

【転送のみ】このファイルの中身は cerebellum_lobe/speech.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/cerebellum_motor.py`

【転送のみ】このファイルの中身は cerebellum_lobe/motor.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/corticospinal.py`

【転送のみ】このファイルの中身は cerebral_cortex/frontal_lobe/corticospinal.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/developmental_clock.py`

【転送のみ】このファイルの中身は maturation/developmental_clock.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/developmental_schedule.py`

【転送のみ】このファイルの中身は maturation/developmental_schedule.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/dopamine.py`

【転送のみ】このファイルの中身は neuromodulator/dopamine.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/double_touch.py`

【転送のみ】このファイルの中身は drives/double_touch.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/hippocampus.py`

【転送のみ】このファイルの中身は limbic/hippocampus.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/homeostatic_scaling.py`

【転送のみ】このファイルの中身は principles/homeostatic_scaling.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/imitation_reward.py`

【転送のみ】このファイルの中身は drives/imitation_reward.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/learning_progress.py`

【転送のみ】このファイルの中身は drives/learning_progress.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/locus_coeruleus.py`

【転送のみ】このファイルの中身は brainstem/locus_coeruleus.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/motor_cortex.py`

【転送のみ】このファイルの中身は cerebral_cortex/frontal_lobe/motor_cortex.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/object_files.py`

【転送のみ】このファイルの中身は cerebral_cortex/parietal_lobe/intraparietal_sulcus.py へ移した（2026-09-04）。

公開している関数・クラスはありません。

### `taro_core/src/brain/precision_perception.py`

【転送のみ】このファイルの中身は principles/precision_perception.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/predictive_coding_latent.py`

【転送のみ】このファイルの中身は principles/predictive_coding_latent.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/sensorimotor_brain.py`

【転送のみ】このファイルの中身は cerebral_cortex/sensorimotor_brain.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/superior_colliculus.py`

【転送のみ】このファイルの中身は midbrain/superior_colliculus.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/taro_brain.py`

【転送のみ】このファイルの中身は cerebral_cortex/recurrent_core.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/taro_brain_motor.py`

【転送のみ】このファイルの中身は cerebral_cortex/recurrent_core_motor.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

### `taro_core/src/brain/vocal_tract.py`

【転送のみ】中身は body/vocal_tract.py へ移した（2026-08-30の整理）。

公開している関数・クラスはありません。

## からだ

### `taro_core/src/body/adenosine.py`

アデノシン — 眠さの原因物質

**class `Adenosine`** アデノシン濃度の管理。sleepiness のソース。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `tick_awake` | arousal | なし | 覚醒中の1秒分。脳活動（arousal）に比例して蓄積する。 |
| `tick_sleep` | — | なし | 睡眠中の1秒分。残量に比例して除去される（多いほど速く除去）。 |
| `get` | — | あり | 現在のアデノシン濃度（0〜1）。そのまま sleepiness として使う。 |

### `taro_core/src/body/blood_vessel.py`

血管（血中栄養素） — 胃と空腹感の間にあるバッファ

**class `BloodVessel`** 血中栄養素のシンプルなバッファ。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `receive_glucose` | absorption_amount | なし | 胃の消化吸収量を受け取る。血糖値が上がる。 |
| `tick` | — | なし | 1秒分の消費。体の細胞がゆっくり血糖を使う。 |
| `get_hunger` | — | あり | 空腹度（0〜1）。血糖値が低いほど1に近づく。 |
| `get_blood_glucose` | — | あり | 血糖値（0〜1）。 |

### `taro_core/src/body/development.py`

月齢(age_months) → 発達パラメータ の対応表を一本化する（2026-08-23新設）。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `vision_acuity_cpd` | age_months | あり | 月齢 → 視力[cycles/度]（Mayer et al. 1995・実測テーブルの線形補間）。 |
| `vision_required_px` | age_months, fovy_deg | あり | 視力フィルタが効く上限まで画素を用意するのに必要な解像度[px]（F1-7と同じ式）。 |
| `vocal_tract_stage_for_age` | age_months | あり | 月齢 → 声道の成熟ステージ(0〜3)。境界の根拠の強さは上のコメント参照。 |
| `develop` | age_months, vision_fovy_deg, fovea_fovy_deg | あり | 月齢1つから、今回配線した各パラメータをまとめて返す（表作成・検算用）。 |

### `taro_core/src/body/homeostasis.py`

恒常性（Homeostasis） — 体の状態を快適値に保とうとする本能

**class `Homeostasis`** 恒常性の本能。arousalの変化から報酬を計算する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `compute_reward` | current_arousal | あり | arousalが下がった分だけ正の報酬。上がった分だけ負の報酬。 |

### `taro_core/src/body/infant_body.py`

新生児の体型（プロポーション）— 太郎の身体そのものの定義。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `taper_weight` | age, until_mo | あり | 新生児向け補正の重み。0ヶ月で1.0、until_mo で0.0（＝素のMIMo）へ線形に薄れる。 |
| `elongate_head` | spec, ratio | あり | 頭を体軸方向に伸ばして球→楕円にする（頭囲は変えない）。 |
| `body_scale_custom` | age, scales, verbose | あり | 体の部位ごとに geom の寸法を係数倍する custom 辞書を作る。 |
| `build_body_kwargs` | age, scales, head_elongation | あり | 環境のコンストラクタに渡す身体の設定を作る（モデル構築前の分）。 |
| `apply_head_mass` | model, age, fraction, verbose | なし | 頭の**密度**を上げて、全体重に占める頭の割合を人間の新生児に合わせる。 |
| `apply_distal_mass` | model, scale, verbose | なし | 手・足（末端）の**質量だけ**を倍率で変える（サイズは変えない）＝感度分析用。 |
| `actuator_strength` | model, aid, actuation_model | あり | アクチュエータの「筋力」を返す。筋肉モデルなら fmax、それ以外は gear。 |
| `scale_actuator_strength` | model, aid, factor, actuation_model | あり | アクチュエータの筋力を factor 倍する。筋肉モデルなら fmax、それ以外は gear。 |
| `apply_physiological_flexion` | model, age, stiffness, verbose, mode, data | あり | 新生児の生理的屈曲を実装する。 |
| `apply_runtime_corrections` | model, data, age, neck, limbs, head_mass, limb_scale, distal_mass, flexion, flexion_stiffness, actuation_model, tone, tone_stiffness, neck_tone, neck_tone_stiffness, neck_tone_target, joint_compliance, joint_compliance_groups, joint_compliance_joints, joint_compliance_params | なし | モデル構築後に上書きする補正（筋力＝アクチュエータのgear）。 |
| `center_eyes` | model, data, verbose, vertical_deg | あり | 眼球の関節角を基準位置に戻す。リセットのたびに呼ぶ。 |
| `apply_eye_centering_spring` | model, data, vertical_deg, stiffness, enabled, verbose | あり | 眼球6関節に、受動的な中央復帰バネを入れる。既定OFF（`EYE_CENTERING`）。 |
| `apply_eye_muscle_scale` | model, scale, actuation_model, verbose | あり | 眼球6関節の筋アクチュエータの出力を scale 倍する。既定1.0＝無補正。 |
| `apply_neck_tone` | model, age, stiffness, target, joints, verbose, data | あり | 首の筋緊張＝頭が重力で倒れきらないように支える弱いバネ。 |

### `taro_core/src/body/infant_limbs.py`

四肢の「発達の向きの逆転」を解消する補正。注意恣意性は小さいが逸脱リスト対象。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `actuator_ratios` | model, data, actuation_model | あり | 各アクチュエータの「筋力 ÷ その関節から先の重力モーメント」を返す。 |
| `measure_reference_ratios` | age_ref | あり | 参照月齢の素の体を作って比を測る。 |
| `apply_limb_inversion_fix` | model, data, age, reference_age, verbose, scale, actuation_model | あり | 四肢のgearを下げ、age の比が reference_age の比を超えないようにする。 |
| `limb_tone_joints` | model, groups | あり | 筋緊張を効かせる関節の名前（":" の後ろ）を返す。 |
| `limb_tone_joints_by_group` | model, groups | あり | (関節名, 部位名) の一覧を返す。部位ごとに強さを変えるために使う。 |
| `gravity_moment` | model, data, joint_name, worst_case | あり | その関節から先（末端側）にぶら下がる重力モーメント [N·m] を測る。 |
| `tone_from_gravity` | model, data, joint_name, hold_deg | あり | 「重力に対して hold_deg 度のずれで釣り合う」剛性 [N·m/rad] を返す。 |
| `disable_limb_tone_spring` | model, groups | あり | apply_limb_tone が四肢に効かせた「継続的なバネ」役割(jnt_stiffness)を無効化する。 |
| `apply_limb_tone` | model, data, age, profile, target, stiffness, hold_deg, groups, until_mo, clamp_to_range, force_qpos, verbose | あり | 四肢の筋緊張＝脱力しても腕が体の前に保たれる弱いバネ。 |

### `taro_core/src/body/infant_neck.py`

新生児の「首がすわっていない」を再現する補正。注意**恣意的な逸脱**（下記ラベリング参照）。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `head_gravity_torque` | model, data | あり | 今の姿勢での「頭の重力モーメント」[N·m]＝首が支えるべき負荷。 |
| `lift_ratio` | model, data, actuation_model | あり | 持ち上げ能力比＝首の筋力 ÷ 頭の重力モーメント。<1 なら頭を持ち上げられない。 |
| `target_ratio_for_age` | age, birth_ratio, settle_age, natural_ratio, control_ratio, mature_age | あり | 月齢に応じた目標比。**崖を作らずに**MIMoの素の値まで繋ぐ。 |
| `apply_newborn_neck` | model, data, age, birth_ratio, settle_age, verbose, actuation_model | あり | 首(tilt)のgearを、月齢に応じた目標比になるよう補正する。 |

### `taro_core/src/body/internal_state.py`

内受容感覚 — 身体の信号を統合して脳に渡す

**class `InternalState`** 太郎の内受容感覚。身体の信号を統合する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `on_feed` | amount | なし | 授乳された分の一部が、時間差で便として溜まる（B2-19）。 |
| `update_from_body` | stomach, blood_vessel, lungs | なし | 臓器から状態を受け取る。blood_vessel があれば血糖値から空腹を計算。 |
| `get_arousal` | — | あり | つらさ（0〜1）。寝ている間・うとうと中は0。 |
| `get_arousal_delta` | prev_arousal | あり | つらさの変化量。下がったら正（ほっとした）。 |
| `apply_care` | care_type | なし | 世話を受ける。泣いていたら泣き止む。 |
| `tick` | adenosine, elapsed_seconds | なし | 1秒分の時間経過。 |
| `can_babble` | — | あり | 喃語が出せる状態かどうか。 |
| `is_sleeping` | — | あり | 太郎が今、睡眠状態にあるかどうかを返す。引数は無く、返り値は sleeping の真偽値。 |
| `is_drowsy` | — | あり | 太郎が今、うとうとした状態（寝入りかけ）にあるかどうかを返す。引数は無く、返り値は drowsy の真偽値。 |
| `is_crying` | — | あり | 太郎が今、泣いている状態かどうかを返す。引数は無く、返り値は crying の真偽値。 |
| `get_state_vector` | — | あり | 脳に渡す状態の数値リスト。 |

### `taro_core/src/body/joint_compliance.py`

関節可動域の限界へ近づく抵抗を滑らかにする — MuJoCoのjnt_solimpを実行時に書き換える。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `apply_joint_compliance` | model, groups, joints, params, verbose | あり | 関節可動域の限界へ近づく抵抗を滑らかにする（既定OFF・呼び出し側でON/OFFを制御）。 |

### `taro_core/src/body/smooth_actuation.py`

筋活性化ダイナミクス（一次遅れ）を既存のトルクモータに足す実行層（taro-C5 実験①）。

**class `SmoothTorqueModel`** SpringDamperModel（瞬時トルク）に一次遅れの活性化ダイナミクスを足した版。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `action` | action | なし | 脳の指令を「目標トルク」として受け取る（＝ここでは即座に印加しない）。 |
| `substep_update` | — | なし | 物理ステップごとに、印加トルクを目標へ一次遅れで近づける（活性化ダイナミクス）。 |
| `reset` | — | なし | 印加値・目標値を 0（無張力）に戻す。 |

### `taro_core/src/body/stomach.py`

胃 — 食べ物を受け取り、消化で空になる容器

**class `Stomach`** 太郎の胃。容器として食べ物を受け取り、消化で中身が減る。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `tick` | — | なし | 1秒分の消化を進める。 |
| `start_feeding` | amount | なし | 授乳を開始する。一瞬ではなく、feeding_duration秒かけて少しずつ胃に入る。 |
| `stop_feeding` | — | なし | 授乳を中断する（赤ちゃんが拒否した等）。 |
| `is_feeding` | — | あり | 今、授乳中かどうか。 |
| `get_hunger` | — | あり | 空腹度（0〜1）。胃が空くほど1に近づく。 |
| `get_last_absorption` | — | あり | 直前の1秒で吸収された量（グルコース）を返す。引数は無い。 |
| `grow` | — | なし | 身体の成熟で胃の容量が増える。 |

### `taro_core/src/body/vocal_tract.py`

声道シミュレータ — 太郎の「口」

**class `VocalTract`** 太郎の口。4つのパラメータから文字を作る。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `speak` | place, manner, voicing, vowel | あり | 口のパラメータ → 文字。 |
| `hear` | char | あり | 文字 → 口のパラメータ。 |
| `get_all_chars` | — | あり | 声道で発声可能な全文字を返す。 |
| `param_distance` | char_a, char_b | あり | 2つの文字の調音的な距離（0〜4）。 |
| `update_stage` | sim_seconds, stage1_time, stage2_time, stage3_time, decouple_time | なし | sim時間に応じて成熟ステージと連動/独立を更新する。 |
| `get_allowed` | — | あり | 現在のステージで使えるパラメータ値を返す。 |
| `get_manner_for_place` | place | あり | 連動モード時：調音点から調音法を自動決定する。 |
| `force_decouple` | — | なし | アブレーション用：最初から独立制御にする。 |
| `is_coupled` | — | あり | 調音点と調音法が連動した状態かどうかを返す。引数は無く、返り値は coupled の真偽値。 |
| `clamp_to_stage` | place, manner, voicing, vowel | あり | ロックされたパラメータを許可値に制限する（身体的制約）。 |
| `num_params` | — | あり | 各パラメータの選択肢数をタプルで返す。 |

## 外枠

### `taro_core/src/wrapper/hybrid_env.py`

HybridEnv — MIMo（外側の身体）と太郎の内臓（内側の身体）を束ねるラッパー層。

**class `HybridEnv`** MIMo環境を包み、太郎の内受容感覚と恒常性報酬を合成するラッパー。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `advance_body_one_second` | — | あり | 内臓を1秒分だけ進める。Taro core_b.tick_body() の毎秒処理を忠実に再現。 |
| `reset` | **kwargs | あり | ラップしているenvをresetし、口反射オブジェクトと内臓の状態を作り直してから、内受容感覚を足した観測とinfoを返す。 |
| `step` | action | あり | 口反射オブジェクトがあればactionを書き換えてからenvを1ステップ進める。ステップ数がsteps_per_body_secondに達するたびに内臓を1秒分進めて恒常性報酬を加算し、内受容感覚を足した観測・合計報酬・terminated・truncated・infoを返す。 |

## 測る道具（実験ファイルで付け外しする）

### `run/plugins/base.py`

プラグインの規約。測る道具・見る道具はすべてこの形にする。

**class `Plugin`** 測る／見る道具の共通の形。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | 環境ができた直後に1回。センサの位置を覚えるなど。 |
| `on_step` | ctx | なし | 毎ステップ。重い処理は書かない（学習が遅くなる）。 |
| `on_step_late` | ctx | なし | 毎ステップ、報酬(rew)・RPE(rpe)が確定した"後"に呼ばれる（2026-08-03新設）。 |
| `on_body_change` | ctx | なし | 体を作り直した直後（体を育てる実験）。model/data が別物になっている。 |
| `on_checkpoint` | ctx | なし | 記録の区切りごと。評価など重い測定はここで。 |
| `metrics` | ctx | あり | いまの値を辞書で返す（記録の区切りごとに呼ばれる）。 |
| `report` | ctx | あり | 最後に1回。まとめの辞書を返す（None でもよい）。 |
| `line` | ctx | あり | 1行のログに混ぜる短い文字列（None なら混ぜない）。 |

## 測る道具 / 共通

### `run/plugins/common/babble_probe.py`

測る道具：喃語モード（F2-1）の発話を記録する。

**class `BabbleProbe`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/block_progress_probe.py`

感覚ブロックごとのprogress（学習進度）を可視化するための、読むだけのプローブ。

**class `BlockProgressProbe`** t.block_pe()が1個に混ぜる直前の、ブロックごとの誤差・progressを記録する。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/body_geoms.py`

自己接触（太郎の体どうし）と外界接触（柵・おもちゃなど）を区別するための共有部品。

**クラスに属さない関数**

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `geoms_of_body_id` | model, root_bid, include_children | あり | root_bid のbodyに属するgeom idの集合を返す。 |
| `geoms_of_body` | model, root_body_name, include_children | あり | root_body_name（文字列）からbody idを引いて geoms_of_body_id を呼ぶだけの薄いラッパー。 |
| `geoms_by_name_prefix` | model, prefixes, include_children | あり | 名前の前方一致でgeomを集める。geom自身の名前とbody名の両方を見る。 |
| `contact_pairs_between` | data, geoms_a, geoms_b | あり | このtickで geoms_a側 と geoms_b側 が実際に接触しているペアの一覧を返す。 |

### `run/plugins/common/contact_reward.py`

指定した部位どうしの自己接触の瞬間とそれ以外の瞬間で、太郎の脳がすでに持っている

**class `ContactReward`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_body_change` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `on_step_late` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `line` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/dashboard.py`

学習の様子の絵（HTML）を、記録の区切りごとに自動で作り直す。

**class `Dashboard`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_checkpoint` | ctx | なし | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/double_touch.py`

ダブルタッチ（自己接触の一致）を検出する ── 触れた側・触れられた側の

**class `DoubleTouch`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_body_change` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `line` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/gaze_probe.py`

測る道具：太郎の視線が的（おもちゃ）にどれだけ・どう留まるかを記録する。

**class `GazeProbe`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/hand_in_view.py`

手が視界に入っている割合を測る。

**class `HandInView`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `line` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/model_snapshots.py`

checkpointごとに脳の途中保存を残す（F2-33②・2026-09-01新設）。

**class `ModelSnapshots`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_checkpoint` | ctx | なし | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/movement_units.py`

動きの区切り（movement units）を数える ── 手の動きが滑らかかどうかを測る。

**class `MovementUnits`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_body_change` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `feed` | v | なし | 速度[m/s]を1つ受け取って数える。 |
| `metrics` | ctx | あり | **（説明文なし）** |
| `line` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/object_files.py`

測る道具：物体ファイル・注意の見た目をCSV・PNGへ書き出す。

**class `ObjectFiles`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | あり | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `line` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/plateau_stop.py`

小脳の予測誤差(cereb_err)が頭打ちに達したら、学習を打ち切る道具。

**class `PlateauStop`** cereb_err が頭打ちになったら、または上限ステップに達したら学習を止める。

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_checkpoint` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `line` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/posture_probe.py`

頭の高さと報酬を記録する。

**class `PostureProbe`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step_late` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `line` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/probe_defaults.py`

測定器（probe類）の既定値を1箇所にまとめたもの。副作用なし・定数のみ。

公開している関数・クラスはありません。

### `run/plugins/common/produce_snapshot.py`

測る道具：太郎が語を言った瞬間に、その目に何が写っていたかを残す。

**class `ProduceSnapshot`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/reach_success.py`

自己接触が実際に起きているか（頭・胸・反対の手に触れたか）を直接測る。

**class `ReachSuccess`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_body_change` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `line` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/scene.py`

【転送のみ】中身は `run/world/scene.py` へ移した（2026-09-13・段D）。

公開している関数・クラスはありません。

### `run/plugins/common/self_model.py`

自己モデルの質を測る。太郎の脳が要るので、run.type=train のときだけ使える。

**class `SelfModel`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_checkpoint` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `line` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/toy_in_view.py`

おもちゃ（test_object1）が視界に入っている割合を測る。

**class `ToyInView`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `line` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/toy_touch.py`

物体（おもちゃ）に手が触れたかを測る。

**class `ToyTouch`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `on_body_change` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `line` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/trace.py`

毎ステップの内部の値の「指紋」を残す。同じシードで結果がばらつく原因を追うため。

**class `Trace`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/vergence_probe.py`

測る道具：輻輳反射がいま何度を目標にし、実際に両目が何度ずれているかを記録する。

**class `VergenceProbe`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/view_video.py`

見る道具：太郎の視界（左目の実入力）と第三者視点を、走行中にそのまま動画にする（2026-09-03）。

**class `ViewVideo`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/word_learning.py`

測る道具：親のfollow-in labeling（F1-3）で語彙がどれだけ育っているかを見る。

**class `WordLearning`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/word_production.py`

測る道具：太郎自身の産出（F2「見た物の名前を言う」）を記録する。

**class `WordProduction`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/world_predictor_log.py`

測る道具：世界の予測器（M7a／M7b-1）の誤差をCSVに書く。

**class `WorldPredictorLog`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step_late` | ctx | なし | **（説明文なし）** |
| `metrics` | ctx | あり | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

### `run/plugins/common/world_predictor_record.py`

記録する道具：世界の予測器（ポート型）が毎tick受け取った入力を1本のpklに残す。

**class `WorldPredictorRecord`** 

| 関数 | 受け取る | 返す | 何をするか |
|---|---|---|---|
| `setup` | ctx | なし | **（説明文なし）** |
| `on_step_late` | ctx | なし | **（説明文なし）** |
| `report` | ctx | あり | **（説明文なし）** |

