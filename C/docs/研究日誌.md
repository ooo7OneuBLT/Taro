# MIMo統合 研究日誌

太郎（`B/docs/研究日誌.md`）と同じ形式。日付ごとに何をして何が分かったかを記録する。

\---

## 2026-07-10 Phase 0 着手：環境構築

* 計画書に基づき、プロジェクトの置き場所を `C:\\claude\\AI\\unificationMIMo` に確定（当初案の`Taro-MIMo`から著者の判断で変更）。
* `git init` で新規リポジトリ化。`.gitignore` に `MIMo/`（他者OSSのclone）・`.venv/`・`\_\_pycache\_\_/` を追加。
* MIMo を `MIMo/` サブフォルダに `git clone --branch v2.0.0 --depth 1` で取得。
* Python 3.12.10 の venv (`.venv/`) を作成。
* OS依存コード（`sys.platform`/`EGL`/`OSMesa`/`DISPLAY`等）の grep を再実施 → v2.0.0タグでも該当なし（クリーン、Linux固有の隠れ処理は無い）。
* **躓き①の実体を特定**：`MIMo/mimoEnv/envs/mimo\_env.py` の `\_initialize\_simulation()`（380〜401行目）が `super().\_initialize\_simulation()` を呼ぶのみで `return self.model, self.data` が無い。gymnasium 1.2.3 の `MujocoEnv.\_\_init\_\_` は `self.model, self.data = self.\_initialize\_simulation()` と戻り値を要求するため、**バージョンの偶然ではなく、MIMo v2.0.0の実コードに存在する互換性バグ**と確定。→ 修正方針：関数末尾に `return self.model, self.data` を追加するローカルパッチで回避見込み。
* 依存パッケージ（torch, mujoco, gymnasium\[mujoco], stable-baselines3ほか）を `pip install -r MIMo/requirements.txt` でインストール完了（全55パッケージ成功）。

## 2026-07-10 Phase 0（続き）：詰まり2件と解消、起動成功

* **詰まりA**：`pip install -e MIMo/` が `ModuleNotFoundError: No module named 'pkg\_resources'` で失敗。
原因：MIMoの`setup.py`が`pkg\_resources`を直接import。pipのビルド分離が新しい`setuptools`（pkg\_resources非同梱）を隔離環境に取得してしまうため。
対処：`pip install "setuptools<81"` に加え、`pip install -e MIMo/ --no-build-isolation` でvenv側の（ダウングレード済みの）setuptoolsを使わせて解消。
* **詰まりB（＝躓き①の正体、想定と一部違った）**：単に`return`が無いだけでなく、`super().\_initialize\_simulation()`の戻り値を**受け取ってすらいない**ため、直後の`self.dt`参照（`self.model`に依存）が`AttributeError`になっていた。
対処：`self.model, self.data = super().\_initialize\_simulation()`と明示的に代入するよう1行修正（`MIMo/mimoEnv/envs/mimo\_env.py` `\_initialize\_simulation()`内、コメント付きでローカルパッチとして明記）。
* **修正後、`MIMoBenchV2-v0`の`reset()`が成功**。

### Phase 1：観測・行動空間の実測値

```
observation\_space:
  observation  : shape=(621,)         float64   ← 固有感覚（関節角度など）
  touch        : shape=(12822,)       float32   ← 全身の触覚センサー格子
  eye\_left     : shape=(256, 256, 3)  uint8     ← 視覚（左目、ステレオ）
  eye\_right    : shape=(256, 256, 3)  uint8     ← 視覚（右目、ステレオ）
  vestibular   : shape=(6,)           float64   ← 前庭覚（加速度3＋角速度3、推定）

action\_space:
  Box(90,) float32   ← 全身90関節、各アクチュエータごとに非対称な可動域(min/max)
```

### 詰まり2（想定より良い結果）：視覚のOpenGL問題は発生せず

引き継ぎメモの「確認2」で最大の懸念だった視覚レンダリングは、**エラーなく一発で成功**。RTX 3060 Tiが正しく使われている見込み（隔離Linuxサンドボックスでは`OpenGLプラットフォームライブラリが無い`で失敗していた箇所）。

### Phase 2：速度計測

```
200ステップ（毎ステップ、ステレオ視覚レンダリング込み）を実測：
  0.981秒 → 4.91 ms/step → 約203.8 steps/sec
  MIMoの1エピソード(max\_episode\_steps=6000)を素朴に全部回すと約29.4秒
```

事前の懸念（「MuJoCo＋カメラ描画は太郎の抽象ループより桁違いに重い」）に反し、**GPU（RTX3060Ti）のおかげで実測は十分高速**。視覚込みで1秒に約200ステップ動く。これは太郎の現在の"高速シミュレーション"路線を維持できる可能性が高いことを示す、具体的な良い材料。

### Phase 0〜2 総括

* 想定していた2つの詰まり（gymnasium戻り値ズレ／視覚OpenGL）は**両方とも実機で再現・解消**できた
* 計算コストへの懸念は**実測により大幅に緩和**（懸念していたほど重くない）
* 残る最大の論点は技術的な話ではなく**哲学的な話**（視覚エンコーダCNNを生得の前処理と見なせるか）→ 引き続き検討課題として保留

## Phase 3：5感覚モジュールの精読とCG表示の確認

各感覚モジュール（vision/touch/proprioception/vestibular/actuation）のソースを読み、実装内容を確認（詳細は[開発計画.md](開発計画.md)）。特に視覚は年齢依存のacuityぼかしと中心窩フォビエーションが既にかかっている点、触覚は4,274点の力ベクトルセンサーである点、運動出力はトルク/位置/筋肉の3モデルから選べる点を確認した。

あわせてMIMo付属の`showroom.py`相当の処理で、実際に3Dウィンドウ表示を試行。

* **詰まり**：`render\_mode='human'`で`GLFWError: The GLFW library is not initialized`が発生。原因は、視覚センサー用のオフスクリーン描画（rgb\_array）が`reset()`時に先に初期化され、その後のウィンドウ描画（human）の初期化と衝突するため。
* **解消**：`reset()`より先に`env.unwrapped.render()`を一度呼び、ウィンドウ描画を先に初期化することで回避。以降は問題なく表示・動作。
* **副次的な発見（速度）※初回計測は無効**：初回は実時間28.5秒で405ステップ・倍率0.14倍という結果だったが、計測中にウィンドウ上でSpaceキー（MuJoCoビューアの一時停止キー）を押しており、`render()`内部の一時停止ループ（`while self.\_paused: ...`）が実時間の計測を止めずにブロックしていたため、数値が不当に低く出ていたと判明（ソース: `gymnasium/envs/mujoco/mujoco\_rendering.py`）。キー入力なしで再計測した結果、**実時間8.01秒で716ステップ、シミュレーション内経過7.16秒＝実時間の0.89倍**（ほぼ等速）。画面表示ありでも大きな速度低下は無いことを確認。

### Phase 3 総括

* 統合構想メモ（開発計画.md）・検討中の設計論点の整理は確定済み
* 5感覚モジュールの実装内容を把握し、開発計画.mdに反映
* CG表示の動作確認・詰まりの解消も完了。表示速度は実時間の約0.89倍とほぼ等速と確認（初回計測の0.14倍はビューアの一時停止操作による誤測定と判明、訂正済み）
* Phase 3完了。次はPhase 4以降（実装）だが、視覚エンコーダCNN等の設計論点は方針確定後に着手する

\---

## Phase 4 手順①：太郎の内臓6ファイルをコピー

Phase 4（ハイブリッド環境の構築）に着手。MIMoは無改変のまま、太郎の内受容感覚をラッパー層に持ち込む方針（詳細な設計判断は私的な設計メモ＝Git管理外を参照）。

* 事前に依存関係を確認：`internal\_state.py`/`stomach.py`/`blood\_vessel.py`/`adenosine.py`/`homeostasis.py`は他のTaroモジュールに一切依存せず（標準ライブラリの`random`のみ）、`insula.py`は`torch`のみに依存。**Taro内の他コードへの依存がゼロ**と確認した上でコピーを実施
* `bridge/taro\_modules/`を新設し、6ファイルをTaro（commit `3b976fc`）からコピー。各ファイル先頭にコピー元コミットを明記（追跡用）
* 単体動作確認：`InternalState`・`Stomach`・`BloodVessel`・`Adenosine`・`Insula`・`Homeostasis`の6クラス全てが、MIMoに一切依存せず単体でimport・インスタンス化できることを確認。`Insula`のforward（状態ベクトル→64次元）、`Homeostasis`の報酬計算も動作確認済み

### 次にやること

* Phase 4手順②③：MIMoを包む`HybridEnv`ラッパーを新規実装（MIMoとの接続・内臓との接続を同時に組む）
* 時間の刻み対策（MIMo 100ステップ＝内臓1秒）を組み込む

\---

## Phase 4 手順②③：HybridEnvラッパーを実装・検証

`bridge/hybrid\_env.py`（`HybridEnv(gymnasium.Wrapper)`）を新規実装。MIMoを無改変のまま包み、太郎の内臓を同居させて1つの身体として動かす層。

**実装した中身**

* `advance\_body\_one\_second()`：内臓の毎秒処理を、Taro `core\_b.py` `tick\_body()` の順序どおり忠実に再現（胃.tick → 血糖.receive\_glucose(吸収量×効率3.0) → 血糖.tick → 内受容.update\_from\_body → 内受容.tick → 胃.grow）。肺は発声/聴覚サブシステムの部品なのでPhase 4では除外。
* **時間の刻み対策**：`step()`のたびにカウンタを+1し、`STEPS\_PER\_BODY\_SECOND`（=100）に達したら内臓を1秒進める。太郎側の定数は無改変。
* **観測の合成**：MIMoの観測dictに `interoception`（\[hunger, sleepiness, discomfort, arousal]）を追加。観測空間にもこのキーを登録。
* **報酬の合成**：`homeostasis.compute\_reward()`（つらさが下がったら正）をMIMoの報酬に加算。
* **簡易授乳（★スタンドイン）**：空腹>0.6で授乳中でなければ自動授乳。本物の親シミュレータは後で差し替え。
* **設計判断**：`insula`（内受容→脳への変換）は「脳側」の部品なのでラッパーでは動かさず、生の内受容ベクトルを観測に足すだけにした。insula変換は脳と繋ぐPhase 5で行う。

**検証（`bridge/test\_hybrid\_env.py`、脳の代わりにランダム行動）**

* テストA（統合）：MIMoを通して300ステップ完走。クラッシュなし。観測に`interoception`が入り、hungerが[0,1]に収まることを確認。
* テストB（身体ダイナミクス）：内臓は「1秒=100 MIMoステップ」と遅い（人間の空腹は数時間スケール）ため、MIMoを数百万ステップ回すのは非現実的。そこで`advance\_body\_one\_second()`を直接3万回まわして高速検証。結果：**hungerが0.0〜0.604で上下し、授乳が1800秒発生し、恒常性報酬が正になるイベントが459回**。空腹が約6〜7時間かけて上がり閾値0.6で授乳が入り0に戻る、という実際の乳児のリズムを再現できた。

**Phase 4 総括**

* 図の下半分（ラッパー＋MIMo＋太郎内臓）が完成。**MIMo（外の身体）＋太郎の内臓（内の身体）が、1つの身体として動作する**ことを実機で確認。
* 脳はまだ繋いでいない（ランダム行動で身体側だけを検証）。
* CNNの扱いについて設計判断を確定（詳細は開発計画.md）。既存AI研究として正直にラベルして進める方針。

---

## Phase 5（前半）：固有感覚・前庭覚・触覚の通訳層を実装

`bridge/sensory_encoders.py`を新規実装。`insula.py`（内受容感覚の通訳、太郎から流用）と同じ考え方で、固有感覚・前庭覚・触覚の3つに専用の変換層を追加。

**実装した中身**
- `ProprioceptionEncoder`：固有感覚（621次元）→ Linear層1枚 → 64次元
- `VestibularEncoder`：前庭覚（6次元）→ Linear層1枚 → 64次元
- `TouchEncoder`：触覚（12,822次元、他より桁違いに大きい）→ Linear→ReLU→Linear の2段階で圧縮 → 64次元

いずれも画像のような空間構造を持たない「数値の並び」なので、視覚（CNN、後続で実装）とは違い、insulaと同じ単純な変換で十分と判断。

**検証**：単体テスト（ダミー入力）、および`HybridEnv`経由の実際のMIMo観測（621/6/12822次元）の両方で、全3エンコーダが正しく64次元へ変換できることを確認。

**次にやること**：視覚（CNN）の通訳層を実装し、5つの通訳の出力を統合して太郎のcortexに繋ぐ。

---

## Phase 5（後半）：視覚エンコーダ（CNN）を実装

`bridge/vision_encoder.py`を新規実装。ステレオ視覚（eye_left・eye_right、各256×256×3）を、他の感覚と同じ64次元に圧縮するCNN。左右は同じ重みのCNNで処理（＝同じ視覚野を両目で共有、という想定）し、統合して1本のベクトルにする。

**詰まりと解消**：MIMoの画像（OpenGL由来）は上下反転のビューとして負のstrideを持っており、`torch.as_tensor()`にそのまま渡すと`ValueError: negative strides are not currently supported`で失敗した。`numpy配列を.copy()`してメモリを実体化してから変換することで解消。

**検証**：ダミー画像で1回の変換に約49ms。パラメータ数は約213万。**この49msは今後の速度最適化の課題として残る**（Phase 2で測った204 steps/secという速度は視覚エンコーダのCNN計算を含んでいないため、実際に学習ループに組み込むと相応に遅くなる見込み）。

---

## Phase 6：感覚運動の脳（試作）を実装

**重要な設計判断**：太郎の本体の脳（`cortex.py`のTaroBrain）は音声・発話計画・語彙と密結合した複雑な設計になっており、直接拡張するのはリスクが大きいと判断。そこで、Phase 6/7の「配線が正しく動くか」の検証専用に、**別の・小さな試作の脳（`bridge/sensorimotor_brain.py`のSensorimotorBrain）を新規に作った。TaroBrain本体には一切触れていない**。本物のTaroBrainとの統合（音声・発話・満腹予期と同じ脳の中で身体運動も扱えるようにする）は、この試作で配線を検証した後の、より大きな後続作業と位置づける。

**設計**：GRU（TaroBrainと同じ発想の1層再帰ネット）が、5感覚を束ねた320次元ベクトルを受け取り、①次に来る感覚を予測する（感覚運動予測ループの中核）②MIMoの90関節への運動命令を出す、の2つを行う。

根拠ラベル：GRU自体はTaroBrainと同じ【既存AI研究】。「次の感覚を予測し外れたら修正する」原理は【人間模倣】（予測処理、DIVA）だが、具体的な実装（320次元ベクトルのMSE予測）は簡略化した近似で⚠️今後の検証・改善の余地がある試作段階。

---

## Phase 7：結合テスト

`bridge/test_phase7_integration.py`で、環境（HybridEnv：MIMo+内臓）とエージェント（試作の感覚運動の脳）を実際に結合。

**結果**
- **200/200ステップ完走。クラッシュ・NaNなし**
- **勾配の流れを検証**：感覚エンコーダ（内受容・固有感覚・前庭覚・触覚・視覚）＋予測headには、全パラメータに正しく有限な勾配が流れることを確認
- **重要な発見（バグではなく設計上の性質）**：運動head（`motor_head`）だけは勾配が流れない。これは、運動命令がMIMoという**微分できない物理シミュレータ**を経由するため。太郎の`basal_ganglia.py`が「知覚の学習＝backprop」「運動選択の学習＝方策勾配（REINFORCE）」を使い分けているのと**全く同じ理由**。運動headの学習には、今後、方策勾配を別途実装する必要がある（今回は未実装、配線の検証のみ）

**Phase 5〜7 総括**
- 5感覚すべての通訳層が完成し、実際にMIMoの観測から正しく変換できることを確認
- 試作の感覚運動の脳を介して、環境とエージェントが結合し、クラッシュせず回ることを確認（Phase 7の目標達成）
- 予測誤差・報酬の値自体は、まだ何も学習していない（重みが未学習の）状態でのものなので、現時点では意味を持たない。学習ループの実装は次の段階
- 視覚エンコーダの計算コスト（49ms/回）が今後の速度面の課題として残る
- 運動headの学習（方策勾配の実装）が次の明確な課題として残る

---

## bridge/フォルダを役割ごとに整理

`bridge/`直下にすべてフラットに置かれていたコードを、統合構想メモの図（環境/ラッパー/エージェント）に対応する形でフォルダ分けした。

```
bridge/src/
  body/      太郎内臓のコピー（internal_state, stomach, blood_vessel, adenosine, homeostasis）
  wrapper/   ラッパー（hybrid_env.py）
  senses/    ラッパーと脳の間の通訳層（insula, sensory_encoders, vision_encoder）
  brain/     エージェント（試作の脳、sensorimotor_brain.py）
bridge/tests/ test_hybrid_env.py, test_phase7_integration.py
```

`insula.py`は元々太郎では「脳」パッケージ内（内受容感覚の通訳）だったため、内臓（body/）ではなく通訳層（senses/）に分類し直した。移動に伴いimportパスを全て修正し、両テスト（test_hybrid_env.py, test_phase7_integration.py）が引き続きPASSすることを確認済み。

---

## 方針転換：試作の脳ではなく、本物のTaroBrainを拡張する方式に切り替え

運動性喃語（motor babbling、生後すぐから見られる本物の発達現象。声の喃語より早く始まる）を最初の学習対象とすることに決定。その実装方法を再検討し、**独立した試作の脳（sensorimotor_brain.py）ではなく、本物の`cortex.py`（TaroBrain）をコピーし、サブクラスで運動性喃語専用の経路を追加する方式**に切り替えた。

**懸念していたリスク**：TaroBrainは音声・語彙と密結合しており、直接拡張するのは大工事になると予想していた。

**実際にやってみた結果（想定より軽く済んだ）**：
- `taro_brain.py`（`cortex.py`のコピー、import文1行のみ修正）と、その依存`vocal_tract.py`（外部依存ゼロ、無編集でコピー）を`bridge/src/brain/`に追加
- **既存のメソッドは一切変更せず**、`TaroBrainWithMotor`というサブクラスで新規追加：
  - `sensory_proj`：5感覚融合ベクトル(320次元)を、音声embeddingと同じ次元に変換する層
  - `sensorimotor_prediction_head`：次に来る感覚を予測する
  - `motor_head`：関節命令を出す
  - `step_motor()`：この3つを使って1ステップ処理する新メソッド（既存の`forward_hidden`等、音声関連メソッドには触れない）
- `body_state_dim=0`で構築することで、`critic.py`への依存も回避（内受容感覚は既に5感覚融合ベクトルに含まれるため二重に持つ必要がない）
- **単体テスト・実際のMIMo経由の結合テスト（50ステップ）とも成功**。想定していたほどの大工事にはならなかった

**この設計の利点**：`sensory_proj`の出力を、既存の`self.gru`（音声用と同じ重み）にそのまま渡しているため、**将来「音声と運動を同じ隠れ状態で統合する」ときの土台がすでにできている**。試作の脳（sensorimotor_brain.py、既に動作確認済みで削除はしない）は、シンプルな比較対象として残す。

**次にやること**：`TaroBrainWithMotor`の`motor_head`に、方策勾配（REINFORCE）で実際に学習させる。報酬は感覚運動予測誤差（予測が当たりやすい動きほど良い＝運動性喃語の再現）。学習を重ねて予測誤差が下がる傾向が出るかを確認する。

---

## Phase 8：運動学習に必要な部品は、ほぼ全て太郎に既存だった

方策勾配の学習ループを設計するにあたり、いくつかの部品が「太郎に既にあるのでは」と気づき、確認したところ、実際にほぼ全て既存だった：

- **報酬の考え方**（予測が当たると心地よい）：`instincts/prediction.py`の`compute_prediction_reward()`。既に`core_b.py`で`compute_total_reward()`経由、実際の学習に組み込まれ稼働中
- **探索の強さ**（NE / 青斑核）：`instincts/locus_coeruleus.py`。報酬が少ない時期は探索を強め、報酬が十分なら弱める。音声に固有の実装ではなく汎用
- **方策勾配そのものの計算**：`basal_ganglia.py`の`TaroLearner.learn_action()`。中身は「選んだ行動のlog確率」と「報酬予測誤差δ」だけを扱う完全に汎用的な数式で、トークンや語彙への参照が一切ない

一方、**喃語を実際に生成するコード（`generate()`）は音声に深く依存しており、運動には転用できない**（発声のどこで止めるかの判定・調音器官の駆動など、中身が全部言葉の都合でできているため）。

そのため実装方針：`dopamine.py`・`locus_coeruleus.py`・`basal_ganglia.py`を`bridge/src/brain/`に無編集でコピーし（太郎からの流用）、`TaroBrainWithMotor.step_motor()`に、運動を正規分布からサンプリングして対数確率を返す仕組みだけを新規追加した（`generate()`の仕組みは使っていない）。誤差→報酬の変換は、太郎の既存の型（`imitation.py`の`max(0, 1 - dist/max_len)`＝誤差が小さいほど1に近い報酬）を踏襲しつつ、感覚のMSE誤差には`max_len`のような自然な上限が無いため、恣意的な正規化定数を持ち込まない`1/(1+誤差)`を採用した。

## 実装のやり直し：感覚エンコーダの「崩壊」

`bridge/tests/test_phase8_motor_learning.py`を書いて300ステップ回したところ、以下の問題が順に見つかった。

**1回目**：感覚エンコーダ（触覚・視覚等の通訳層）をオプティマイザに含め忘れ、ランダムな重みのまま固定されていた（`TaroLearner`は`brain.parameters()`しか見ないため）。修正し、脳＋感覚エンコーダをまとめて学習対象にした。

**2回目（本質的な問題）**：感覚エンコーダも一緒に学習させたところ、予測誤差が数万→9600万へ発散した。診断の結果、**「次の感覚」という予測の正解自体が、今まさに学習中の感覚エンコーダの出力**であることが原因と判明。エンコーダの出力にLayerNorm（学習パラメータなし、平均0・分散1に正規化するだけ）をかけて発散は止まったが、その代わり予測誤差が完全に0まで下がるという、別の意味で不自然な結果になった。

**3回目（崩壊の実測）**：学習後、全く違う2つの身体状態（リセット直後／5歩ランダムに動いた後）を融合ベクトルに変換して比較したところ、320次元のベクトルがほぼ同じ値になっていた（差はベクトルの大きさの2%程度）。予測head側の出力も同様。**「感覚を正しく予測できるようになった」のではなく、「どんな入力が来ても、ほぼ同じ一定の値を出す」という近道を学習していた**。これは以前確認済みの「衛星ヘッド崩壊」（体の状態だけで正解できる近道が常に成立し、語が不要になった事例）と構造的に同じ問題。

**原因**：予測に使う側と、正解を作る側が、同じ・同時に更新される重みを共有しているため、両方が「入力を無視して定数を出す」方向に一緒に動いても、誤差が簡単にゼロになってしまう（自己教師あり学習で知られる collapse という落とし穴）。

## 対策の根拠：恒常的シナプススケーリング（Homeostatic Synaptic Scaling）

対策案を3つ検討し（1. 正解側だけ更新をゆっくり追従させる／2. 出力のばらつきが下がったら罰する／3. 感覚エンコーダを一旦凍結し脳側だけ先に学習）、それぞれの人間模倣としての妥当性をWeb検索で確認した。

結論：案3（凍結→解凍）は「何ステップで解凍するか」という新たな恣意的な定数が必要になる上、人間の感覚野が文字通り「完全に凍結」した状態を経る訳ではないため、根拠は薄い。

対して、**恒常的シナプススケーリング**（Turrigianoらの研究）という、脳に実在する仕組みが今回の問題に直接対応することが分かった：
- 「予測誤差から学ぶ」ヘブ型の仕組み（今回のbackprop）は、放っておくと不安定化し、活動が0に潰れるか暴走するかのどちらかに転びやすいことが理論・実験の両面で知られている
- これに対抗するのが恒常的シナプススケーリング：神経細胞は「これくらいの活動量が普通」という基準値（set point）を持ち、実際の活動がそこから外れるとシナプスの強さ全体を底上げ／底下げして基準値に戻す
- 発達期の大脳皮質感覚野で機能していることも確認されている

「予測誤差からの学習」と「暴走・崩壊を防ぐ恒常性」は脳の中でも別々の・並行して働く仕組みであるため、今回もこれを、NE・ドーパミンと同じ「本能」として実装する方針とした（設計のみ確定。実装はこれから）。

参考文献は`docs/参考文献リスト.md`にまとめた。

**自己訂正メモ**：当初「案2（ばらつきで罰する）は根拠のない工学的トリック」と即断したが、確認せずに判断したための誤りだった。実際には恒常的シナプススケーリングと機能的に同じであり、正しく実装すれば人間模倣として十分成立する。裏付けを取らずに「これは工学的」「これは人間模倣的」と判定しないこと（次節の教訓として記録）。

## 教訓：新しい仕組みを作る前に、太郎の既存コードを確認する

このセッションでは、NE（探索）・予測誤差報酬・方策勾配の計算という3つの部品について、一度は「MIMo用に新しく設計する」つもりで検討を始めたが、実際には全て太郎に既存で、そのまま流用できるものだった。指摘を受けて都度確認し直す形になった。

**今後のルール**：MIMo統合で何か新しい仕組み（報酬・探索・学習・恒常性維持など）が必要だと思ったら、実装を始める前に、まず`B/src/taro/brain/instincts/`（と`basal_ganglia.py`）に同等のものが既に無いかを確認する。太郎は既に多くの本能を実装済みであり、「無いはず」という思い込みで新規設計から入らない。

---

## Phase 0-8の複数エージェントレビューと、その後の修正

Phase 0-8の実装・方向性について、長期目標（介護AI）・中期目標（人間模倣）・短期目標（`MIMo統合_計画書.md`との整合性）・Pythonコードの正しさ・学術的な正しさの5観点で複数エージェントにレビューさせ、討論させた。詳細な所見は`docs/人間模倣からの逸脱リスト.md`に記録した。

**見つかった実装バグの修正**：`taro_brain_motor.py`の`step_motor()`は、クランプ**前**の`raw_action`でlog_probを計算していたが、実際にMIMoへ送られる（報酬の原因になる）のはクランプ**後**の`motor_action`だった。これは太郎自身の`cortex.py`が過去に発見・修正した「ノイズ適用前の値でlog_probを計算してしまう」バグ（B2-6）と同型の再発だった。B2-6と同じ修正方針（実際に使われた値でlog_probを計算する）で修正した。

**修正後に判明した未解決の問題**：バグ修正後にテストを再実行したところ、感覚エンコーダの崩壊（Phase 8で対策したはずのもの）が**再び起きた**（違う入力での見分けやすさが0.3%まで悪化）。乱数シードを固定していないため、恒常的シナプススケーリングによる対策は実行ごとに効いたり効かなかったりする、**不安定な状態**であることが分かった。「対策済み」と早合点せず、未解決のまま`docs/人間模倣からの逸脱リスト.md`のB3に記録した。

---

## 崩壊問題の根本原因を特定・解決（複数エージェントによる討論）

その後、GRU分離（音声用と運動用）・PV-RNNに着想を得た確率的潜在変数（`predictive_coding_latent.py`）・独立した固定の目標エンコーダ（`target_fusion`）と、いくつもの対策を積み重ねたが、8シードで検証すると3/8しか健全にならず、根本解決には至っていなかった。

そこで、ML工学・予測符号化の忠実性・力学系・一般ML文献・予測符号化/PV-RNN研究の文献という5つの視点から複数エージェントに調査させ、3ラウンドの討論をさせたところ、**3ラウンドとも一致して同じ原因**に到達した：`predictive_coding_latent.py`の`prior_net`（"事前の予想"を作る部品）が、コード中のすべての使用箇所で`.detach()`されており、**一度も学習されずランダム初期化のまま固定され続けていた**。これが、KLダイバージェンスの分母（`exp(logvar_p)`）を、学習で補正されないシード固有の値にしてしまい、「一部のシードだけ崩壊する」現象の直接の原因になっていた。あわせて、力学系の視点からは「方策勾配（REINFORCE）が`z`を経由して感覚エンコーダまで逆伝播し、高分散なノイズを知覚学習に混ぜている」ことも指摘された。

**対策**：①`prior_logvar`・`post_logvar`を安全域`[-4, 4]`にクランプ、②外側のKL損失で`prior_net`のdetachを外し実際に学習されるようにする、③`motor_head`に渡す`z`を`.detach()`し方策勾配が共有エンコーダへ漏れないようにする、の3点を実装。

**結果**：同じ8シードで再検証したところ、崩壊率が**5/8→1/8に改善**した。詳細は`docs/人間模倣からの逸脱リスト.md`のHに記録。一般ML文献調査からは「固定した目標エンコーダだけでは崩壊を完全には防げない（BYOLのアブレーションが示す通り、理論的にはシード依存の"引力域"問題が残りうる）」という知見も得られており、今回の1/8という残りは、これと整合する結果と考えられる。

---

## 測り方の見直しと、崩壊解決の最終確認

上記の「1/8」を出した崩壊チェックは「リセット直後 vs 5歩後」の1回勝負で、ランダムな5歩に依存して結果がブレることが分かった（実行するたびにシード5が崩壊したり健全になったりした）。今日1日で測り方のミスを4回・乱数のズレを3回やらかしたのを機に、`docs/検証の落とし穴チェックリスト.md`を作成し、崩壊チェックを「24個の多様な姿勢での融合ベクトルのばらつき（バッチ分散、未学習エンコーダ基準で正規化）」という安定した指標に置き換えた。

さらに、この新しい測定器そのものを検証した（チェックリスト項目6＝測定器の健康診断）：
- **Test A**：わざと定数を返す偽エンコーダにかけて 0.00% → 崩壊を正しく検出できる（測定器は健全）
- **Test B**：`prior_net`の修正をわざと元に戻した"壊れた版"を学習させると、シード5が0.3%まで崩壊（1/5）。**同じシード5が、修正版では81%（健全）**。バグ1個だけを変えた対照実験で結果が崩壊↔健全と反転し、検証済みの測定器がそれを正しく判定した。

**最終結論**：
1. 崩壊は**本物の現象**だった（測定の誤検知ではない。Test A・Bで実証）。ただし旧チェックは深刻さを**水増し**していた（直したシード5を「崩壊」と誤判定していた）。「崩壊は全部測定ミスだったのでは」という一時の疑いも、行き過ぎだった。
2. `prior_net`・`z.detach`の修正は、その本物の崩壊を**実際に解決していた**。信頼できる新チェックで、修正版は**13シード全て健全（崩壊 0/13）**。
3. motor collapse（運動のフリーズ）も別途検証し、起きていないことを確認（逸脱リストB1）。

課題1（感覚エンコーダの崩壊）・課題2（運動のフリーズ）はいずれも解決とみなせる。

---

## 評価指標の見直しと、自己モデルの"形成"の検証（2026-07-11）

崩壊解決の後、「順モデル（自己内部モデル）が確立したか」の検証（目標C1）に進んだ。当初は**「持続予測（＝"何もしない"と言い張るだけの予測）に、絶対誤差で勝てるか」を合格基準**にしていた。多角的に diagnose した結果、次々と formulation の問題が見つかり、各々を直すたびに改善した：①順モデルが**行動を入力に取っていなかった**（遠心性コピーを追加）②予測を32次元の潜在から"まるごと"当てていた（**残差予測**＝今の感覚＋変化量に変更）③予測の"正解"が崩壊防止の凍結ランダムエンコーダで難しすぎた（**固有感覚という自然な信号**に変更）。基準値比較は 191%→106% と一直線に改善したが、**それでも持続予測に勝てず、経験を増やしても互角の手前で頭打ち**になった。

**スケール不一致の発見と高速化**：この頭打ちを掘る中で、根本的な問題に気づいた——300ステップ＝シミュレート内わずか3秒で、人間が数ヶ月かけて作る内部モデルの"確立"を測っていた。対策として、この段階に不要な**視覚と触覚センサをオフにし、脳の判断を1秒に1回に間引く（frame-skip）**ことで、実行速度を **sim/real 0.18→5.83（約32倍速）** に改善（重かったのは触覚センサ計算74%と視覚レンダリングで、物理は激安と実測で判明）。これで初めて"数時間ぶんの人生"を現実的な時間で与えられるようになった。

**評価指標そのものの誤り**：ここで、そもそも「持続予測に勝つ」という基準が、時系列予測の一般的なサニティチェックであって、**自己モデルの分野の標準ではなかった**ことに気づいた。先行研究（発達ロボティクスの自己他者認知、乳児の随伴性検出）を当たると、内部モデル形成は**絶対精度ではなく「自己（随伴）vs 他者（非随伴）の予測誤差の対比＝分類」で機能的に判定**していた。汎用MLの直感で baseline を選び、ドメインの文献で裏取りしていなかったのが反省点（検証の落とし穴チェックリストに項目7として追加）。

**正しい指標で見ると結論が変わった**：自己vs他者の対比（入替テスト）と、予測変化と実変化の相関を主指標に据え直すと、太郎の自己モデルは **経験とともに強く伸びていた**（入替マージン +8.6%→+22%→+43%、相関 0.2→0.4）。この指標が本物であることは健康診断で確認（未学習の太郎では≈0）。一方の絶対精度の頭打ちは、**1秒先の全身関節が転倒・接触で予測不能な部分が大半という課題の性質**によるもので、モデルの欠陥ではないと結論した。

**PV-RNN化の是非**：予測が弱い一因として「推論時の誤差回帰が簡略（今の一瞬にしか合わせない）だから」を疑い、アブレーション（回帰の回数 0/3/10）で確かめたところ、**回数を増やしても予測は改善しなかった**。よって「回帰が浅いから作り替える」根拠は弱いと判断（多時刻の"窓"への拡張は別軸で未検証）。作り替える前に安いアブレーションで確かめ、無駄な改修を回避した。

**残る論点**：自己モデル形成の究極の判定は、先行研究では「自分用のモデルを他者に当てはめて他者を予測できるか」＝自己→他者の転用（目標D）。ただし他者の観測には視覚が必要で、真のDは視覚＋対応問題という大きな一歩。詳細な数値と判定は`docs/人間模倣からの逸脱リスト.md`セクションIに記録。

---

## 目標C2：睡眠リプレイによる自己モデルの定着（2026-07-13）

C1で「絶対精度の頭打ち」を"課題の性質"と結論していたが、真因をさらに掘った。**消去法**：学習率を0.001/0.005/0.01で振っても飽和点（持続予測比~80/変化相関~0.45）は動かず＝lrは無罪（3seed）。探索ノイズも、報酬ベースのNE調整が最初の10分で下限まで下げていて無関係と判明（「永久に高ノイズ」という当初の見立ては誤りで、実測のnoise列で訂正）。予測ホライズン(K)を短くする診断は、Kを変えると指標の意味・生きた時間まで同時に動く交絡で判定不能だった。

決め手は**公平対決**（予測構造だけを同じ土俵＝1秒先で比較）：素のMLP順モデルをオフライン多周回で学習すると相関0.58まで届いた（naiveな「0.1秒×10連鎖」ロールアウトは誤差の雪だるまで大負け）。＝**頭打ちはホライズンでなく、太郎の"オンライン1回きり学習（復習不足）"が原因**、と推理が確定した。

これは人間の睡眠中の記憶固定の欠落そのもの：既に目標Bには海馬（`hippocampus.py`）による睡眠リプレイがあるのに、Cの自己モデル学習はそれを使っていなかった。そこで**睡眠中に貯めた経験を再生して定着させるリプレイ**を実装し検証したところ、相関 0.44→**0.56**・持続予測比 82→**69**と頭打ちを突破（オフライン上限にほぼ一致）。自己/他者の区別（agency）も 83→**95%** に改善。独立した2手法（オフライン0.58・リプレイ0.56）の一致が、真因の確定を裏づけた。

**本実装（太郎C2-1）**：この機構を実験scriptから脳本体へ格上げした。運動用の海馬 `MotorHippocampus`（言語用海馬の"型"を感覚運動テンソル用に書き直したもの＝コピーではない）と、`TaroBrainWithMotor` に `infer_latent`（行動をGRU入力へ）・`predict_proprio`（残差予測）・`consolidate`（睡眠リプレイ）を追加。脳の公式メソッドだけでリプレイ効果を再現（改善幅 persist −12・corr +0.13、実験と一致）。＝太郎は**言語も自己モデルも同じ海馬リプレイで睡眠中に定着する**形に統一された。

## 発達の時計 と 運動小脳（2026-07-14、太郎C2-2/C2-3）

**発達の時計（C2-2）**：時間を3軸（①現実／②物理・代謝＝sim秒／③発達年齢＝累積学習回数）に分離。`DevelopmentalClock` を新設し、成熟（NE結晶化）を②sim秒判定から③発達年齢駆動へ付け替え。睡眠は逸脱として不採用（覚醒中の周期リプレイで代替）と決定。詳細は `doc/人間模倣からの逸脱リスト.md`。

**運動小脳（C2-3）**：Bの海馬が未コピーだった教訓（項目8）から、**もう一つの未コピー機構＝小脳（運動の自動化）**を連続版 `MotorCerebellum` として実装。方針の行動を状態から真似るフィードフォワード地図＋馴染み度で自動化重みを上げる（結晶化）。目的は**逆モデルの前提＝行動→感覚の結びつきの強化**。

**健全性アブレーション（ON/OFF×2シード×1時間）**：小脳が**変な挙動をしないか**の確認（確立度向上は不問）。結果＝**全項目健全**：クラッシュ/NaNなし、persist は未学習124〜131%→70〜73%へ正常下降（発散なし）、agency 43〜51%→88〜97%（崩壊なし）、margin +48〜+51%（反転なし）、運動フリーズなし（mag比97〜108%）、小脳ゲート自己安定（err_ema 0.21〜0.22）。一方 **margin/corr/persist/agency は ON≈OFF＝現指標では効果不変**（Cでは結晶化系が元々ほぼ無効）。

**判断**：小脳は**安全なので既定ONで採用**。ただし「良性だが現指標では不変・常時稼働・全身1つのw（部位別未実装）」ゆえ、**逸脱ではなく"注意すべき機能"**として `doc/注意すべき機能リスト.md` に登録。本当の出番は「行動→感覚の結びつき強化（逆モデルB道の地ならし）」または「部位ごとのw（somatotopy）精緻化」の後。数値は `logs/C/cerebellum_ablation/`。

## 逆モデル診断と予測幅スイープ（2026-07-14）

**逆モデル診断（Stage1/1.5）**：順モデルを凍結して"逆算"できるか検証。①model上＝`infer/random≈0.2`（ランダムの5倍目標に当たる）②現実の実行テスト＝`star/random≈0.5`（現実でも2倍、MuJoCo state save/restore, `d_floor≈0`で妥当）。→ **順モデルは逆算に耐える（可逆）／現実に部分転移（GIGOで頭打ち）**。3シード。詳細は `docs/C/逆モデル創発_B道_文献と課題.md`。

**予測幅スイープ：1秒(K=100)が最良、"短縮＝人間条件"仮説は棄却**：
- 動機＝「人間は短い予測幅+閉ループ→太郎も予測幅を短くすれば精度↑？」。指標はpersistでなく**行動依存(margin)/逆到達(star/random)**（persistは短い幅で持続予測が満点になり不公平）。
- **2つの公平軸で挟み撃ち**：①発達時間そろえ（学習3600回固定）②経験量そろえ（sim60分固定＝短いKは学習回数を増やす）。
- **結果**：両軸ともK=100が最良。**K=10は経験10倍（学習36000回）でもK=100（3600回）に全指標で届かず**。→ 差は交絡でなく**本質的に長い予測幅が有利**。
- **解釈**：0.1秒だと動きが小さすぎ信号が消える（カオス減より信号減が勝つ）。人間の短予測幅の強さは**閉ループ由来**で、太郎は開ループゆえ短縮だけでは人間条件にならない。
- **決定**：予測幅は**K=100で確定**。さらに伸ばす（K=200/500）は指標は上がりうるが**非人間的＋長Kで交絡が悪化**ゆえ棄却（`doc/やることリスト.md`に形状確認として保留）。人間の高精度の本命は**閉ループ**＝将来。※1シードだが全指標・両軸で一貫。ログ＝`logs/C/horizon_sweep/`・`horizon_eqsim/`。

## Goal Babbling：目標指向で"逆モデル側"が伸びる（2026-07-14）

運動性喃語（発達段階）の中で、動かし方を **Motor Babbling（ランダム探索）→ Goal Babbling（目標指向）** へ半分移行（`C_GOALBABBLE`、`i%2`で50%）。目標＝経験バッファから過去の固有感覚をサンプル（Self-Priorの最小版）、逆モデル推論（凍結順モデルを勾配で反転）でそこに届く行動 a\* を逆算して据える。K=100・2シード・逆テスト込み。

**結果（Motor→Goal）**：
- **逆側が明確に改善**（2シード一致）：現実の目標到達 **star/random 0.55→0.45**、行動の復元 **recover_corr 0.32→0.50**。margin +47→+53、agency もやや上昇。
- **順モデルの精度は横ばい**：corr 0.534→0.535、err_areal ≈1.07で不変。

**解釈**：Goal Babbling は順モデルの予測精度でなく、**逆側（望む感覚→行動・目標到達）を直接鍛える**＝発達ロボティクスの定説（goal babbling は inverse model を効率よく bootstrap する, Rolf et al.）通りの効き方。＝**目標指向という機構自体が効くと実証**。

**暫定・次**：探索/目標指向の切り替えが `i%2` 固定＝非人間的（`doc/人間模倣からの逸脱リスト.md`に記載）。次は**NE（好奇心）・恒常性のズレから切り替え確率を創発**させる精緻化へ。ログ＝`logs/C/goalbabble/`。

### 切り替えの創発化（`C_GB_SWITCH`、2026-07-14）

`i%2`固定を、内的信号から創発する切り替えに置換。3方式を比較（star/random, 低い＝良）：
- **fixed（i%2, 50%）**：0.445（基準）
- **ne（NE単独）**：0.52。NEはCで10分で下限に張り付き幅がない→実質90%目標ねらいに退化→**やりすぎで悪化**（探索が減る）。＝**50%のバランスは偶然でなく効いていた**。
- **pe（予測誤差＋NEの足し算, 創発, 既定）**：**0.46**。motor(0.545)・ne(0.52)より良く、**fixed(0.445)にほぼ並ぶ**。＝**切り替えを驚き駆動の創発に置換しても効果は維持**、より人間的。ただし僅差でfixedに及ばず・seedばらつき大。

**位置づけ**：向き（驚き大・NE大→探索）は根拠あり（EFE/LC-NE/予測符号化）だが、**足し算・重み・正規化の式は恣意的**（逸脱ラベル済み）。完全な根拠づけ＝EFE直接計算（やることリスト）。＝**恣意的な50%を、根拠ある向きの創発に置換（効果同等・人間度↑）**を達成。

## C4：閉ループ制御 — MPC流用は失敗、予測幅を揃える（2026-07-14）

閉ループ vs 開ループの到達診断（`closed_loop_probe`, `C_CLPROBE`）を実装。目標姿勢g（過去に経験した姿勢）へ、開ループ（逆算した1行動を保持）/ 閉ループ（内側の刻みごとに実際の状態から再逆算して補正）/ random / nothing で到達距離を比較（MuJoCo save/restore, d_floor≈0で妥当）。

**第1版（補正の刻み＝K//8＝0.12秒、順モデルは1秒先予測）＝失敗**：closed/open ＝ 1.13, 1.16（2seed）＝**閉ループが開ループに負け**。closed/nothing≈0.95（目標へほぼ近づけない）。
- **原因＝予測幅のミスマッチ**：順モデルは1秒先を予測するのに、閉ループは0.12秒ごとに補正→ちょっとしか動かず、再計算しても状態がほぼ同じ→ほぼ同じ行動を出し直すだけ＋ジッター＋GIGO。＝**「1秒モデルをMPCで0.12秒補正に流用」は噛み合わない**（＝K=100で回してた点をユーザーが指摘）。

**修正方針（詰める）**：補正の刻みを**順モデルの予測幅に合わせる**（`k_inner = K`）。＝再計算のたびに、モデルが得意な予測幅で補正する。これが噛み合えば「閉ループ（横並びに補正）＞開ループ（撃ちっぱなし）」が出るはず。→ 再検証。
（さらに本命は、短K＋閉ループ込みの訓練で"短い先を予測するモデル"を作ること＝人間版。まずは既存モデルで予測幅を揃えて検証。）

**予測幅マッチ版・根拠づけ版（K=100）＝いずれも負け**：k_inner=K（1秒補正）に揃えても closed/open＝1.05〜1.09（reach_len=4固定）／1.17〜1.31（停止条件・reach_len≈2.2）。＝**K=100の閉ループは版に依らず開ループに負け**（補正が1秒間隔で粗く、2回で止まる浅いループ）。停止条件は「1回でも近づかなければ止める」でやや早すぎ（ノイズに弱い、要改良）。＝**K=100は閉ループに不適**と確定。

**短K(K=10)＋閉ループreaching訓練（held goal＋停止条件）＝失敗（せいぜい微増）**：閉ループ訓練 vs 開ループ(motor babbling) を短K同士で比較。margin +29 vs +25、corr 0.38 vs 0.37＝**目的的な動きは僅かに信号を足すが救うほどでない**。closed/open(probe)＝1.08〜1.11で**閉ループ制御も開ループに負け**。しかも短K全体が **K=100最良(margin48/corr0.53)に遠く及ばない**（margin29/corr0.38）。

**C4の結論（決着）**：**閉ループ単体では短Kを救えない**。真の壁は**"0.1秒で学習"すること自体**（信号がノイズに埋もれてモデルが育たない）。人間は「一生かけて（長いスケールで）学び、0.1秒では使うだけ＋時間積分＋多感覚融合」でこれを回避してる。＝閉ループは"使う側"の工夫で、"学習側"の工夫（①階層的時間スケール＝長い窓で学び短い間隔で使う ②時間積分 ③多感覚）が要る。候補は `doc/やることリスト.md`「短K学習の信号不足を効かせる候補」に記録。→ **C4＝閉ループ制御を探究し「単体では不十分、学習側の工夫が必要」と結論**。次の本命はこの候補群、または聴覚D。

## 短K学習の候補検証とK=100採用（2026-07-14）

C4の「短K学習の信号不足」を、やることリストの候補で検証（自律作業＋その後）。すべて負／小改善：
- **中間予測幅の sweet spot**（K=30/50 閉ループ）：closed/openが全Kで>1＝sweet spotなし。閉ループの敗因は予測幅でなくジッター。
- **Kalman平滑化**（Wolpert根拠）：閉ループのジッターを修正（K=50で closed/open 1.2→0.98）＝制御は直るが短K学習は直らず（K=10は平滑でも救えず＝GIGO）。
- **MTRNN（遅い層＝長窓を積む、Tani/Hasson根拠）**：最小版（遅い層→予測ヘッド）margin+2、top-down版（遅い層→速いGRU）margin+1.2。**両方とも小改善どまり**（K=100の48に遠く及ばず）。固定の漏れ積分では不足＝学習される遅いユニットが要るが勾配が繊細で未実施。
- **測定の健全性**：MTRNNのコード変更後も **off が baseline（margin~25）を再現**＝evaluate/agencyへの遅い層配線は測定を壊してない、と確認（落とし穴6）。指標の妥当性も文献照合＝margin/agency/classifyは分野標準（比較器モデル, Lanillos自己他者, 乳児随伴検出）、corrは部分的（向きのみ）、と裏取り（落とし穴7）。

**決定＝K=100（1秒間隔）で進む**：短K学習・短K閉ループ・MTRNNが全て届かず、1秒間隔が一番良いモデル（margin48/corr0.53）・一番動く。しかも0.1秒で"学習"するのは人間もやってない（人間は長スケール学習＋睡眠リプレイ＋短制御）。**制御が1秒＝人間の0.1秒より粗い点は逸脱として記録**（`doc/人間模倣からの逸脱リスト.md`）。制御の人間化は保留。MTRNNの未コミット変更はrevertし、知見のみ残す。→ **1秒のまま目標D（聴覚D等）へ進む**。

## D準備＝現C性能の総点検（順/逆の天井）と睡眠リプレイ既定ONの発見（2026-07-15）

Dは「Cの自己モデルを他者に当てはめる」段階なので、着手前に**Cが今どれだけの性能か**を総点検した。

**★睡眠リプレイが既定OFFだった＝「+48」の正体**：Dの自己モデル(`D/scripts/`の寄せ集め版)が margin+11 で頭打ち。真因を消去法で追った（proprio内容・落下・前庭…全部ハズレ）が、**実CSVを直接照合**して判明＝**margin+48は`replay_on`（`C_REPLAY=1`）だけ、既定の`patched_layernorm`(replay OFF)は+11**。つまり **taro-C2睡眠リプレイ（自己モデル確立の本命機構）が既定OFF**で、素のCも私のDも揃ってリプレイ無し＝+11だった。→ **`C_REPLAY`既定を`"1"`(ON)に変更**（本命機構が既定OFFなのが誤解の元。アブレーション時のみ`C_REPLAY=0`）。教訓＝[[feedback-debugging-workflow]]どおり**推測を重ねず生データに当たる**（メモリの「睡眠リプレイで頭打ち突破」を自分で活かせていなかった）。

**順モデルの天井（リプレイON, seed0）**：margin 0→約48で**1800step付近で頭打ち**、以後~46-51で横ばい。corr~0.55、agency~94%(後半平均)、classify~96%。＝G1/G2とも満タン、実質飽和。※各指標は各checkpointの**その時点モデルのスナップショット**（学習中データは母数に含まない）。agencyはn=30で±数%のノイズ。

**逆モデルの天井を初測定（`C_INVTRAJ=1`＝各checkpointで逆probeも回す、5400stepまで）**：逆モデル（＝順モデルを反転して使う。独立ネットは無い＝創発）は **600stepの時点で既に平ら**、5400まで伸びない。**説明力(1−infer/random)~+78%＝強／行動当て(recover_corr)~0.37・到達力(1−star/random)~+50%＝弱**。原因は**縮退**（1秒・全身の逆問題は多対一。`err_infer<err_areal`＝逆算行動が本物より観測をよく説明＝本物は一意でない、が証拠）。＝「**区別/説明は強・正確な復元/到達は弱**」を天井として確認。**追加学習で逆は伸びない**（あなたの「5000まで測るべき」仮説を検証した結果＝"逆は学習量でなく問題の性質で頭打ち"）。

**Dへの含意**：①C3(逆モデル)強化のための追加学習は不要＝もう天井。②D2/D3を**"区別"で測る**設計は正しい（強い側を使い弱い側を避ける）。③逆の弱さ(到達~50%)が効くのは**太郎ベータが目標を区別可能に動けるか**の1点＝**D2-0（源泉分離性ゲート）で最初に検証**。ただし行動空間の縮退≠目標空間の区別不能（ベータは不正確でも目標の方向へは動く＝「それっぽい動き」）。太郎の今の逆モデルは人間で言えば**リーチ開始期（生後3-5ヶ月・弾道的で方向は合うが不正確）**の雰囲気（ゆるい類推、閉ループ<開ループと符合）。

**インフラ追加**（`run_c_metrics_ac_lr.py`）：`C_SAVEMODEL=<path>`＝確立モデルを保存（従来は測って捨てていた）。`C_INVTRAJ=1`＝逆モデルの天井軌跡を記録。最高モデルを `D/models/self_A_best_seed0.pt`（proprio621+前庭+内受容, replay+cereb ON, margin~51）に保存＝Dの土台。二体は今後**太郎アルファ（学ぶ side）／太郎ベータ（相手）**と呼ぶ。

## D環境の作り直し（継承型）とD0＝暗い部屋問題の発見・解決（2026-07-15）

**D環境の作り直し＝接頭辞で解決**：自作の`two_agent_env`（生MjSpec・qpos只・自作触覚）がCの感覚スタックを捨てていたため差分埋めが泥沼化。**MIMoEnvは名前の接頭辞で自分の体を識別する**（関節=`robot:`、アクチュエータ=`act:`）と判明→ベータを`beta_`付きでattachすれば**MIMoEnvが自動でアルファだけを"自分"と認識**。`d_env.TwoMimoEnv`＝`MIMoV2DummyEnv`継承＋`_initialize_simulation`差し替えのみ（約70行）で、アルファはproprio621/前庭/触覚/視覚をフル取得。**移植検証＝単体C margin+50.0 → D2体 +50.1（差+0.1）**＝Cの最高モデルが無傷で載る。HybridEnv(interoception)・Cの学習ループ・睡眠リプレイは全て継承。

**接触は物理から創発しない（実測）**：自由なMIMoは倒れ、2体は**接触率2%・触覚の97%は床**。sep/積み重ねを振っても最良15%か物理破綻。→**分野標準はMIMo `selfbody`の「座位＋`<weld body1="mimo_location"/>`で固定＋腕のみ自由」**（BabyBench 2025の課題も self-touch/hand regard＝どちらも"自分"。2体触覚は標準タスクに無く、Dは新規領域）。座位なら床は尻・脚で感じ**手は浮く**＝どの部位で感じたかで発信元を切り分けられる（somatotopy）。MIMo既定の触覚scaleは**指先0.002 vs 下腿0.038＝19倍**＝人間の皮質拡大が実装済み。

**D0（自己接触）を新設**：Cの自己モデル(+50)は**触覚を一度も感じていない**＝触覚を知らないモデルに他者の触覚を渡しても解釈できない。人間も触覚が最初の自己探索感覚。→`MIMoSelfBody-v0`＋HybridEnv＋Cの学習ループで**固有感覚＋触覚(2134次元)を予測**する自己モデルを確立（`d0_selftouch.py`）。

**★暗い部屋問題を実測で発見・解決**：
1. **バネダンパー(既定)＋旧報酬**：margin+42、しかし動画で**右手が人間離れした暴れ方**（ユーザーが目視で発見）。実測 |a|=0.833・**飽和65%**＝可動限界に叩きつけ。「自己接触65%」の正体は探索でなく**振り切った先に体があっただけ**→創発の主張は撤回。
2. **筋肉モデル(`MuscleModel`)へ**：MIMoは駆動を3種持つのに既定はバネダンパーだった。筋肉は**行動8→16（主動筋/拮抗筋）**、固有感覚**226→242**（筋の活性・長さ＝筋紡錘/腱器官に対応）、ランダム行動の関節速度が**8.3→2.0（1/4）**＝人間らしい。**ランダムでも自己接触82%**（腕が自然に体に添う＝乳児の姿勢）。
3. **しかし学習すると悪化**：筋肉＋旧報酬で**飽和100%・自己接触0%・触覚3778で一定**＝**全筋肉を最大共収縮させて腕をロック**（筋肉の痙攣）。**指標は margin+52.5/corr0.757 と最高値**。＝報酬`1/(1+誤差)`が「予測しやすい状態」を求めた結果、**動きを消せば予測は完璧＝高報酬**という**暗い部屋問題**（自由エネルギー原理への古典的批判）が実際に発生。**学習するほどランダムより人間から遠ざかる**。**指標だけでは検出不能（動画で発覚）**＝落とし穴。
4. **学習進度(Oudeyer)へ変更**＝`pe_slow − pe_fast`（太郎が既にGoal Babbling切替用に計算していた値。恣意的定数ゼロ）。**結果＝関節速度6.06（最も穏やか）・自己接触31〜67%・触覚は変動、かつ margin+55.0/corr0.810/persist34.4 で全条件中"最高"**。＝**動きを取り戻したのに自己モデルはむしろ向上**（トレードオフでなかった。動く太郎は情報の豊かな経験を作る）。「人間らしくしたら性能も上がった」が筋肉・動機で**2回連続**。

**留保**：①飽和は学習で28%→70%へ上昇＝暗い部屋の引力は弱まったが残存。②1シードのみ。③自己接触率は**計測窓に依存**（筋肉は遅く、60stepでは腕が体に届かず0%、150stepで31%）。④`d0_record_trained.py`の飽和率表示にバグがあった（1つでも該当すれば100%）→修正済み。


## ★上記D0節の主張のうち3件を撤回（2026-07-15・同日）

上のD0節は**測定が汚染された状態で書いた**。同日中に原因が判明したので、以下を撤回する。
**残す理由**：どう間違えたかが、この先の判断材料になるため（消すと同じ失敗を繰り返す）。

| 撤回する主張 | 実際は何だったか |
|---|---|
| **暗い部屋問題を実測で発見した** | **LocusCoeruleusの尺度依存の産物**。NEは報酬の絶対値に固定閾値(<0.1で探索/>0.3で活用)を当てる設計で、値域の違う報酬を入れると壊れる（学習進度≒0.04は常に「報酬ゼロ」と誤認）。NEを相対化すると`predict`は**飽和しない(11%)**＝暗い部屋は起きない。 |
| **学習進度で解決した（margin+55.0が最高）** | **エピソード打ち切りバグ込みの数字**。修正後は全指標で悪化（飽和71%・固有感覚persist104%＝体のマッピングが壊れる）。さらにCの綺麗な環境で測ると**動機4条件が全部同着**。 |
| **自己接触が創発した（31〜67%）** | **初期姿勢で既に触れていただけ**。接触ペアを直接読むと、**何もしない太郎（行動ゼロ・学習ゼロ）で自己接触100%**（`right_fingers - right_lower_arm` 30/30回＝指が前腕に載りっぱなし）。学習後の41〜96%は創発どころか**何もしないより下**。 |

**根本原因（`MIMoSelfBody-v0`を借りたこと）**：
- `done_active=True`（既定）＝環境の課題を達成すると終了。目標部位がランダムなので**初期姿勢で既に触れている目標を引くと3 env.stepで終了**。
- `max_episode_steps=500`＝K=100の太郎は**5回判断で打ち切り**（Cは6000＝60回判断）。
- `<weld body1="mimo_location"/>`＝**太郎の腰が世界に溶接**されていた（体そのものが1ミリも動かせない）。
→ 詳細と対策は `検証の落とし穴チェックリスト` 項9。**Cの環境(`MIMoBenchV2-v0`)は max_episode_steps=6000・終了条件全てFalseで最初からクリーン**。汚染されていたのはD0だけ。

## 動機の比較は n=1 では判定不能と判明（2026-07-15）

触覚という交絡を外すため、**Cの環境（触覚なし・proprio621＝margin+51が出ると分かっている唯一の環境）**で動機だけを差し替えた。

| 条件（全て seed0・3600step） | margin |
|---|---|
| 予測しやすさ＋絶対NE（朝のラン・`C_INVTRAJ=1`） | **46.2** |
| 予測しやすさ＋絶対NE（午後のラン） | **57.0** |
| 予測しやすさ＋相対NE | 50.9 |
| 学習進度＋相対NE | 51.8 / 52.3 |
| 学習進度＋絶対NE | 50.8 |

**同一条件・同一シードで 46.2 vs 57.0（10.8ポイント差）。条件間の差は 1.5ポイント。ばらつきが測りたい差の7倍。**
朝のランは`C_INVTRAJ=1`で各checkpointに逆probeを挟んでおり、**その診断が乱数を消費して学習の乱数列をずらした**（落とし穴チェック項3を自分で踏んだ）。

**結論**：①**今日の動機比較は全て無効**（「学習進度が僅かに最良」も撤回）。②Cでは**探索ノイズが0.095〜0.500と5倍違っても結果が同じ**＝体が十分に振れていれば動機は結果を左右しない。**動機の設計が効くのは、狙わないと信号が立たない課題だけ**。③今後の比較は**最低3シード・診断コードを外して**回す。

## ★Cの margin+51 は「床でもがく太郎」が出した数字だった（2026-07-15）

録画を等速で撮って初めて分かった。**Cの太郎は0秒で立っているが2.8秒で転倒し、以降20秒間ずっと床でもがいている。** 今日一日「Cの+51に追いつけ」と基準にしてきたが、**その+51がどんな体勢で出た数字かを誰も見ていなかった**。

**ただし+51自体は本物**：marginは行動を入れ替えたときの誤差変化＝**行動依存**を測る。転倒は重力＝行動と無関係なので、転倒がmarginを水増しすることはない（むしろ下げる）。

**本当の含意**＝自己モデルの成否を決めるのは**体が行動でどれだけ動けるか**：
- **C**：転倒 → 手足が自由に振れる → 信号が大きく変わる → margin +51
- **D0**：座位＋腰を世界に溶接＋腕を胸に畳んだまま → ほぼ動けない → 学べない

「Cは成功・Dは失敗」の正体は、動機でも触覚でもなく**環境が体の自由を奪っていたか**だった。

## 仰向け(supine)の導入＝立位と互角、しかも発達段階として正しい（2026-07-15）

**仮説**：Cが成功したのは転んで偶然「手足が自由な状態」になったから。ならば**意図して仰向けから始めれば**、転倒なしで同じ状態に到達できる。人間でも**新生児の自己接触は仰向けで起きる**（座位で触るのは首がすわった後）。

**実装** `D/scripts/d_supine_env.py`：仰向けの姿勢の式は MIMo 同梱の `roll_over.py`（`STARTING_POSITION="supine"`）から借りたが、**シーンはCと同一の`benchmarkv2_scene.xml`のまま**にした。`roll_over_scene.xml`を丸ごと借りると`<weld body1="head" body2="upper_body"/>`（首の溶接）が付いてきて、**姿勢と溶接の2つが同時に変わり比較が壊れる**（項1＝交絡）。→ 変わるのは**姿勢ただ1つ**。

**0コスト診断（何もしない太郎・`d_supine_check.py`）**：腰の高さが**0.050mのまま20秒間不動**＝完全に安定。新生児のように腕を顔の横に曲げて寝る。**転倒問題は消えた。**

**結果（3シード・3600step）**：

| 条件 | margin | corr | persist | agency |
|---|---|---|---|---|
| 仰向け・触覚なし | +51.4 / +40.0 / +53.1 | 0.54/0.46/0.58 | 71/81/66 | 95/94/99% |
| 立位（既知の全ラン） | +46.2〜+57.0 | 〜0.57 | 66〜81 | 93〜99% |

**仰向けは立位と互角。転倒は必要なかった**（必要だったのは手足の自由）。**仰向けを標準にできる**：転倒の暴れが無く、発達段階として正しく、性能は同じ。

## 触覚は「対象が無い」のではなく「学習が失敗している」（2026-07-15）

**仰向け・触覚あり（3シード）を、必ず分けて測る**（`d_supine_split.py`）。予測対象は固有感覚621＋触覚3606＝4227次元で**85%が触覚**なので、合計で見ると触覚に薄められる。

| seed | 固有感覚(15%) | 触覚(85%) | 合計 |
|---|---|---|---|
| 0 | persist 83.9 / margin **+32.4** | persist 103.7 / margin +7.2 | +14.0 |
| 1 | persist 72.6 / margin **+45.0** | persist 99.7 / margin +9.5 | +19.9 |
| 2 | persist 73.7 / margin **+46.1** | persist 96.9 / margin +12.6 | +23.1 |

**合計だけ見て「触覚ありは+15〜24で明確に劣化」と報告したが撤回**。分けると固有感覚は+32〜46で、**触覚なし(+40〜53)とほぼ同等＝体のマッピングは壊れていない**。**同じ日に同じ間違い（合計で見て薄められる）を2回やった**（D0で1回、仰向けで2回目）。→ 落とし穴チェック項11に追加。

**生き残った所見＝触覚は学べない（persist 97〜104%＝「何も変わらない」予測と互角以下）。**

**原因の切り分け（`d_supine_touch_truth.py`）**：候補は①触覚が行動で変化していない（対象が無い）②変化しているのに学習が失敗、の2つで**打ち手が正反対**。基準線（何もしない太郎）と比べた。

```
学習した太郎の触覚: 平均757.5  幅268.2〜1445.3  変動係数 35.8%
何もしない太郎    : 平均224.8                   変動係数  1.6%   → 22.4倍
「本物の自己接触」（同じ手の中の指どうしを除外）: 29/30 = 97%
   right_upper_arm - upper_body 21回 / left_upper_arm - upper_body 13回
   head - right_upper_arm 13回 / chest - left_hand 10回
```

**→ ②が答え。触覚は22倍動いており、対象は存在する。学習側の問題。**（「仰向けは背中が床に触れっぱなしで触覚がより一定になるのでは」という予想は逆だった。）

**留保**：動画では太郎は**体ごと転がっている**。`right_upper_arm - upper_body` が「触りにいった結果」か「転がって腕が胴体に当たった副産物」かは**区別できていない**。自己接触の創発は主張しない（同日2回誤報告済み）。

## メモリ：太郎1本の90%は「MIMoの表情テクスチャ」だった（2026-07-15）

6本並列で物理メモリ31.7GBのうち**空き773MB・コミット99%**に達した。太郎は**コア律速ではなくメモリ律速**（コアは18個空いていた）。段階ごとに実測（`C/scripts/mem_profile.py`）：

```
import torch                +  146 MB
import gym / mimoEnv        +   88 MB
gym.make（MuJoCo構築）       + 2370 MB   ← 90%
脳＋小脳の構築               +  2.8 MB   ← 太郎の脳の実体は1.3MB
```

mjModelの中を数えると **`tex_data` だけで976MB**＝MIMoの**表情7種**(`tex_head_default/happy/sad/angry/surprised/disgusted/scared` 各2500x15000=107MB)と**服の柄**(各2480x14880=106MB)。**太郎は視覚OFFなのでこれを一度も見ていない。**

**対策** `D/scripts/mimo_lean.py`：ファイル由来のテクスチャを**同じ名前のまま極小の単色に置換**（削除だと`_get_facial_expressions(EMOTES)`が名前で引くので落ちる）。

```
素のまま      RSS=2369.7MB  tex=976.7MB  nv=111 nbody=59 ngeom=78 nu=90  qpos合計=8.308976
11枚を極小化  RSS=  16.3MB  tex=  5.5MB  nv=111 nbody=59 ngeom=78 nu=90  qpos合計=8.308976
```
**145分の1。物理は50step回した後のqpos合計まで完全一致。** さらに**太郎の学習も完全一致**を実測で確認（life=0min行が `56.08/0.43/0.0035/130.59/43.00/101.1` で素のモデルと一致）。

**効果**：1本 2.64GB→**0.28GB**。同時実行 6本→**約22本**（メモリ律速→CPU律速）＝**シードを3から20に増やせる**。今日最大の失敗（n=1でノイズに埋もれた）に直接効く。

**★視覚を足すときの注意**：テクスチャを消すと**太郎が見る映像から顔と服の柄が消える**。Dは「自分の体か相手の体か」を見分ける課題なので、気づかないまま実験すると原因不明の失敗をする。→ **`vision_params is not None` なら自動で素のテクスチャに戻す**安全装置を実装済み（人が覚えておく必要はない）。`C_LEAN=0`で従来のモデルにも戻せる。

## 次の一手：触覚が学べない原因＝「温度や質感が無いから」仮説（2026-07-15）

**ユーザーの仮説**：「人間は質感や温度で、これが人なのか物なのかを判断している」。

**裏づけ（実在の神経機構）**：[Ackerley et al. (2014) J Neurosci](https://www.jneurosci.org/content/34/8/2879) が微小神経電図法で人の神経を直接記録。前腕を 18℃/32℃/42℃ で撫でると、**C触覚線維(CT afferents)は人肌の32℃でのみ最もよく発火**し、**主観的な快さと発火頻度が相関するのも32℃のときだけ**。結論＝CTは「**人肌の温度で、ゆっくり撫でられる**」に特化して調律された、**人と人の肌の接触を知らせる末梢機構**。
分布：CTは毛のある皮膚（前腕・背中・顔）に多く、手のひらは**約7分の1の密度**（[Watkins et al. 2021, J Neurophysiol](https://journals.physiology.org/doi/full/10.1152/jn.00587.2020)。「手のひらに無い」という旧定説は更新された）。

**設計への含意＝人間の触覚は密度勾配が正反対の2系統**：

| | 密度が高い場所 | 用途 |
|---|---|---|
| 識別的触覚(Aβ) | **指先**が密（MIMo実測 指先0.002 vs 下腿0.038＝19倍） | 物の形・質感 |
| 情動的触覚(CT) | **前腕・体幹**が密（手のひらの7倍） | **人**（人肌の温度＋速さ） |

**MIMoが持っているのは前者だけ**＝完全に「物を触るための手」の設計。**人を感じる系統が太郎には1本も無い。** `infant_touch_params()` は部位ごとの密度を係数で操作できるので、**逆勾配のチャンネルを足す**のは構造的に無理がない。

## 触覚が学べない原因を特定：情報不足ではなく「予測対象の形」（2026-07-15）

**問い**：触覚 persist 97〜104%（学べない）の原因は、①学ぶ対象が無い ②情報が足りない ③学習側、のどれか。打ち手が正反対になるので切り分ける。

**ユーザーの仮説**：「人間は質感や温度で、これが人なのか物なのかを判断している」＝今の触覚は「どこかに何かが当たった力」しか伝えず、**何に当たったのかを区別する情報が無い**から予測不能。

**やり方**（`d_touch_predictability.py`）：太郎の実際の課題は「今の状態と行動から**0.5秒後**の触覚を当てる」。そこで**もっと簡単な問題**を出す＝「**今の**関節角度から**今の**触覚を当てる」。関節角度が決まれば体の形は幾何学的に決まるので、原理的には解けるはず。入力を3段階（①固有感覚621 ②＋前庭 ③真の物理状態qpos+qvel＝オラクル）にして、情報がどこで失われるかを見る。

**★測定器を3回作り直した（毎回「ユーザーの仮説を支持する」という答えが出たが、全部偽物だった）**：

| 版 | 落ちた健康診断 | 実際に起きていたこと |
|---|---|---|
| 1版 | 全部負・**オラクルが最悪(-29%)** | 300stepしか学習させておらず単なる収束不足。**情報を増やしたのに成績が下がる**のは収束不足のサイン |
| 2版 | **負例（入力=乱数）が学習R²+90.2%** | 3606次元の出力を3200標本で当てさせ、ネットが入力を無視して**丸暗記**。学習R²は全部ただの暗記だった |
| 3版 | **正例（入力=触覚そのもの＝恒等写像）が検証R²+60.3%** | 時系列で後ろ20%を切ったため、太郎が転がって別姿勢になった区間を検証にしていた＝外挿を要求 |

**最後に効いた対照＝「太郎が学べると分かっているもの（固有感覚）を、同じ測定器に通す」**。従来の正例（恒等写像）は「学習手続きが動くか」しか見ていない。**太郎の課題における正例**が要る。→ 落とし穴チェック項13。

**結果（同じ入力=真の状態225次元・同じデータ・同じ分割・同じ出力次元55、対象だけ差し替え）**：

| 予測対象 | 学習R² | 検証R² |
|---|---|---|
| **固有感覚** | +93.4% | **+27.0%** |
| **触覚** | +77.9% | **−65.6%** |

**差93ポイント。** データ量・容量・分割の厳しさは**固有感覚側にも等しく効いている**のに、固有感覚は正・触覚は負＝**触覚という対象に固有の困難**。（参考：①固有感覚のみ→触覚 −34.1% ②＋前庭 −29.3%。健康診断＝正例(恒等)+96.7% / 負例(乱数)−15.9%で通過。）

**★判定の誤りを修正**：「オラクルが解けない＝情報が無い」と判定してはいけない。**物理が情報の存在を保証している**——関節角度が決まれば体の形は幾何学的に確定し、MuJoCoは決定論的なので (qpos, qvel) から接触力は数学的に一意に決まる。オラクルのR²が負でも意味するのは**「決まってはいるが実質的に学習不能なほど暴れる関数」**。スクリプトの自動判定にこの誤りを書いていたので修正した。

**結論＝ユーザーの仮説は"理由が変わって"生き残る**：
- ❌「情報が足りないから学べない」→ **物理的に成立しない**（情報は100%存在する）
- ⭕「**力だけでは足りない**」→ **実証された**。理由は「接触力が不連続でカオス的」だから。腕が1ミリ動けば接触がON/OFFで反転し力が跳ぶ。**部位ごとに畳んで55次元まで抽象化してもダメ**だった。

**打ち手＝「情報を足す」ではなく「予測対象を、暴れる力から滑らかな量に変える」**。力が学習不能な理由（不連続・カオス）は**温度には当てはまらない**：触れている相手が自分の腕なら、腕が1ミリ動こうが32℃のまま。＝ユーザーの言う「温度」の正体は**予測可能な形をした触覚**だった。

**次の検証**：温度チャンネルを実装し、**同じ測定器・同じ対比**にかける。温度の検証R²が固有感覚(+27%)側に来れば仮説は実証、触覚(−66%)側なら死ぬ。

## 温度チャンネルを実装し、同じ測定器で**棄却**した（2026-07-15）

**実装** `D/scripts/d_thermal.py`（`ThermalWrapper`＝観測に部位ごとの温度55次元を足す。物理には干渉しない）。設計の根拠：
- **人肌32℃・室温22℃**＝Ackerley et al. (2014) のCT線維が最もよく発火した「典型的な皮膚温(neutral)」そのもの。18℃/42℃では発火が落ちる。**恣意的な定数ではなく実験で測られた値**。
- **熱慣性（時定数1.0秒）**＝皮膚には熱容量があり温度は跳ねずに追従する。「力が学習不能な理由（不連続・カオス）」を構造的に持たせないのが狙い。※1.0秒は**根拠の弱い仮置き＝逸脱**。
- **部位ごと55次元**＝CTは密度が低く前腕・体幹に多い（手のひらは約1/7）。per-pointの3606次元は人間から遠い。

**単体テスト（何もしない太郎・仰向け）**：21/55の部位が人肌(1.000)を検出。**温度の変動係数7%（接触力は376%）**＝狙いどおり滑らかになった。

**★結果＝棄却**。同じ入力(真の状態225)・同じデータ・同じ分割・同じ出力次元(55)、変えたのは予測対象だけ：

| 予測対象 | 検証R² |
|---|---|
| 固有感覚（学べると分かっている） | **+26.5%** |
| **温度（今回追加）** | **−53.1%** |
| 接触力（学べない） | **−54.4%** |

**温度は接触力と1.3ポイントしか違わない。滑らかにしても、まったく学べるようにならなかった。**

**なぜ私の推論が外れたか（論点のすり替え）**：「腕が1ミリ動いても触れている相手が自分の腕なら32℃のまま」＝**時間方向**の滑らかさ。しかし予測に必要なのは**姿勢から温度を当てる**こと。温度の値を決めるのは「どこに触れているか」＝**接触そのもの**なので、**温度も姿勢の不連続な関数**になる。**時間について滑らかなことと、姿勢について滑らかなことは別物**だった。温度は接触の読み出し方を変えただけで、**接触の予測困難さをそのまま相続していた**。

**さらに裏側の問題**：熱慣性を入れた＝**0.5秒後の温度は「今の温度」でほぼ当たる**＝persistで勝てない。**滑らかにするほど「何もしない予測」が強くなり、行動依存(margin)が消える。** 温度は両方向から詰んでいた。

**生き残る事実**：接触に由来する信号は、姿勢からの予測が原理的には可能でも実質的に不可能。固有感覚は可能。＝**触覚を予測対象にすること自体が誤り**の可能性。

**次**：`d_train_self.py`（今朝「放棄」と印を付けたスクリプト）にあった設計＝「**触覚は状態zへの入力（文脈）として持つが、予測対象は自分の固有感覚**」。当時は別の理由（制御可能性比という誤った代理指標）で捨てたが、**設計自体は今のデータと整合する**。→ `C_TOUCH_MODE=input` を実装して検証する。

## ★バグ：仰向けのランはシードで再現できていなかった（2026-07-15）

省メモリ化の前後で仰向けseed0の life=0min が **58.99 vs 58.20** とズレた。立位Cでは完全一致していたので省メモリ化のせいではない。真因は `d_supine_env.SupineMimoEnv.reset_model` の：
```python
qpos[7:] += self.np_random.uniform(low=-self._jitter, high=self._jitter, ...)
```
**`self.np_random` は gym が管理する乱数で、`torch.manual_seed(seed)` も `np.random.seed(seed)` も効かない。** ランナーは `env.reset()` をシードなしで呼んでいたので、**毎回ちがう初期姿勢**になっていた。立位Cはjitterが無いため影響を受けず、**仰向けを足すまでこの穴は露呈しなかった**。

→ `env.reset(seed=seed)` に修正。検証：仰向けseed0を2回回して `58.0/+0.5/0.003/131.6/46.0` で完全一致、立位Cも `56.1/+0.4/0.004/130.6/43.0` で従来どおり（壊していない）。

**含意**：**仰向け3シードの比較（margin +51.4/+40.0/+53.1）には、制御されていない乱数が混ざっていた**。18本を回す前に見つかったのは幸い。落とし穴チェック項3（乱数）に「環境の乱数は別系統」を追記すべき。
