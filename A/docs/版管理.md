# 版管理（バージョン履歴）

すべての修正・検証に識別番号をつけて記録する。

---

## 目標A：オウム返し

### A1：最初の試作（DEMO）

| 版 | 変更内容 | 結果 | 状態 |
|----|---------|------|------|
| **A1-1** | 初期モデル。GRU seq2seq風、生成時に親の隠れ状態を引き継がない。出題は「あ」「い」「う」1文字交互 | 「あ」単体：93ターンで成功。3文字交互：2000ターンで失敗（「い」に固着、最大連続4回） | ⚠️ コードは上書き済み。結果は研究日誌に記録 |
| **A1-2** | A1-1の温度減衰を速めた（decay 0.9995→0.998） | 悪化（最大連続2回）。局所解への固着が早まった。**教訓：対症療法で根本原因を見逃した** | ⚠️ コードは上書き済み。結果は研究日誌に記録 |
| **A1-3** | ①出題を人間的フレーズに変更（「まま」「ばぁ」「ないない」）②親の発話の隠れ状態を生成時に引き継ぐよう修正（短期記憶の再現）③ログを実験別フォルダに保存 | 3フレーズ：355ターン・sim29.6分で10回連続達成 ✅ | **現行版** ← gitに保存済み |

#### A1で上書きしてしまったコードについて
A1-1, A1-2のコードはgit導入前に上書きしたため復元できない。変更内容と結果は研究日誌に記録済み。**今後はすべての修正を「コミット→変更→コミット」で記録し、コードレベルで復元可能にする。**

---

### A2：身体的制約の導入（予定）

| 版 | 変更内容（予定） | 状態 |
|----|-----------------|------|
| **A2-1** | 発声体力（赤ちゃんの口・喉・肺の未熟さ）を導入。出力長を体力で制限、発達で伸びる | 未着手 |
| A2-2〜 | 比較実験（本能を1つ抜く）等、A1の知見を踏まえた改良 | 未着手 |

---

## 命名規則

```
A2-3
│ │ └─ 修正番号（1から連番）
│ └── 版番号（1から連番）
└──── 目標（A=オウム返し, B=次の発達段階, ...）
```

- 各修正は**gitコミット**と**この表への追記**で記録する
- **修正前に必ずコミット**してからコードを変える
- 実験ログは `logs/<版名>_<説明>/` に保存

---

*最終更新：2026-06-26*
